---
title: "Espacio columna, rango e independencia"
subtitle: "Álgebra · Lección 3"
nivel: L2
modulo: matematica
fase: F1
prereqs: [matematica/02-matriz-como-transformacion]
siguientes: [matematica/04-producto-interno]
libro: "MML §2.5 Linear Independence · §2.6 Basis and Rank · §2.7 Linear Mappings"
engine: markdown
pyodide:
  packages:
    - numpy
---

::: {.meta-leccion data-leccion="matematica/03-espacio-columna"}
[[L2]{.nivel .nivel-L2} **Derivación**]{.item}
[**Fase** F1]{.item}
[**Libro** MML §2.5–2.7]{.item}
[**Prereqs** [La matriz como transformación](02-matriz-como-transformacion.qmd)]{.item}
:::

## Objetivo

Al terminar esta lección se puede:

1. Definir el [espacio columna]{.g} de una matriz y su [rango]{.g}, y demostrar que $Ax=b$ tiene solución si y solo si $b$ pertenece a ese espacio.
2. Definir el [núcleo]{.g} y demostrar que es un subespacio.
3. Demostrar el [teorema del rango-nulidad]{.g}, $\operatorname{rango}(A)+\dim\ker(A)=n$, y leer en él qué se pierde cuando el rango no es completo.
4. Demostrar que el conjunto de soluciones de $Ax=b$, cuando tiene alguna, es una traslación del núcleo, y reconocer la [colinealidad]{.g} exacta en una matriz de diseño.

:::: {.hilo}
::: {}
#### De dónde viene

- **Álgebra 1**: la combinación lineal, el span y la independencia. El espacio columna no es más que el span de las columnas, con un nombre propio porque se usa constantemente.
- **Álgebra 2**: que $Av$ es una combinación lineal de las columnas de $A$ con los coeficientes de $v$. Toda esta lección es esa frase llevada a sus consecuencias.
:::

::: {}
#### Para qué sirve después

- **Álgebra 5 y 6**: cuando $b$ **no** está en el espacio columna, que es lo normal, lo más cerca que se puede llegar es su proyección, y eso es mínimos cuadrados. El rango columna completo es exactamente la condición de unicidad de la Proposición 6.3.
- **Álgebra 7**: Gram-Schmidt construye una base ortonormal del espacio columna.
- **Álgebra 9**: el núcleo de $A-\lambda I$ es el eigenespacio de $\lambda$; que sea no trivial es la definición de eigenvalor.
- **Estadística 13**: dos predictores linealmente dependientes hacen inidentificables sus coeficientes, y esta lección dice por qué con precisión.
:::
::::

## Notación

::: {.notacion}
| Símbolo | Se lee | Significado |
|---|---|---|
| $A$ | A | Matriz $m\times n$: $m$ filas, $n$ columnas |
| $\operatorname{col}(A)$ | espacio columna de A | El conjunto de todos los $Ax$ posibles: el span de las columnas |
| $\ker(A)$ | núcleo de A | El conjunto de los $x$ con $Ax=0$ |
| $\operatorname{rango}(A)$ | rango de A | La dimensión de $\operatorname{col}(A)$ |
| $n$ | ene | El número de **columnas** de $A$, que es la dimensión del espacio de entrada |
| $m$ | eme | El número de filas, que es la dimensión del espacio de llegada |
| $\lambda$ | lambda | Un escalar; en $A-\lambda I$, el candidato a eigenvalor |
| $z$ | zeta | Un vector del núcleo de $A$ |
:::

## 1. El alcance de una matriz

La lección 2 dejó establecido que $Ax$ es la combinación lineal de las columnas de $A$ con los coeficientes de $x$. Recorrer todos los $x$ posibles genera, entonces, todas las combinaciones lineales posibles de las columnas.

::: {.definicion}
**Definición 3.1 (espacio columna).** $\operatorname{col}(A) = \{Ax : x\in\mathbb{R}^n\}$, que es el span de las columnas de $A$. Es un subespacio de $\mathbb{R}^m$: contiene al cero y es cerrado bajo sumas y múltiplos, porque $A0=0$, $Ax+Ay=A(x+y)$ y $cAx = A(cx)$.
:::

::: {.definicion}
**Definición 3.2 (rango).** $\operatorname{rango}(A) = \dim\operatorname{col}(A)$: cuántas direcciones independientes hay entre las columnas. Siempre $\operatorname{rango}(A)\le\min(m,n)$, y se dice que $A$ tiene **rango columna completo** si $\operatorname{rango}(A)=n$.
:::

::: {.proposicion}
**Proposición 3.3 (cuándo hay solución).** El sistema $Ax=b$ tiene al menos una solución si y solo si $b\in\operatorname{col}(A)$.
:::

::: {.demostracion}
*Demostración.* Si existe $x$ con $Ax=b$, entonces $b$ es de la forma $Ax$ y por la Definición 3.1 pertenece a $\operatorname{col}(A)$. Recíprocamente, si $b\in\operatorname{col}(A)$, por definición existe algún $x$ con $Ax=b$, que es una solución. [∎]{.fin-demo}
:::

La proposición es casi una tautología, y aun así es la que gobierna la mitad de lo que viene: **el espacio columna es todo lo que el modelo puede producir**, y la pregunta de si un objetivo es alcanzable se convierte en una pregunta de pertenencia. El visual deja mover $b$ y cambiar el rango para ver cómo se encoge el alcance.

```{=html}
<div class="viz-col">
  <div class="controles">
    <button type="button" id="co-r2" data-rango="2">Rango 2</button>
    <button type="button" id="co-r1" data-rango="1">Rango 1</button>
    <button type="button" id="co-r0" data-rango="0">Rango 0</button>
    <label style="margin-left:auto"><input type="checkbox" id="co-proy" checked> Mostrar proyección</label>
  </div>
  <svg id="co-svg" viewBox="0 0 560 400" role="img" aria-label="Espacio columna de una matriz y un vector objetivo"></svg>
  <p class="lectura" id="co-lectura"></p>
</div>

<style>
.viz-col {
  border: 1px solid var(--r-linea, #ddd);
  border-radius: 6px; padding: 1rem 1.1rem; margin: 1.6rem 0;
}
.viz-col .controles {
  display: flex; flex-wrap: wrap; gap: .45rem .7rem;
  align-items: center; margin-bottom: .8rem; font-size: .85rem;
}
.viz-col button {
  font-size: .8rem; padding: .25rem .7rem; border-radius: 5px;
  border: 1px solid var(--r-linea, #ccc); background: transparent; color: inherit; cursor: pointer;
}
.viz-col button:hover { border-color: var(--r-acento, #0F6E68); }
.viz-col label { display: flex; align-items: center; gap: .4rem; }
.viz-col svg { width: 100%; height: auto; display: block; touch-action: none; }
.viz-col .lectura { font-size: .85rem; margin-top: .7rem; min-height: 3em; }
</style>

<script>
(function () {
  var C1 = "#2563EB", C2 = "#C2410C", COL = "#0F6E68", B = "#7C3AED";
  var X0 = -4, X1 = 4, Y0 = -2.8, Y1 = 2.8, W_ = 560, H_ = 400, M_ = 24;
  var sx = function (x) { return M_ + ((x - X0) / (X1 - X0)) * (W_ - 2*M_); };
  var sy = function (y) { return H_ - M_ - ((y - Y0) / (Y1 - Y0)) * (H_ - 2*M_); };
  var ix = function (px) { return X0 + ((px - M_) / (W_ - 2*M_)) * (X1 - X0); };
  var iy = function (py) { return Y0 + ((H_ - M_ - py) / (H_ - 2*M_)) * (Y1 - Y0); };

  var RANGOS = { 2: [[2, -0.8], [0.6, 1.6]], 1: [[2, 1], [1, 0.5]], 0: [[0, 0], [0, 0]] };
  var c1 = RANGOS[1][0].slice(), c2 = RANGOS[1][1].slice();
  var b = [1.2, 2.1], arrastrando = false;

  var svg = document.getElementById("co-svg");
  var lec = document.getElementById("co-lectura");
  var verProy = document.getElementById("co-proy");

  function el(t, at) {
    var n = document.createElementNS("http://www.w3.org/2000/svg", t);
    for (var k in at) n.setAttribute(k, at[k]);
    return n;
  }
  function det() { return c1[0]*c2[1] - c1[1]*c2[0]; }
  function norma(v) { return Math.hypot(v[0], v[1]); }

  function rango() {
    if (norma(c1) < 1e-9 && norma(c2) < 1e-9) return 0;
    return Math.abs(det()) > 1e-9 ? 2 : 1;
  }
  // proyección ortogonal de b sobre col(A)
  function proyectar() {
    var r = rango();
    if (r === 2) return b.slice();
    if (r === 0) return [0, 0];
    var d = norma(c1) > 1e-9 ? c1 : c2;
    var t = (b[0]*d[0] + b[1]*d[1]) / (d[0]*d[0] + d[1]*d[1]);
    return [t*d[0], t*d[1]];
  }

  function dibujar() {
    svg.textContent = "";
    var defs = el("defs", {});
    [[C1,"k1"],[C2,"k2"],[B,"kb"]].forEach(function (c) {
      var m = el("marker", { id: "co-" + c[1], viewBox: "0 -5 10 10", refX: 8,
        markerWidth: 5, markerHeight: 5, orient: "auto" });
      m.appendChild(el("path", { d: "M0,-4L9,0L0,4", fill: c[0] }));
      defs.appendChild(m);
    });
    svg.appendChild(defs);

    for (var g = -4; g <= 4; g++) {
      svg.appendChild(el("line", { x1: sx(g), y1: sy(Y0), x2: sx(g), y2: sy(Y1),
        stroke: "currentColor", "stroke-width": g===0?1.2:.5, opacity: g===0?.4:.11 }));
      svg.appendChild(el("line", { x1: sx(X0), y1: sy(g), x2: sx(X1), y2: sy(g),
        stroke: "currentColor", "stroke-width": g===0?1.2:.5, opacity: g===0?.4:.11 }));
    }

    var r = rango(), p = proyectar();

    // el espacio columna
    if (r === 2) {
      svg.appendChild(el("rect", { x: M_, y: M_, width: W_-2*M_, height: H_-2*M_,
        fill: COL, opacity: .08 }));
    } else if (r === 1) {
      var d = norma(c1) > 1e-9 ? c1 : c2, k = 12 / norma(d);
      svg.appendChild(el("line", { x1: sx(-k*d[0]), y1: sy(-k*d[1]),
        x2: sx(k*d[0]), y2: sy(k*d[1]), stroke: COL, "stroke-width": 9, opacity: .2 }));
    } else {
      svg.appendChild(el("circle", { cx: sx(0), cy: sy(0), r: 9, fill: COL, opacity: .3 }));
    }

    // columnas
    [[c1,C1,"k1"],[c2,C2,"k2"]].forEach(function (par) {
      if (norma(par[0]) < 1e-9) return;
      svg.appendChild(el("line", { x1: sx(0), y1: sy(0), x2: sx(par[0][0]), y2: sy(par[0][1]),
        stroke: par[1], "stroke-width": 2.4, "marker-end": "url(#co-" + par[2] + ")" }));
    });

    var dist = Math.hypot(b[0]-p[0], b[1]-p[1]);
    var alcanzable = dist < 1e-6;

    // residuo: de la proyección a b
    if (verProy.checked && !alcanzable) {
      svg.appendChild(el("line", { x1: sx(p[0]), y1: sy(p[1]), x2: sx(b[0]), y2: sy(b[1]),
        stroke: "#B91C1C", "stroke-width": 1.6, "stroke-dasharray": "4,3" }));
      svg.appendChild(el("circle", { cx: sx(p[0]), cy: sy(p[1]), r: 5,
        fill: COL, stroke: "#fff", "stroke-width": 1.5 }));
    }

    // b, arrastrable
    svg.appendChild(el("line", { x1: sx(0), y1: sy(0), x2: sx(b[0]), y2: sy(b[1]),
      stroke: B, "stroke-width": 2.4, "marker-end": "url(#co-kb)" }));
    var cb = el("circle", { cx: sx(b[0]), cy: sy(b[1]), r: 9, fill: B,
      "fill-opacity": .25, stroke: B, "stroke-width": 1.6, cursor: "grab" });
    cb.addEventListener("pointerdown", function (e) {
      arrastrando = true; svg.setPointerCapture(e.pointerId); e.preventDefault();
    });
    svg.appendChild(cb);
    var tb = el("text", { x: sx(b[0]) + 12, y: sy(b[1]) - 8, fill: B,
      "font-size": 14, "font-style": "italic" });
    tb.textContent = "b";
    svg.appendChild(tb);

    var nombre = r === 2 ? "todo el plano ℝ²" : r === 1 ? "una recta" : "solo el origen";
    lec.innerHTML =
      'rango(A) = <b>' + r + '</b> · col(A) es <b>' + nombre + '</b> · dim del núcleo = <b>' + (2-r) + '</b><br>' +
      (alcanzable
        ? '<b style="color:' + COL + '">Ax = b tiene solución exacta.</b> b está dentro del alcance de A.'
        : '<b style="color:#B91C1C">Ax = b no tiene solución.</b> b queda fuera de col(A) a distancia ' +
          dist.toFixed(3) + '. Lo más cerca que puedes llegar es el punto verde — eso <i>es</i> mínimos cuadrados.');
  }

  svg.addEventListener("pointermove", function (e) {
    if (!arrastrando) return;
    var caja = svg.getBoundingClientRect();
    b = [Math.max(X0, Math.min(X1, ix((e.clientX-caja.left)*(W_/caja.width)))),
         Math.max(Y0, Math.min(Y1, iy((e.clientY-caja.top)*(H_/caja.height))))];
    dibujar();
  });
  svg.addEventListener("pointerup", function () { arrastrando = false; });
  svg.addEventListener("pointerleave", function () { arrastrando = false; });
  verProy.addEventListener("change", dibujar);
  document.querySelectorAll(".viz-col [data-rango]").forEach(function (btn) {
    btn.addEventListener("click", function () {
      var m = RANGOS[btn.dataset.rango];
      c1 = m[0].slice(); c2 = m[1].slice(); dibujar();
    });
  });
  dibujar();
})();
</script>
```

Con rango 2 el alcance es todo el plano y cualquier $b$ tiene solución exacta. Con rango 1 se encoge a una recta, y con rango 0 al origen: la matriz no puede producir nada. En los dos últimos casos el punto verde, la proyección de la lección 5, es lo más cerca que se puede llegar.

## 2. El núcleo, y lo que la matriz aniquila

::: {.definicion}
**Definición 3.4 (núcleo).** $\ker(A) = \{x\in\mathbb{R}^n : Ax = 0\}$, el conjunto de vectores que la matriz manda al origen.
:::

::: {.proposicion}
**Proposición 3.5.** $\ker(A)$ es un subespacio de $\mathbb{R}^n$.
:::

::: {.demostracion}
*Demostración.* Contiene al cero, porque $A0=0$. Si $Ax=0$ y $Ay=0$, entonces $A(x+y)=Ax+Ay=0$ y $A(cx)=cAx=0$ para todo escalar $c$; el conjunto es cerrado bajo sumas y múltiplos. [∎]{.fin-demo}
:::

Cuando el rango baja, el núcleo crece, y lo hacen en cantidades exactamente complementarias. El teorema siguiente lo enuncia.

::: {.proposicion}
**Teorema 3.6 (rango-nulidad).** Para toda $A$ de tamaño $m\times n$,
$$\operatorname{rango}(A) + \dim\ker(A) = n.$$
:::

::: {.demostracion}
*Demostración.* Sea $k=\dim\ker(A)$ y tómese una base $\{u_1,\dots,u_k\}$ del núcleo. Extiéndase a una base de todo $\mathbb{R}^n$ añadiendo $\{w_1,\dots,w_{n-k}\}$; siempre se puede, porque todo conjunto independiente se completa a una base. La demostración consiste en ver que las imágenes $\{Aw_1,\dots,Aw_{n-k}\}$ son una base de $\operatorname{col}(A)$.

**Generan.** Cualquier $x\in\mathbb{R}^n$ se escribe $x=\sum_i a_iu_i + \sum_j b_jw_j$. Aplicando $A$ y usando $Au_i=0$,
$$Ax = \sum_j b_j\,Aw_j,$$
de modo que todo elemento de $\operatorname{col}(A)$ es combinación de los $Aw_j$.

**Son independientes.** Supóngase $\sum_j c_jAw_j = 0$. Por linealidad, $A\big(\sum_j c_jw_j\big)=0$, así que $\sum_j c_jw_j\in\ker(A)$ y por tanto se escribe como $\sum_i d_iu_i$ para ciertos $d_i$. Entonces
$$\sum_j c_jw_j - \sum_i d_iu_i = 0$$
es una combinación lineal nula de vectores de una base de $\mathbb{R}^n$, de modo que todos los coeficientes son cero; en particular todos los $c_j$.

Hay $n-k$ vectores en esa base, luego $\operatorname{rango}(A) = n-k = n-\dim\ker(A)$. [∎]{.fin-demo}
:::

Léase en una matriz de datos: si hay $n=10$ predictores y el rango es 8, el núcleo tiene dimensión 2, y eso significa que hay dos direcciones de combinaciones de predictores que **no cambian ninguna predicción**. Los coeficientes en esas direcciones son inidentificables: el ajuste devolverá unos números cualesquiera de entre infinitos igual de válidos, sin avisar.

::: {.proposicion}
**Proposición 3.7 (la forma del conjunto de soluciones).** Si $Ax_0=b$, entonces el conjunto de todas las soluciones de $Ax=b$ es
$$\{x_0 + z : z\in\ker(A)\}.$$
En particular, la solución es única si y solo si $\ker(A)=\{0\}$, es decir si y solo si $A$ tiene rango columna completo.
:::

::: {.demostracion}
*Demostración.* Si $z\in\ker(A)$ entonces $A(x_0+z) = Ax_0 + Az = b+0 = b$, así que todo punto de ese conjunto es solución. Recíprocamente, si $Ax_1=b$, entonces $A(x_1-x_0)=b-b=0$, de modo que $z=x_1-x_0\in\ker(A)$ y $x_1=x_0+z$. La unicidad equivale entonces a que el único $z$ posible sea el cero, y por el Teorema 3.6 eso ocurre exactamente cuando $\operatorname{rango}(A)=n$. [∎]{.fin-demo}
:::

Conviene fijarse en la estructura: el conjunto de soluciones no es un subespacio, no contiene al cero salvo si $b=0$, sino un subespacio **trasladado**. Esa es la razón de que un sistema tenga cero, una o infinitas soluciones, y nunca exactamente dos.

## 3. Dependencia lineal en una matriz de datos

El rango deficiente exacto no aparece por casualidad en datos medidos: aparece cuando una columna se construye a partir de otras. Y hay que ser preciso con qué significa «a partir de otras».

```{pyodide}
import numpy as np

# Tres columnas: peso, altura y el indice de masa corporal, que se calcula
# con las otras dos. Parece que sobra una columna, pero la relacion es
# peso / altura^2, que NO es una combinacion lineal.
rng = np.random.default_rng(1)
peso = rng.normal(72, 12, 50)          # kg
altura = rng.normal(1.70, 0.09, 50)    # m
imc = peso / altura ** 2

A_derivada = np.column_stack([peso, altura, imc])
A_nueva = np.column_stack([peso, altura, rng.normal(0, 1, 50)])

print("rango con el IMC derivado :", np.linalg.matrix_rank(A_derivada))
print("rango con una columna nueva:", np.linalg.matrix_rank(A_nueva))
print()
print("valores singulares (derivada):", np.linalg.svd(A_derivada, compute_uv=False).round(3))
print()
print("El rango sigue siendo 3: peso/altura^2 es una relacion, pero no LINEAL.")
print("La dependencia lineal exige que una columna sea suma de multiplos de otras.")
```

Lo que sí produce rango deficiente exacto es una construcción muy común: incluir todas las categorías de una variable indicadora junto con el intercepto.

```{pyodide}
import numpy as np

# ocho observaciones repartidas en tres grupos, codificadas como indicadoras
grupo = np.array([0, 1, 2, 0, 1, 2, 0, 1])
D = np.eye(3)[grupo]                                  # una columna por grupo
intercepto = np.ones((len(grupo), 1))

X_trampa = np.column_stack([intercepto, D])           # 4 columnas
X_bien = np.column_stack([intercepto, D[:, 1:]])      # se descarta una categoria

print("X con todas las indicadoras:", X_trampa.shape, "-> rango",
      np.linalg.matrix_rank(X_trampa), " -> dim ker =", X_trampa.shape[1] - np.linalg.matrix_rank(X_trampa))
print("X descartando una          :", X_bien.shape, "  -> rango",
      np.linalg.matrix_rank(X_bien), " -> dim ker =", X_bien.shape[1] - np.linalg.matrix_rank(X_bien))
print()
print("suma de las indicadoras por fila:", D.sum(axis=1))
print("identica a la columna del intercepto: esa es la dependencia lineal exacta.")
print()
# el nucleo, explicito: el vector que aniquila a X_trampa
z = np.array([1.0, -1.0, -1.0, -1.0])
print("X_trampa @ [1, -1, -1, -1] =", (X_trampa @ z).round(12))
print("-> sumarle ese vector a cualquier solucion da otra solucion (Proposicion 3.7)")
```

La última línea es el Teorema 3.6 en números: el rango es 3 con cuatro columnas, luego el núcleo tiene dimensión 1, y ahí está su generador. Sumarle ese vector a cualquier vector de coeficientes da otro que predice **exactamente lo mismo**: subir el intercepto en una unidad y bajar las tres indicadoras en una se cancela en cada fila.

## Ejercicios

### Ejercicio 1, Rango y núcleo

Calcular el rango de la matriz y deducir la dimensión de su núcleo con el Teorema 3.6.

```{pyodide}
#| exercise: ex_rango
import numpy as np

A = np.array([[1.0, 2.0, 3.0],
              [2.0, 4.0, 6.0],
              [1.0, 1.0, 1.0]])

r = ______
dim_nucleo = ______

print(f"rango = {r}, dim ker = {dim_nucleo}")
(r, dim_nucleo)
```

::: {.hint exercise="ex_rango"}
`np.linalg.matrix_rank(A)` da el rango. El teorema dice rango + dim ker = n, donde n es el **número de columnas**: `A.shape[1]`. La segunda fila es el doble de la primera.
:::

::: {.solution exercise="ex_rango"}
```{pyodide}
import numpy as np

A = np.array([[1.0, 2.0, 3.0],
              [2.0, 4.0, 6.0],
              [1.0, 1.0, 1.0]])

r = np.linalg.matrix_rank(A)
dim_nucleo = A.shape[1] - r

print(f"rango = {r}, dim ker = {dim_nucleo}")
(r, dim_nucleo)
```
:::

```{pyodide}
#| exercise: ex_rango
#| check: true
if tuple(int(v) for v in result) == (2, 1):
    feedback = {"correct": True, "message": "Correcto. La fila 2 es el doble de la fila 1, así que el rango cae a 2 y queda una dirección aniquilada.", "type": "success"}
else:
    feedback = {"correct": False, "message": f"Esperaba (2, 1) y obtuve {result}. Recuerda: n es el número de COLUMNAS.", "type": "error"}
feedback
```

### Ejercicio 2, ¿Está b en el alcance?

Decidir si el sistema tiene solución exacta comparando rangos, que es la Proposición 3.3 llevada a la práctica.

```{pyodide}
#| exercise: ex_alcance
import numpy as np

A = np.array([[1.0, 2.0],
              [2.0, 4.0],
              [3.0, 6.0]])
b = np.array([1.0, 2.0, 4.0])

def tiene_solucion(A, b):
    aumentada = np.column_stack([A, b])
    return ______

print(tiene_solucion(A, b))
tiene_solucion(A, b)
```

::: {.hint exercise="ex_alcance"}
Teorema de Rouché-Frobenius: hay solución exactamente cuando agregar `b` como columna extra **no** aumenta el rango. Compara `matrix_rank(A)` contra `matrix_rank(aumentada)`.
:::

::: {.solution exercise="ex_alcance"}
```{pyodide}
import numpy as np

A = np.array([[1.0, 2.0],
              [2.0, 4.0],
              [3.0, 6.0]])
b = np.array([1.0, 2.0, 4.0])

def tiene_solucion(A, b):
    aumentada = np.column_stack([A, b])
    return np.linalg.matrix_rank(A) == np.linalg.matrix_rank(aumentada)

print(tiene_solucion(A, b))
tiene_solucion(A, b)
```
:::

```{pyodide}
#| exercise: ex_alcance
#| check: true
if result is False or result == False:
    feedback = {"correct": True, "message": "Correcto: col(A) es la recta generada por (1,2,3), y b=(1,2,4) no está en ella. Rango 1 contra rango 2 en la aumentada.", "type": "success"}
else:
    feedback = {"correct": False, "message": f"Esperaba False. Las columnas de A son proporcionales, así que col(A) es una recta; b se sale de ella. Obtuve {result}.", "type": "error"}
feedback
```

## Reto

En `proyectos/notebooks/F1-retos.ipynb`, sección **Mat 3**:

1. Escribir `rango_por_escalonamiento(A, tol)` que calcule el rango por eliminación gaussiana con pivoteo parcial, sin usar `matrix_rank` ni `svd`. Compararla contra NumPy en cincuenta matrices aleatorias, incluidas algunas de rango deficiente construidas a propósito.
2. Armar una matriz de diseño con intercepto y las indicadoras completas de una variable de cuatro categorías. Confirmar que el rango es 4 y no 5, y encontrar a mano el generador del núcleo. Después ajustar un modelo lineal con esa matriz: ¿falla, avisa, o devuelve coeficientes en silencio? Anotar la respuesta en `50-Errores/`.
3. Generar $A$ de $100\times20$ con rango exacto 15 y calcular sus valores singulares. ¿Cuántos son «cero»? ¿Qué tan cerca de cero, en realidad? Eso explica por qué `matrix_rank` necesita una tolerancia, y por qué el rango numérico no es una propiedad binaria.

## Del libro

*Mathematics for Machine Learning* cubre este material en §2.5 *Linear Independence*, §2.6 *Basis and Rank* y §2.7 *Linear Mappings*. El libro tiene copyright y no es descargable desde el entorno en que se escribió esta página, así que se cita número y título y no se le atribuye ninguna frase.

**Preguntas para leer §2.5–2.7 con lápiz:**

1. MML define el rango para filas y para columnas por separado y después demuestra que coinciden. ¿En qué punto de la demostración del Teorema 3.6 se usaría ese hecho, si se quisiera enunciar el teorema en términos de filas?
2. §2.6 define base y dimensión. ¿Qué parte de la demostración del Teorema 3.6 depende de que toda familia independiente se pueda **extender** a una base?
3. §2.7 trata las aplicaciones lineales en abstracto, sin matrices. ¿Qué es el núcleo y qué es la imagen en ese lenguaje, y cómo se traduce el Teorema 3.6?

## Para el Cerebro

Nota nueva en `10-Conceptos/espacio-columna.md`, enlazada a [[combinacion-lineal]], [[proyeccion-ortogonal]] y [[minimos-cuadrados]]. El Teorema 3.6 conviene rehacerlo a mano: es el primero del plan cuya demostración construye una base en vez de calcular, y ese salto conviene darlo despacio una vez.

```
¿Qué es el espacio columna de A?::El conjunto de todos los Ax posibles: el span de las columnas
¿Qué es el rango?::La dimensión del espacio columna: cuántas direcciones independientes hay entre las columnas
¿Cuándo tiene solución Ax = b?::Exactamente cuando b está en col(A) (Proposición 3.3)
¿Qué es el núcleo de A?::El conjunto de los x con Ax = 0; es un subespacio
¿Qué dice el teorema del rango-nulidad?::rango(A) + dim ker(A) = n, el número de COLUMNAS
¿Cómo se demuestra?::Extendiendo una base del núcleo a una base del espacio de entrada y viendo que las imágenes de los vectores añadidos son base de col(A)
¿Qué forma tiene el conjunto de soluciones de Ax = b?::x₀ + ker(A): un subespacio trasladado, no un subespacio
¿Cuándo es única la solución?::Cuando ker(A) = {0}, es decir con rango columna completo
¿Por qué un sistema nunca tiene exactamente dos soluciones?::Porque si tiene dos, su diferencia está en el núcleo, y entonces hay infinitas
¿Crea colinealidad exacta una columna derivada como peso/altura²?::No: la dependencia tiene que ser lineal, y una división no lo es
¿Qué sí crea rango deficiente exacto?::Incluir todas las indicadoras de una categoría junto con el intercepto: suman uno en cada fila
```

::: {#fuentes .fuentes}
### Fuentes

**Lo que esta página demuestra sola.** Las Proposiciones 3.3, 3.5 y 3.7 y el Teorema 3.6 se demuestran aquí, a partir de la linealidad de $A$ y de la noción de base de la lección 1. Se comprueban además numéricamente: que una columna derivada de forma no lineal, el índice de masa corporal a partir de peso y altura, **no** baja el rango, y que las indicadoras completas junto al intercepto sí lo bajan, con el generador del núcleo escrito explícitamente y comprobado. El visual es el de la versión anterior, al que se le añadieron identificadores a los botones para que el gate de visuales pueda accionarlos.

**Lo que se usa de otras lecciones sin repetir.** La combinación lineal, el span y la independencia son la lección 1. Que $Ax$ combina las columnas de $A$ es la lección 2. Que toda familia independiente se extiende a una base se usa en la demostración del Teorema 3.6 y viene de la lección 1.

**Lo que se enuncia sin demostrar.** Que el rango por filas y por columnas coinciden; aquí solo se usa el de columnas, y la igualdad no hace falta en ninguna demostración de esta página.

**Lo que viene de los libros.**

- [*Mathematics for Machine Learning*](https://mml-book.github.io/book/mml-book.pdf) (Deisenroth, Faisal & Ong, Cambridge University Press, 2020), §2.5 *Linear Independence*, §2.6 *Basis and Rank* y §2.7 *Linear Mappings* — citadas por número y título, sin transcribir texto.

**Lo que es mío, no del libro.** La Proposición 3.7 presentada como «el conjunto de soluciones es el núcleo trasladado» y usada para explicar por qué un sistema nunca tiene dos soluciones; y los dos ejemplos de la sección 3, que separan la dependencia lineal de la mera relación funcional entre columnas.

[Índices verificados el 12-09-2026 contra el índice publicado del PDF oficial.]{.sello}
:::

→ Siguiente: [Producto interno y ángulo](04-producto-interno.qmd)
