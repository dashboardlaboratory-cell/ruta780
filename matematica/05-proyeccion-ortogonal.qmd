---
title: "Proyección ortogonal"
subtitle: "Álgebra · Lección 5"
nivel: L2
modulo: matematica
fase: F1
prereqs: [matematica/04-producto-interno]
siguientes: [matematica/06-minimos-cuadrados]
libro: "MML §3.5 Orthonormal Basis · §3.6 Orthogonal Complement · §3.8 Orthogonal Projections"
engine: markdown
pyodide:
  packages:
    - numpy
---

::: {.meta-leccion data-leccion="matematica/05-proyeccion-ortogonal"}
[[L2]{.nivel .nivel-L2} **Derivación**]{.item}
[**Fase** F1]{.item}
[**Libro** MML §3.5 · §3.6 · §3.8]{.item}
[**Prereqs** [Producto interno](04-producto-interno.qmd)]{.item}
:::

## Objetivo

Al terminar esta lección se puede:

1. Derivar la fórmula de la [proyección ortogonal]{.g} sobre una recta a partir de una sola condición: que el [residuo]{.g} sea perpendicular.
2. Demostrar que la proyección es el punto **más cercano** del subespacio, usando el teorema de Pitágoras de la lección 4.
3. Demostrar que la [matriz de proyección]{.g} cumple $P^2=P$ y $P^\top=P$, que sus eigenvalores son ceros y unos, y que su traza es su rango.
4. Generalizar de una recta a un subespacio cualquiera con las [ecuaciones normales]{.g} $A^\top A\hat{x} = A^\top b$, y reconocer que centrar una variable es un caso particular de todo esto.

:::: {.hilo}
::: {}
#### De dónde viene

- **Álgebra 4**: el producto interno, la ortogonalidad (Definición 4.3) y sobre todo la Proposición 4.4 —Pitágoras—, que es la que convierte «perpendicular» en «el más cercano».
- **Álgebra 3**: el espacio columna de una matriz. Proyectar sobre un subespacio es proyectar sobre el espacio columna de la matriz que lo genera.
:::

::: {}
#### Para qué sirve después

- **Álgebra 6**: mínimos cuadrados es exactamente esta lección aplicada al vector de observaciones y al espacio columna de la matriz de diseño; las ecuaciones normales de la Proposición 5.7 son las de allí.
- **Álgebra 7**: Gram-Schmidt construye una base ortonormal restando proyecciones, una a una.
- **Estadística 13 y 14**: la recta de regresión es una proyección, los residuos son el vector $b-p$, y la traza de $P$ es el número de coeficientes ajustados —los grados de libertad que se restan en $\hat{\sigma}^2$—.
- **Estadística 4 y 5**: centrar una variable, que allí es «restarle la media», aquí resulta ser la proyección sobre el complemento ortogonal del vector de unos (Proposición 5.9).
:::
::::

## Notación

::: {.notacion}
| Símbolo | Se lee | Significado |
|---|---|---|
| $a$, $b$ | a, be | Vectores de $\mathbb{R}^n$: $a$ genera la recta sobre la que se proyecta, $b$ es lo que se proyecta |
| $p$ | pe | La proyección de $b$: la sombra que cae sobre el subespacio |
| $e$ | e | El residuo, $e = b - p$. **Aquí es eso**; en Estadística 13 es el residuo de la regresión, que es el mismo objeto |
| $A$ | A | Matriz $n\times k$ cuyas columnas generan el subespacio |
| $P$ | pe mayúscula | La matriz de proyección: $p = Pb$ para todo $b$ |
| $\mathbf{1}$ | uno | El vector de unos, $(1,1,\dots,1)^\top$ |
| $\hat{x}$ | equis sombrero | Los coeficientes de la proyección en la base de columnas: $p = A\hat{x}$ |
:::

## 1. La sombra sobre una recta

::: {.definicion}
**Definición 5.1 (proyección sobre una recta).** Sea $a\ne 0$. La **proyección ortogonal** de $b$ sobre la recta $\{ta : t\in\mathbb{R}\}$ es el único punto $p$ de esa recta tal que $b-p$ es ortogonal a $a$.
:::

La definición no dice cómo calcular $p$; dice qué propiedad lo caracteriza. La fórmula sale de imponerla.

::: {.proposicion}
**Proposición 5.2 (la fórmula).** Ese punto existe, es único, y vale
$$p = \frac{\langle a,b\rangle}{\langle a,a\rangle}\,a \;=\; \frac{a\,a^\top}{a^\top a}\,b.$$
:::

::: {.demostracion}
*Demostración.* Todo punto de la recta es de la forma $ta$, así que basta encontrar el escalar $t$. La condición de la Definición 5.1 es $\langle a,\, b-ta\rangle = 0$, y por linealidad del producto interno (Definición 4.1),
$$\langle a,b\rangle - t\,\langle a,a\rangle = 0 \quad\Longrightarrow\quad t = \frac{\langle a,b\rangle}{\langle a,a\rangle},$$
donde la división es legítima porque $\langle a,a\rangle = \|a\|^2 > 0$ al ser $a\ne 0$ y el producto interno definido positivo. El valor de $t$ queda determinado sin ninguna libertad, de modo que $p=ta$ existe y es único. Para la segunda forma, por asociatividad del producto de matrices,
$$\frac{\langle a,b\rangle}{\langle a,a\rangle}a = \frac{a\,(a^\top b)}{a^\top a} = \frac{(a\,a^\top)\,b}{a^\top a},$$
que es multiplicar $b$ por una matriz fija, la misma para todo $b$. [∎]{.fin-demo}
:::

La condición de perpendicularidad se eligió por su significado geométrico, pero lo que la vuelve **la** definición correcta es que produce el punto más cercano.

::: {.proposicion}
**Proposición 5.3 (mejor aproximación).** Para todo $t\in\mathbb{R}$,
$$\|b - ta\|^2 = \|b-p\|^2 + \|p - ta\|^2 \;\ge\; \|b-p\|^2,$$
con igualdad si y solo si $ta=p$. La proyección es, por tanto, el punto de la recta que minimiza la distancia a $b$.
:::

::: {.demostracion}
*Demostración.* Escríbase $b-ta = (b-p) + (p-ta)$. El primer sumando es el residuo $e$, ortogonal a $a$ por la Definición 5.1; el segundo es un múltiplo de $a$, porque tanto $p$ como $ta$ lo son. Dos vectores así son ortogonales entre sí: $\langle e,\, p-ta\rangle = 0$. Aplicando la Proposición 4.4 —Pitágoras— a esa descomposición,
$$\|b-ta\|^2 = \|b-p\|^2 + \|p-ta\|^2.$$
El segundo sumando es no negativo y vale cero exactamente cuando $ta=p$. [∎]{.fin-demo}
:::

Merece subrayarse de dónde viene el mínimo: **no** de derivar e igualar a cero, sino de Pitágoras. Optimizar aquí es geometría, no cálculo, y esa es la razón de que la solución sea única y exacta en vez de aproximada.

```{=html}
<div class="viz-proy">
  <div class="controles">
    <label>Dirección de b <input id="pr-ang" type="range" min="0" max="358" step="2" value="62"> <span id="pr-ang-v">62°</span></label>
    <label>Punto de prueba t <input id="pr-t" type="range" min="-40" max="140" step="5" value="115"> <span id="pr-t-v">1,15</span></label>
  </div>
  <svg id="pr-svg" viewBox="0 0 620 320" role="img" aria-label="A la izquierda, el vector b y la recta generada por a: se dibujan la sombra de b sobre la recta, el residuo perpendicular y un punto de prueba que se mueve por la recta. A la derecha, la distancia de b a cada punto de la recta en funcion del coeficiente, una parabola cuyo minimo cae exactamente en la sombra."></svg>
  <p class="lectura" id="pr-lectura"></p>
</div>
<style>
.viz-proy { border: 1px solid var(--r-linea, #ddd); border-radius: 10px; padding: 1rem 1.1rem; margin: 1.7rem 0; }
.viz-proy .controles { display: flex; flex-wrap: wrap; gap: .45rem 1.1rem; align-items: center; margin-bottom: .85rem; font-size: .85rem; }
.viz-proy label { display: flex; align-items: center; gap: .4rem; }
.viz-proy input[type=range] { width: 120px; padding: 0; }
.viz-proy svg { width: 100%; height: auto; display: block; }
.viz-proy .lectura { font-size: .85rem; margin-top: .75rem; min-height: 5.2em; line-height: 1.6; }
.viz-proy .lectura .fx { font-family: var(--bs-font-monospace, monospace); font-size: .95em; }
</style>
<script>
(function () {
  var C_A = "#0F6E68", C_B = "#B45309", C_P = "#0F6E68", C_E = "#B91C1C", C_TEO = "var(--r-tinta,#1f2430)";
  var svg = document.getElementById("pr-svg"), lec = document.getElementById("pr-lectura");
  var ctlAng = document.getElementById("pr-ang"), etqAng = document.getElementById("pr-ang-v");
  var ctlT = document.getElementById("pr-t"), etqT = document.getElementById("pr-t-v");

  // Todo el marco sale de constantes: a fijo, limites fijos, escala fija.
  var A = [2.6, 0.7], LARGO_B = 2.4;
  var X0 = -3.2, X1 = 4.4, Y0 = -1.6, Y1 = 2.8;
  var PX0 = 32, PX1 = 352, PY0 = 262, PY1 = 30;
  var QX0 = 404, QX1 = 604, QY0 = 262, QY1 = 44, DMAX = 5;

  function el(t, at, txt) {
    var e = document.createElementNS("http://www.w3.org/2000/svg", t);
    for (var k in at) e.setAttribute(k, at[k]);
    if (txt !== undefined) e.textContent = txt;
    return e;
  }
  function px(x) { return PX0 + (x - X0) / (X1 - X0) * (PX1 - PX0); }
  function py(y) { return PY0 - (y - Y0) / (Y1 - Y0) * (PY0 - PY1); }
  function qx(t) { return QX0 + (t + 0.4) / 1.8 * (QX1 - QX0); }
  function qy(d) { return QY0 - Math.min(d, DMAX) / DMAX * (QY0 - QY1); }
  function punto(u, v) { return u[0] * v[0] + u[1] * v[1]; }
  function norma(u) { return Math.hypot(u[0], u[1]); }

  function flecha(v, color, ancho, etq) {
    svg.appendChild(el("line", { x1: px(0), y1: py(0), x2: px(v[0]).toFixed(1), y2: py(v[1]).toFixed(1),
      stroke: color, "stroke-width": ancho }));
    var a = Math.atan2(py(v[1]) - py(0), px(v[0]) - px(0)), L = 9;
    svg.appendChild(el("path", { d: "M" + px(v[0]).toFixed(1) + "," + py(v[1]).toFixed(1) +
      " L" + (px(v[0]) - L * Math.cos(a - 0.4)).toFixed(1) + "," + (py(v[1]) - L * Math.sin(a - 0.4)).toFixed(1) +
      " L" + (px(v[0]) - L * Math.cos(a + 0.4)).toFixed(1) + "," + (py(v[1]) - L * Math.sin(a + 0.4)).toFixed(1) + " Z",
      fill: color }));
    if (etq) svg.appendChild(el("text", { x: px(v[0]) + 9, y: py(v[1]) - 5, "font-size": 12, "font-weight": 600, fill: color }, etq));
  }

  function dibujar() {
    while (svg.firstChild) svg.removeChild(svg.firstChild);
    var ang = +ctlAng.value, tPrueba = +ctlT.value / 100, i;
    etqAng.textContent = ang + "°";
    etqT.textContent = tPrueba.toFixed(2).replace(".", ",");
    var B = [LARGO_B * Math.cos(ang * Math.PI / 180), LARGO_B * Math.sin(ang * Math.PI / 180)];
    var t = punto(A, B) / punto(A, A);
    var P = [t * A[0], t * A[1]], E = [B[0] - P[0], B[1] - P[1]];
    var Q = [tPrueba * A[0], tPrueba * A[1]];
    var dist = function (u) { return Math.hypot(B[0] - u[0], B[1] - u[1]); };

    // --- panel izquierdo ---
    svg.appendChild(el("rect", { x: PX0, y: PY1, width: PX1 - PX0, height: PY0 - PY1, fill: "none", stroke: "var(--r-linea,#ddd)" }));
    svg.appendChild(el("line", { x1: px(X0), y1: py(0), x2: px(X1), y2: py(0), stroke: "var(--r-linea,#eee)" }));
    svg.appendChild(el("line", { x1: px(0), y1: py(Y0), x2: px(0), y2: py(Y1), stroke: "var(--r-linea,#eee)" }));
    // la recta generada por a, extendida: es el conjunto donde puede caer la sombra
    var k0 = -1.2, k1 = 1.6;
    svg.appendChild(el("line", { x1: px(k0 * A[0]), y1: py(k0 * A[1]), x2: px(k1 * A[0]), y2: py(k1 * A[1]),
      stroke: C_A, "stroke-width": 1, "stroke-dasharray": "5 4", "stroke-opacity": 0.6 }));
    // el punto de prueba y su distancia a b
    svg.appendChild(el("line", { x1: px(Q[0]).toFixed(1), y1: py(Q[1]).toFixed(1), x2: px(B[0]).toFixed(1), y2: py(B[1]).toFixed(1),
      stroke: C_TEO, "stroke-width": 1, "stroke-dasharray": "3 3", "stroke-opacity": 0.55 }));
    svg.appendChild(el("circle", { cx: px(Q[0]).toFixed(1), cy: py(Q[1]).toFixed(1), r: 4, fill: "none", stroke: C_TEO, "stroke-width": 1.4 }));
    // la sombra y el residuo
    svg.appendChild(el("line", { x1: px(0), y1: py(0), x2: px(P[0]).toFixed(1), y2: py(P[1]).toFixed(1),
      stroke: C_P, "stroke-width": 7, "stroke-opacity": 0.25 }));
    svg.appendChild(el("line", { x1: px(P[0]).toFixed(1), y1: py(P[1]).toFixed(1), x2: px(B[0]).toFixed(1), y2: py(B[1]).toFixed(1),
      stroke: C_E, "stroke-width": 1.6, "stroke-dasharray": "4 3" }));
    // marca de angulo recto en el pie de la sombra
    var un = norma(A), ux = A[0] / un, uy = A[1] / un;
    var en = norma(E) > 1e-9 ? norma(E) : 1, ex = E[0] / en, ey = E[1] / en, s = 0.16;
    svg.appendChild(el("path", { d: "M" + px(P[0] + s * ux) + "," + py(P[1] + s * uy) +
      " L" + px(P[0] + s * ux + s * ex) + "," + py(P[1] + s * uy + s * ey) +
      " L" + px(P[0] + s * ex) + "," + py(P[1] + s * ey),
      fill: "none", stroke: C_E, "stroke-width": 1, "stroke-opacity": 0.8 }));
    flecha(A, C_A, 2.4, "a");
    flecha(B, C_B, 2.4, "b");
    svg.appendChild(el("text", { x: px(P[0]) - 4, y: py(P[1]) + 18, "font-size": 11, "font-weight": 600, fill: C_P }, "p"));
    svg.appendChild(el("text", { x: PX0 + 6, y: 22, "font-size": 11, "font-weight": 600, fill: "var(--r-tinta,#222)" }, "la sombra de b sobre la recta de a"));

    // --- panel derecho: la distancia en funcion de t ---
    svg.appendChild(el("rect", { x: QX0, y: QY1, width: QX1 - QX0, height: QY0 - QY1, fill: "none", stroke: "var(--r-linea,#ddd)" }));
    var d = "";
    for (i = 0; i <= 120; i++) {
      var tt = -0.4 + 1.8 * i / 120;
      d += (i ? " L" : "M") + qx(tt).toFixed(1) + "," + qy(dist([tt * A[0], tt * A[1]])).toFixed(1);
    }
    svg.appendChild(el("path", { d: d, fill: "none", stroke: C_TEO, "stroke-width": 2 }));
    svg.appendChild(el("line", { x1: qx(t).toFixed(1), y1: QY0, x2: qx(t).toFixed(1), y2: qy(dist(P)).toFixed(1),
      stroke: C_P, "stroke-width": 1.4, "stroke-dasharray": "3 3" }));
    svg.appendChild(el("circle", { cx: qx(t).toFixed(1), cy: qy(dist(P)).toFixed(1), r: 4.5, fill: C_P }));
    svg.appendChild(el("circle", { cx: qx(tPrueba).toFixed(1), cy: qy(dist(Q)).toFixed(1), r: 4, fill: "none", stroke: C_TEO, "stroke-width": 1.4 }));
    for (i = 0; i <= 1.0001; i += 0.5) {
      svg.appendChild(el("text", { x: qx(i), y: QY0 + 15, "font-size": 9, "text-anchor": "middle", fill: "var(--r-tinta-suave,#777)" }, i.toFixed(1)));
    }
    svg.appendChild(el("text", { x: QX0 + 6, y: 22, "font-size": 11, "font-weight": 600, fill: "var(--r-tinta,#222)" }, "distancia de b al punto t·a"));
    svg.appendChild(el("text", { x: (QX0 + QX1) / 2, y: QY0 + 30, "font-size": 9.5, "text-anchor": "middle", fill: "var(--r-tinta-suave,#777)" }, "coeficiente t"));

    lec.innerHTML = "<span class=\"fx\">t̂ = ⟨a,b⟩/⟨a,a⟩ = " + t.toFixed(3) +
      "   ·   ‖b − p‖ = " + dist(P).toFixed(3) +
      "   ·   punto de prueba t = " + tPrueba.toFixed(2) + " → ‖b − t·a‖ = " + dist(Q).toFixed(3) +
      "<br>⟨a, b−p⟩ = " + punto(A, E).toExponential(1) + "   ·   ‖b‖² = " + punto(B, B).toFixed(3) +
      "   ·   ‖p‖² + ‖b−p‖² = " + (punto(P, P) + punto(E, E)).toFixed(3) + "</span><br>" +
      "El círculo hueco es un punto cualquiera de la recta y el relleno es la sombra. La parábola de la derecha nunca baja " +
      "por debajo del punto relleno: mover t a un lado o a otro solo puede alejarse. Y el residuo sale perpendicular a la " +
      "recta para cualquier posición de b, que es de donde sale la fórmula.";
  }

  ctlAng.addEventListener("input", dibujar);
  ctlT.addEventListener("input", dibujar);
  dibujar();
})();
</script>
```

La parábola de la derecha es la Proposición 5.3 dibujada: sea cual sea la dirección de $b$, su mínimo cae exactamente donde está la sombra, y el residuo sale perpendicular a la recta —la marca de ángulo recto del pie—. Los dos extremos también se leen ahí: con $b$ sobre la recta el residuo se anula, y con $b$ perpendicular la sombra colapsa al origen.

```{pyodide}
import numpy as np

a = np.array([2.6, 0.7])
b = np.array([1.1, 2.1])

t = (a @ b) / (a @ a)
p = t * a
e = b - p

print(f"t = {t:.6f}")
print(f"p = {p.round(6)}")
print(f"e = {e.round(6)}")
print()
print(f"<a, e> = {e @ a:.2e}    <- cero: el residuo es perpendicular")
print(f"||b||^2       = {b @ b:.6f}")
print(f"||p||^2 + ||e||^2 = {p @ p + e @ e:.6f}    <- Pitagoras, exacto")
print()
# la Proposicion 5.3, comprobada en una rejilla de puntos de la recta
ts = np.linspace(-1, 2, 301)
distancias = np.array([np.linalg.norm(b - tt * a) for tt in ts])
print("el minimo de la rejilla cae en t =", round(float(ts[distancias.argmin()]), 2))
print("y la formula da t =", round(float(t), 2))
print("ninguna distancia de la rejilla es menor que ||e||:",
      bool(np.all(distancias >= np.linalg.norm(e) - 1e-12)))
```

## 2. La matriz de proyección

La segunda forma de la Proposición 5.2 dice que proyectar es multiplicar por una matriz fija. Esa matriz tiene dos propiedades que la caracterizan.

::: {.definicion}
**Definición 5.4 (matriz de proyección sobre una recta).** $P = \dfrac{a\,a^\top}{a^\top a}$, de modo que $p = Pb$ para todo $b$.
:::

::: {.proposicion}
**Proposición 5.5 (propiedades de $P$).** La matriz de la Definición 5.4 cumple:

1. $P^\top = P$ (simétrica);
2. $P^2 = P$ (idempotente);
3. sus eigenvalores son $1$ —con multiplicidad 1, en la dirección de $a$— y $0$ —con multiplicidad $n-1$—;
4. $I-P$ es la matriz de proyección sobre el complemento ortogonal, y $P(I-P)=0$.
:::

::: {.demostracion}
*Demostración.* (1) $(a\,a^\top)^\top = a\,a^\top$ porque trasponer un producto invierte el orden, y el denominador es un escalar. (2) Multiplicando y usando asociatividad,
$$P^2 = \frac{a\,a^\top}{a^\top a}\cdot\frac{a\,a^\top}{a^\top a} = \frac{a\,(a^\top a)\,a^\top}{(a^\top a)^2} = \frac{a\,a^\top}{a^\top a} = P,$$
porque el factor central $a^\top a$ es un escalar que se cancela con uno del denominador. (3) $Pa = a(a^\top a)/(a^\top a) = a$, así que $a$ es eigenvector de eigenvalor 1; y si $v\perp a$, entonces $Pv = a(a^\top v)/(a^\top a) = 0$, así que todo el complemento ortogonal —de dimensión $n-1$— es eigenespacio de eigenvalor 0. Eso agota $\mathbb{R}^n$. (4) $(I-P)^\top = I-P$ y $(I-P)^2 = I - 2P + P^2 = I-2P+P = I-P$ usando (2); y $P(I-P) = P - P^2 = 0$. [∎]{.fin-demo}
:::

Que $P^2=P$ no es un tecnicismo: dice que proyectar dos veces es lo mismo que proyectar una, lo cual es obvio en cuanto se piensa en la sombra —una vez que ya está sobre la recta, volver a proyectarla no la mueve—. Y el apartado (4) dice que el residuo también es una proyección: la de $b$ sobre todo lo que la recta no cubre. Las dos piezas de Pitágoras son, entonces, dos proyecciones complementarias.

```{pyodide}
import numpy as np

a = np.array([2.6, 0.7])
P = np.outer(a, a) / (a @ a)          # np.outer construye a aᵀ

print("P =\n", P.round(6))
print()
print("P^T = P ?", bool(np.allclose(P.T, P)))
print("P^2 = P ?", bool(np.allclose(P @ P, P)))
print("eigenvalores:", np.linalg.eigvalsh(P).round(10))
print("traza(P) =", round(float(np.trace(P)), 10), " = rango(P) =", int(np.linalg.matrix_rank(P)))
print()
I = np.eye(2)
print("(I-P)^2 = I-P ?", bool(np.allclose((I - P) @ (I - P), I - P)))
print("P(I-P) = 0 ?", bool(np.allclose(P @ (I - P), np.zeros((2, 2)))))
```

## 3. De una recta a un subespacio

El argumento no usó en ningún momento que el subespacio fuera de dimensión 1. Con $k$ direcciones, la condición de perpendicularidad hay que imponerla contra todas a la vez.

::: {.definicion}
**Definición 5.6 (proyección sobre un subespacio).** Sea $A$ una matriz $n\times k$ de columnas linealmente independientes. La proyección de $b$ sobre el espacio columna de $A$ es el único $p = A\hat{x}$ tal que $b-p$ es ortogonal a **todas** las columnas de $A$, es decir $A^\top(b-p)=0$.
:::

::: {.proposicion}
**Proposición 5.7 (ecuaciones normales).** Con $A$ de columnas independientes, $A^\top A$ es invertible y
$$A^\top A\,\hat{x} = A^\top b, \qquad \hat{x} = (A^\top A)^{-1}A^\top b, \qquad P = A(A^\top A)^{-1}A^\top.$$
:::

::: {.demostracion}
*Demostración.* La condición $A^\top(b-A\hat{x})=0$ se reordena en $A^\top A\hat{x} = A^\top b$ por linealidad. Falta ver que $A^\top A$ es invertible. Supóngase $A^\top A v = 0$ para algún $v$; entonces
$$0 = v^\top A^\top A v = (Av)^\top(Av) = \|Av\|^2,$$
y por ser el producto interno definido positivo, $Av=0$. Como las columnas de $A$ son independientes, eso obliga a $v=0$: el núcleo de $A^\top A$ es trivial y la matriz, cuadrada $k\times k$, es invertible. Sustituyendo $\hat{x}$ en $p=A\hat{x}$ sale la expresión de $P$. Las cuatro propiedades de la Proposición 5.5 se comprueban igual: $P^\top = P$ por construcción, y
$$P^2 = A(A^\top A)^{-1}\underbrace{A^\top A(A^\top A)^{-1}}_{I}A^\top = A(A^\top A)^{-1}A^\top = P.$$
[∎]{.fin-demo}
:::

::: {.proposicion}
**Proposición 5.8 (mejor aproximación en el subespacio).** Para todo $x\in\mathbb{R}^k$,
$$\|b - Ax\|^2 = \|b-p\|^2 + \|p - Ax\|^2 \;\ge\; \|b-p\|^2,$$
con igualdad solo si $Ax=p$. Además $\operatorname{traza}(P) = k$, el número de columnas.
:::

::: {.demostracion}
*Demostración.* La descomposición es la misma de la Proposición 5.3: $b-Ax = (b-p) + (p-Ax)$, donde $p-Ax$ está en el espacio columna y $b-p$ es ortogonal a él por la Definición 5.6; Pitágoras (Proposición 4.4) separa los cuadrados. Para la traza, por la propiedad cíclica,
$$\operatorname{traza}(P) = \operatorname{traza}\big(A(A^\top A)^{-1}A^\top\big) = \operatorname{traza}\big((A^\top A)^{-1}A^\top A\big) = \operatorname{traza}(I_k) = k.$$
[∎]{.fin-demo}
:::

Ese último número reaparecerá con otro nombre: la traza de la matriz de proyección es el número de direcciones que el modelo puede ajustar, y es lo que en Estadística 14 se resta al contar los grados de libertad de $\hat{\sigma}^2$.

```{=html}
<div class="viz-plano">
  <div class="controles">
    <label>Altura de b sobre el plano <input id="pl-h" type="range" min="0" max="180" step="10" value="110"> <span id="pl-h-v">1,10</span></label>
    <label>Punto de prueba: <input id="pl-s" type="range" min="-120" max="120" step="10" value="90"> <span id="pl-s-v">0,90</span>·a₁</label>
    <label>+ <input id="pl-t" type="range" min="-120" max="120" step="10" value="-40"> <span id="pl-t-v">−0,40</span>·a₂</label>
  </div>
  <svg id="pl-svg" viewBox="0 0 620 340" role="img" aria-label="Dibujo en perspectiva de un plano generado por dos vectores dentro del espacio de tres dimensiones. El vector b esta fuera del plano; su sombra cae dentro y el residuo los une perpendicularmente. Un punto de prueba se mueve por el plano y su distancia a b nunca es menor que la del residuo."></svg>
  <p class="lectura" id="pl-lectura"></p>
</div>
<style>
.viz-plano { border: 1px solid var(--r-linea, #ddd); border-radius: 10px; padding: 1rem 1.1rem; margin: 1.7rem 0; }
.viz-plano .controles { display: flex; flex-wrap: wrap; gap: .45rem 1rem; align-items: center; margin-bottom: .85rem; font-size: .85rem; }
.viz-plano label { display: flex; align-items: center; gap: .4rem; }
.viz-plano input[type=range] { width: 96px; padding: 0; }
.viz-plano svg { width: 100%; height: auto; display: block; }
.viz-plano .lectura { font-size: .85rem; margin-top: .75rem; min-height: 5.4em; line-height: 1.6; }
.viz-plano .lectura .fx { font-family: var(--bs-font-monospace, monospace); font-size: .95em; }
</style>
<script>
(function () {
  var C_PLANO = "#0F6E68", C_B = "#B45309", C_E = "#B91C1C", C_TEO = "var(--r-tinta,#1f2430)";
  var svg = document.getElementById("pl-svg"), lec = document.getElementById("pl-lectura");
  var ctlH = document.getElementById("pl-h"), etqH = document.getElementById("pl-h-v");
  var ctlS = document.getElementById("pl-s"), etqS = document.getElementById("pl-s-v");
  var ctlT = document.getElementById("pl-t"), etqT = document.getElementById("pl-t-v");

  // Dos vectores fijos generan el plano; el dibujo es una proyeccion axonometrica
  // con ejes constantes, asi que nada del marco depende de lo que se mueva.
  var A1 = [1.0, 0.25, 0.35], A2 = [0.2, 1.0, -0.30], BASE = [0.5, -0.3];
  var EX = [0.86, 0.36], EY = [-0.80, 0.42], EZ = [0.0, -0.95];
  var CX = 300, CY = 210, ESC = 86;

  function el(t, at, txt) {
    var e = document.createElementNS("http://www.w3.org/2000/svg", t);
    for (var k in at) e.setAttribute(k, at[k]);
    if (txt !== undefined) e.textContent = txt;
    return e;
  }
  function P2(v) {   // de R^3 a la pantalla
    return [CX + ESC * (v[0] * EX[0] + v[1] * EY[0] + v[2] * EZ[0]),
            CY + ESC * (v[0] * EX[1] + v[1] * EY[1] + v[2] * EZ[1])];
  }
  function comb(s, t) { return [s * A1[0] + t * A2[0], s * A1[1] + t * A2[1], s * A1[2] + t * A2[2]]; }
  function resta(u, v) { return [u[0] - v[0], u[1] - v[1], u[2] - v[2]]; }
  function punto(u, v) { return u[0] * v[0] + u[1] * v[1] + u[2] * v[2]; }
  function norma(u) { return Math.sqrt(punto(u, u)); }
  var N = (function () {          // normal al plano: producto cruz de a1 y a2
    var n = [A1[1] * A2[2] - A1[2] * A2[1], A1[2] * A2[0] - A1[0] * A2[2], A1[0] * A2[1] - A1[1] * A2[0]];
    var m = norma(n);
    return [n[0] / m, n[1] / m, n[2] / m];
  })();

  function segmento(u, v, color, ancho, guion) {
    var a = P2(u), b = P2(v);
    var at = { x1: a[0].toFixed(1), y1: a[1].toFixed(1), x2: b[0].toFixed(1), y2: b[1].toFixed(1),
               stroke: color, "stroke-width": ancho };
    if (guion) at["stroke-dasharray"] = guion;
    svg.appendChild(el("line", at));
  }

  function dibujar() {
    while (svg.firstChild) svg.removeChild(svg.firstChild);
    var h = +ctlH.value / 100, s = +ctlS.value / 100, t = +ctlT.value / 100, i;
    etqH.textContent = h.toFixed(2).replace(".", ",");
    etqS.textContent = s.toFixed(2).replace(".", ",").replace("-", "−");
    etqT.textContent = t.toFixed(2).replace(".", ",").replace("-", "−");

    var p = comb(BASE[0], BASE[1]);                       // la sombra: fija en el plano
    var b = [p[0] + h * N[0], p[1] + h * N[1], p[2] + h * N[2]];
    var q = comb(s, t);                                   // punto de prueba, movil
    var e = resta(b, p);

    // la rejilla del plano
    for (i = -3; i <= 3; i++) {
      segmento(comb(i / 2, -1.6), comb(i / 2, 1.6), C_PLANO, i === 0 ? 1.1 : 0.6, null);
      segmento(comb(-1.6, i / 2), comb(1.6, i / 2), C_PLANO, i === 0 ? 1.1 : 0.6, null);
    }
    var esquinas = [comb(-1.6, -1.6), comb(1.6, -1.6), comb(1.6, 1.6), comb(-1.6, 1.6)].map(P2);
    svg.appendChild(el("polygon", {
      points: esquinas.map(function (c) { return c[0].toFixed(1) + "," + c[1].toFixed(1); }).join(" "),
      fill: C_PLANO, "fill-opacity": 0.07, stroke: C_PLANO, "stroke-opacity": 0.5 }));

    // los dos generadores
    [[A1, "a₁"], [A2, "a₂"]].forEach(function (par) {
      segmento([0, 0, 0], par[0], C_PLANO, 2.4, null);
      var c = P2(par[0]);
      svg.appendChild(el("text", { x: (c[0] + 8).toFixed(1), y: (c[1] + 4).toFixed(1), "font-size": 12,
        "font-weight": 600, fill: C_PLANO }, par[1]));
    });

    // el punto de prueba y su distancia a b
    segmento(q, b, C_TEO, 1, "3 3");
    var cq = P2(q);
    svg.appendChild(el("circle", { cx: cq[0].toFixed(1), cy: cq[1].toFixed(1), r: 4.5, fill: "none", stroke: C_TEO, "stroke-width": 1.4 }));
    svg.appendChild(el("text", { x: (cq[0] + 7).toFixed(1), y: (cq[1] + 14).toFixed(1), "font-size": 11, fill: C_TEO }, "q"));

    // b, su sombra y el residuo
    segmento([0, 0, 0], b, C_B, 2.6, null);
    segmento(p, b, C_E, 1.8, "4 3");
    var cb = P2(b), cp = P2(p);
    svg.appendChild(el("circle", { cx: cb[0].toFixed(1), cy: cb[1].toFixed(1), r: 4.5, fill: C_B }));
    svg.appendChild(el("text", { x: (cb[0] + 8).toFixed(1), y: (cb[1] - 4).toFixed(1), "font-size": 12, "font-weight": 600, fill: C_B }, "b"));
    svg.appendChild(el("circle", { cx: cp[0].toFixed(1), cy: cp[1].toFixed(1), r: 4.5, fill: C_E }));
    svg.appendChild(el("text", { x: (cp[0] + 8).toFixed(1), y: (cp[1] + 14).toFixed(1), "font-size": 12, "font-weight": 600, fill: C_E }, "p"));

    svg.appendChild(el("text", { x: 20, y: 24, "font-size": 11, "font-weight": 600, fill: "var(--r-tinta,#222)" },
      "el plano generado por a₁ y a₂, dentro de ℝ³"));

    var dq = norma(resta(b, q)), de = norma(e);
    lec.innerHTML = "<span class=\"fx\">‖b − p‖ = " + de.toFixed(4) +
      "   ·   ‖b − q‖ = " + dq.toFixed(4) + "   ·   diferencia = " + (dq - de).toFixed(4) +
      "<br>⟨a₁, b−p⟩ = " + punto(A1, e).toExponential(1) + "   ·   ⟨a₂, b−p⟩ = " + punto(A2, e).toExponential(1) +
      "   ·   ‖b−q‖² − ‖b−p‖² = " + (dq * dq - de * de).toFixed(4) +
      "   ·   ‖p−q‖² = " + Math.pow(norma(resta(p, q)), 2).toFixed(4) + "</span><br>" +
      "El residuo es perpendicular a los dos generadores a la vez, y por eso a todo el plano: eso es lo que dice Aᵀ(b−p) = 0. " +
      "Mover q por el plano nunca acorta la distancia, y lo que sobra es exactamente ‖p−q‖², que es Pitágoras.";
  }

  ctlH.addEventListener("input", dibujar);
  ctlS.addEventListener("input", dibujar);
  ctlT.addEventListener("input", dibujar);
  dibujar();
})();
</script>
```

El punto hueco es un $Ax$ cualquiera y el relleno es la proyección. La lectura confirma las dos proposiciones a la vez: los dos productos internos contra $a_1$ y $a_2$ salen en el error de máquina —el residuo es perpendicular al plano entero, no solo a una dirección—, y la diferencia de cuadrados entre la distancia de prueba y la mínima coincide dígito a dígito con $\|p-q\|^2$, que es Pitágoras otra vez.

```{pyodide}
import numpy as np

rng = np.random.default_rng(0)
A = rng.normal(size=(6, 2))          # un plano dentro de R^6
b = rng.normal(size=6)

P = A @ np.linalg.inv(A.T @ A) @ A.T
p = P @ b
e = b - p

# El signo del cero al redondear cambia entre versiones de numpy, asi que se
# imprime la cota y no el arreglo: lo que se afirma es la perpendicularidad.
print("max |A^T e| por debajo de 1e-12 :", bool(np.all(np.abs(A.T @ e) < 1e-12)),
      "   <- perpendicular a TODAS las columnas")
print("P^T = P ?", bool(np.allclose(P.T, P)))
print("P^2 = P ?", bool(np.allclose(P @ P, P)))
print("traza(P) =", round(float(np.trace(P)), 6), " = rango(P) =", int(np.linalg.matrix_rank(P)),
      " = columnas de A =", A.shape[1])
print()
print(f"||b||^2           = {b @ b:.6f}")
print(f"||p||^2 + ||e||^2 = {p @ p + e @ e:.6f}")
print()
# la Proposicion 5.8 contra la fuerza bruta: 20 000 puntos del plano al azar
X = rng.normal(size=(20_000, 2)) * 2
distancias = np.linalg.norm(b[None, :] - X @ A.T, axis=1)
print(f"menor distancia encontrada probando 20 000 puntos: {distancias.min():.6f}")
print(f"distancia de la proyeccion                        : {np.linalg.norm(e):.6f}")
print("ninguno de los 20 000 la mejora:", bool(np.all(distancias >= np.linalg.norm(e) - 1e-12)))
```

## 4. Centrar es proyectar

La operación más común de toda la estadística descriptiva resulta ser un caso particular de esta lección.

::: {.proposicion}
**Proposición 5.9 (la media es una proyección).** Sea $\mathbf{1}=(1,\dots,1)^\top\in\mathbb{R}^n$ y $x\in\mathbb{R}^n$. La proyección de $x$ sobre la recta generada por $\mathbf{1}$ es
$$p = \bar{x}\,\mathbf{1}, \qquad \bar{x}=\frac{1}{n}\sum_i x_i,$$
y su residuo es el vector centrado $\tilde{x}$, con $\tilde{x}\perp\mathbf{1}$ y $\sum_i\tilde{x}_i=0$.
:::

::: {.demostracion}
*Demostración.* Aplíquese la Proposición 5.2 con $a=\mathbf{1}$: $\langle\mathbf{1},x\rangle = \sum_i x_i$ y $\langle\mathbf{1},\mathbf{1}\rangle = n$, de modo que
$$p = \frac{\sum_i x_i}{n}\,\mathbf{1} = \bar{x}\,\mathbf{1}.$$
El residuo es $e = x - \bar{x}\mathbf{1}$, cuya coordenada $i$ es $x_i-\bar{x}$: exactamente el vector centrado. Que sea ortogonal a $\mathbf{1}$ es la Definición 5.1, y escrito en coordenadas, $\langle\mathbf{1},\tilde{x}\rangle = \sum_i(x_i-\bar{x}) = 0$: las desviaciones respecto de la media suman cero. [∎]{.fin-demo}
:::

Tres cosas que estaban sueltas quedan atadas por esta proposición. Que las desviaciones respecto de la media sumen cero deja de ser un truco algebraico y pasa a ser la ortogonalidad del residuo. Que la media sea el número que minimiza $\sum_i(x_i-c)^2$ es la Proposición 5.3 con $a=\mathbf{1}$. Y el centrado que la lección 4 exigía antes de calcular el coseno —y que Estadística 5 exige antes de calcular la correlación— es quitarle a cada variable su sombra sobre $\mathbf{1}$.

```{pyodide}
import numpy as np

x = np.array([37.0, 38, 39, 40, 41, 42])
n = len(x)
unos = np.ones(n)

P = np.outer(unos, unos) / (unos @ unos)     # proyeccion sobre la recta de los unos
p = P @ x
e = x - p

print("proyeccion de x sobre la recta de los unos:", p.round(6))
print("la media de x, repetida n veces           :", np.full(n, x.mean()).round(6))
print("iguales:", bool(np.allclose(p, np.full(n, x.mean()))))
print()
print("residuo (el vector centrado):", e.round(6))
print("suma del residuo            :", round(float(e.sum()), 12), "  <- ortogonal a los unos")
print()
# la media minimiza la suma de cuadrados, que es la Proposicion 5.3 con a = 1
cs = np.linspace(x.mean() - 3, x.mean() + 3, 601)
sc = np.array([((x - c) ** 2).sum() for c in cs])
print(f"c que minimiza la suma de cuadrados en la rejilla: {cs[sc.argmin()]:.4f}")
print(f"media de x                                       : {x.mean():.4f}")
print(f"P^2 = P ? {bool(np.allclose(P @ P, P))}    traza(P) = {round(float(np.trace(P)), 6)}")
```

## Ejercicios

### Ejercicio 1 — Proyectar sobre una recta

Calcular la proyección de `b` sobre la dirección de `a`, con la fórmula de la Proposición 5.2.

```{pyodide}
#| exercise: ex_proy
import numpy as np

a = np.array([3.0, 4.0])
b = np.array([5.0, 0.0])

p = ______

print("p =", p.round(4))
print("residuo perpendicular?", abs((b - p) @ a) < 1e-10)
p.round(4)
```

::: {.hint exercise="ex_proy"}
La fórmula es $p = \dfrac{a^\top b}{a^\top a}\,a$. En NumPy el producto interno es `a @ b`, y el resultado se multiplica por el vector `a` completo.
:::

::: {.solution exercise="ex_proy"}
```{pyodide}
import numpy as np

a = np.array([3.0, 4.0])
b = np.array([5.0, 0.0])

p = ((a @ b) / (a @ a)) * a

print("p =", p.round(4))
print("residuo perpendicular?", abs((b - p) @ a) < 1e-10)
p.round(4)
```
:::

```{pyodide}
#| exercise: ex_proy
#| check: true
import numpy as np
esperado = np.array([1.8, 2.4])
if isinstance(result, np.ndarray) and result.shape == (2,) and np.allclose(result, esperado, atol=1e-3):
    feedback = {"correct": True, "message": "Correcto: t = 15/25 = 0.6, así que p = 0.6·(3,4) = (1.8, 2.4).", "type": "success"}
elif isinstance(result, np.ndarray) and np.allclose(result, np.array([0.6, 0.6]), atol=1e-3):
    feedback = {"correct": False, "message": "Calculaste el escalar t pero no lo multiplicaste por el vector a.", "type": "warning"}
else:
    feedback = {"correct": False, "message": f"Esperaba {esperado} y obtuve {result}.", "type": "error"}
feedback
```

### Ejercicio 2 — La matriz de proyección

Construir $P$ y comprobar que es idempotente, que es el apartado (2) de la Proposición 5.5.

```{pyodide}
#| exercise: ex_matP
import numpy as np

a = np.array([1.0, 1.0, 0.0])

P = ______

print("P =\n", P.round(4))
print("P² = P ?", np.allclose(P @ P, P))
bool(np.allclose(P @ P, P))
```

::: {.hint exercise="ex_matP"}
$P = \dfrac{a\,a^\top}{a^\top a}$. El producto exterior $a\,a^\top$ es `np.outer(a, a)`; el denominador es el escalar `a @ a`.
:::

::: {.solution exercise="ex_matP"}
```{pyodide}
import numpy as np

a = np.array([1.0, 1.0, 0.0])

P = np.outer(a, a) / (a @ a)

print("P =\n", P.round(4))
print("P² = P ?", np.allclose(P @ P, P))
bool(np.allclose(P @ P, P))
```
:::

```{pyodide}
#| exercise: ex_matP
#| check: true
if result is True:
    feedback = {"correct": True, "message": "Correcto. Ojo con la diferencia: np.outer(a,a) da una matriz 3×3; a @ a da el escalar 2.", "type": "success"}
else:
    feedback = {"correct": False, "message": f"Esperaba True. Revisa que uses np.outer en el numerador y el escalar a @ a en el denominador. Obtuve {result}.", "type": "error"}
feedback
```

## Reto

En `proyectos/notebooks/F1-retos.ipynb`, sección **Mat 5**:

1. Añadir `proyectar(b, A)` a `scratch/lineal.py`, que funcione tanto si `A` es un vector como si es una matriz, y devuelva el par $(p, e)$. Probar sobre cien casos de dimensiones variadas que $A^\top e\approx 0$ y que $P^2=P$ a tolerancia $10^{-12}$.
2. **Rango deficiente.** Construir $P = A(A^\top A)^{-1}A^\top$ con una $A$ cuyas columnas **no** sean independientes y ver qué ocurre. Después usar `np.linalg.pinv` en lugar de `inv` y comparar: ¿qué hace la pseudoinversa que la inversa no puede, y en qué queda la Proposición 5.7 cuando su hipótesis falla? Anotarlo en `50-Errores/`.
3. Comprobar que la matriz de centrado $C = I - \frac{1}{n}\mathbf{1}\mathbf{1}^\top$ de la Proposición 5.9 cumple $C^2=C$ y $C\mathbf{1}=0$, y que centrar una matriz de datos por columnas es multiplicarla por $C$ a la izquierda. Después comprobar que la matriz de covarianza muestral es $\frac{1}{n-1}X^\top C X$: la covarianza es un producto interno entre variables ya proyectadas.

## Del libro

*Mathematics for Machine Learning* trata este material en §3.8 *Orthogonal Projections*, con §3.6 *Orthogonal Complement* para el residuo y §3.5 *Orthonormal Basis* para el caso en que las columnas ya vienen ortonormales, donde $A^\top A = I$ y las ecuaciones normales se reducen a $\hat{x}=A^\top b$.

El libro tiene copyright y no es descargable desde el entorno en que se escribió esta página, así que se cita número y título y no se le atribuye ninguna frase.

**Preguntas para leer §3.5, §3.6 y §3.8 con lápiz:**

1. ¿Qué dimensión tiene el complemento ortogonal de un subespacio de dimensión $k$ dentro de $\mathbb{R}^n$, y cómo se relaciona eso con el apartado (4) de la Proposición 5.5?
2. Si las columnas de $A$ son ortonormales, ¿en qué se convierte $(A^\top A)^{-1}$, y por qué eso hace la proyección tan barata de calcular?
3. MML presenta la proyección minimizando una distancia. ¿En qué punto de ese argumento aparece —explícita o implícitamente— el teorema de Pitágoras que aquí es la Proposición 4.4?

## Para el Cerebro

Nota nueva en `10-Conceptos/proyeccion-ortogonal.md`, enlazada a [[producto-interno]], [[espacio-columna]] y [[minimos-cuadrados]]. La Proposición 5.9 conviene rehacerla a mano: es corta y convierte «restar la media» en un caso particular de proyectar, que es de las pocas ideas que ahorran memoria de verdad.

```
¿Qué caracteriza a la proyección ortogonal?::Que el residuo b − p sea perpendicular al subespacio (Definición 5.1)
¿Cuál es la fórmula sobre una recta?::p = (⟨a,b⟩/⟨a,a⟩)·a, y en forma matricial P = a aᵀ/(aᵀa)
¿Por qué la proyección es el punto más cercano?::Por Pitágoras: ‖b − ta‖² = ‖b − p‖² + ‖p − ta‖² ≥ ‖b − p‖²
¿Qué dos propiedades tiene la matriz de proyección?::Simétrica (Pᵀ = P) e idempotente (P² = P)
¿Qué significa P² = P?::Que proyectar dos veces es lo mismo que proyectar una: la sombra ya está en el subespacio
¿Cuáles son los eigenvalores de P?::Solo unos y ceros: 1 en las direcciones que sobreviven, 0 en las que aniquila
¿Qué es I − P?::La proyección sobre el complemento ortogonal; P(I−P) = 0
¿Cuáles son las ecuaciones normales?::AᵀA x̂ = Aᵀb, que salen de exigir Aᵀ(b − Ax̂) = 0
¿Por qué AᵀA es invertible?::Porque AᵀAv = 0 implica ‖Av‖² = 0, y con columnas independientes eso obliga a v = 0
¿Cuánto vale la traza de P?::El número de columnas de A: los grados de libertad que consume el modelo
¿Qué proyección es restar la media?::La proyección sobre la recta del vector de unos; el residuo es el vector centrado (Proposición 5.9)
¿Por qué las desviaciones respecto de la media suman cero?::Porque el residuo de esa proyección es ortogonal al vector de unos
```

::: {#fuentes .fuentes}
### Fuentes

**Lo que esta página demuestra sola.** Todo: las Proposiciones 5.2, 5.3, 5.5, 5.7, 5.8 y 5.9 se demuestran aquí a partir de la Definición 5.1 y de las propiedades del producto interno de la lección 4, con la Proposición 4.4 —Pitágoras— como única herramienta para los dos resultados de mejor aproximación. Se comprueban además numéricamente: la perpendicularidad del residuo y Pitágoras sobre una recta; $P^\top=P$, $P^2=P$, los eigenvalores $\{0,1\}$ y $P(I-P)=0$; la generalización a un plano dentro de $\mathbb{R}^6$, con la traza igual al número de columnas y con 20 000 puntos del plano que no consiguen acercarse más que la proyección; y la Proposición 5.9, con la media saliendo de la fórmula de proyección y la rejilla confirmando que minimiza la suma de cuadrados. Los dos visuales se contrastaron antes de publicarse: 180 direcciones × 37 puntos de prueba para el primero, y 19 alturas × 25 × 25 puntos del plano para el segundo.

**Lo que se usa de otras lecciones sin repetir.** El producto interno, sus tres propiedades y el teorema de Pitágoras son la lección 4. El espacio columna y la independencia lineal, la lección 3.

**Lo que se enuncia sin demostrar.** Nada.

**Lo que viene de los libros.**

- [*Mathematics for Machine Learning*](https://mml-book.github.io/book/mml-book.pdf) (Deisenroth, Faisal & Ong, Cambridge University Press, 2020), §3.5 *Orthonormal Basis*, §3.6 *Orthogonal Complement* y §3.8 *Orthogonal Projections* — citadas por número y título. El libro tiene copyright y no es descargable desde este entorno, así que **no se transcribe ninguna frase**.

**Lo que es mío, no del libro.** El orden —definir la proyección por la propiedad que la caracteriza y deducir la fórmula, en vez de dar la fórmula y comprobar la propiedad—, la insistencia en que el mínimo sale de Pitágoras y no de derivar, y la Proposición 5.9, que presenta el centrado de la estadística descriptiva como el caso más simple de proyección.

[Índices verificados el 12-09-2026 contra el índice publicado del PDF oficial.]{.sello}
:::

→ Siguiente: [Mínimos cuadrados](06-minimos-cuadrados.qmd)
