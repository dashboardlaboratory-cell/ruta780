---
title: "Multiplicadores de Lagrange y condiciones KKT"
subtitle: "Álgebra · Lección 18"
nivel: L2
modulo: matematica
fase: F1
prereqs: [matematica/15-convexidad, matematica/13-derivada-gradiente]
siguientes: []
libro: "MML §7.2 Constrained Optimization and Lagrange Multipliers"
engine: markdown
pyodide:
  packages:
    - numpy
---

::: {.meta-leccion data-leccion="matematica/18-lagrange-kkt"}
[[L2]{.nivel .nivel-L2} **Derivación**]{.item}
[**Fase** F1]{.item}
[**Libro** MML §7.2]{.item}
[**Prereqs** [Convexidad](15-convexidad.qmd) · [Derivada y gradiente](13-derivada-gradiente.qmd)]{.item}
:::

## Objetivo

Al terminar esta lección se puede:

1. Demostrar la condición de Lagrange: en un óptimo sujeto a $g=0$ los gradientes de $f$ y de $g$ son paralelos, y el factor de proporcionalidad es el [multiplicador]{.g data-t="multiplicador de Lagrange"}.
2. Escribir el [lagrangiano]{.g} y reconocer que sus puntos críticos son el sistema que hay que resolver.
3. Demostrar que minimizar una forma cuadrática sobre la esfera unitaria es el problema de eigenvalores de la lección 12.
4. Enunciar las [condiciones KKT]{.g} con su [holgura complementaria]{.g}, y demostrar que en un problema convexo son **suficientes** para el óptimo global.

:::: {.hilo}
::: {}
#### De dónde viene

- **Álgebra 13**: la regla de la cadena y la fórmula $D_vf=\nabla f\cdot v$, que es lo que obliga al gradiente a ser ortogonal a las direcciones admisibles.
- **Álgebra 15**: la caracterización de primer orden de la convexidad (Proposición 15.3). La suficiencia de las KKT se demuestra aplicándola dos veces.
- **Álgebra 5**: el complemento ortogonal. Que $\nabla f$ sea ortogonal a un hiperplano lo obliga a estar en la recta que lo genera.
- **Álgebra 12**: el criterio espectral y el cociente de Rayleigh, que aquí reaparecen como un caso particular de Lagrange.
:::

::: {}
#### Para qué sirve después

- **Estadística 12 y 16**: los máximos de verosimilitud con restricciones (probabilidades que suman uno, varianzas no negativas) se plantean así.
- **Machine Learning**: las máquinas de vector soporte son un problema convexo con desigualdades, y son las KKT las que hacen aparecer los vectores soporte como las restricciones activas.
- **Inferencia causal**: la reponderación con pesos que suman uno y son no negativos es un problema KKT.
:::
::::

## Notación

::: {.notacion}
| Símbolo | Se lee | Significado |
|---|---|---|
| $g(x)=0$ | ge de equis igual a cero | Una restricción de igualdad |
| $g_i(x)\le0$ | — | Una restricción de desigualdad |
| $\lambda$ | lambda | El multiplicador asociado a una restricción |
| $\mathcal{L}(x,\lambda)$ | ele de equis y lambda | El lagrangiano, $f-\lambda g$ |
| $x^\star$ | equis estrella | El óptimo del problema con restricciones |
| restricción activa | — | Una desigualdad que en el óptimo se cumple con igualdad |
:::

## 1. Igualdades: los gradientes se alinean

::: {.definicion}
**Definición 18.1 (problema con una restricción de igualdad).** Dadas $f,g$ diferenciables, el problema es
$$\min_x f(x) \quad\text{sujeto a}\quad g(x)=0.$$
Un punto es **factible** si cumple la restricción.
:::

::: {.proposicion}
**Proposición 18.2 (condición de Lagrange).** Sea $x^\star$ un mínimo local de $f$ sobre $\{g=0\}$, con $f$ y $g$ diferenciables y $\nabla g(x^\star)\ne0$. Entonces existe $\lambda\in\mathbb{R}$ tal que
$$\nabla f(x^\star) = \lambda\,\nabla g(x^\star).$$
:::

::: {.demostracion}
*Demostración.* Sea $v$ cualquier vector con $\nabla g(x^\star)\cdot v=0$. Como $\nabla g(x^\star)\ne0$, el teorema de la función implícita, que se da por conocido, garantiza que existe una curva diferenciable $\gamma:(-\varepsilon,\varepsilon)\to\{g=0\}$ con $\gamma(0)=x^\star$ y $\gamma'(0)=v$: las direcciones ortogonales al gradiente de la restricción son exactamente las que se pueden recorrer sin salirse de ella.

La función de una variable $t\mapsto f(\gamma(t))$ tiene un mínimo local en $t=0$, porque todos los $\gamma(t)$ son factibles y $x^\star$ es mínimo entre los factibles. Por la Proposición 14.7 aplicada en dimensión uno, su derivada se anula ahí; y por la regla de la cadena (Proposición 13.3) junto con la Proposición 13.8,
$$0 = \frac{d}{dt}f(\gamma(t))\Big|_{t=0} = \nabla f(x^\star)\cdot\gamma'(0) = \nabla f(x^\star)\cdot v.$$
Esto vale para **todo** $v$ ortogonal a $\nabla g(x^\star)$. Es decir, $\nabla f(x^\star)$ es ortogonal a todo el hiperplano $\{v:\nabla g(x^\star)\cdot v=0\}$, y por tanto pertenece a su complemento ortogonal (lección 5), que es la recta generada por $\nabla g(x^\star)$. Luego $\nabla f(x^\star)=\lambda\nabla g(x^\star)$ para algún escalar $\lambda$. [∎]{.fin-demo}
:::

La lectura geométrica es directa: si los gradientes no fueran paralelos, $\nabla f$ tendría una componente a lo largo de la restricción, y moverse en su contra bajaría $f$ sin salirse de ella. Solo cuando no queda ninguna componente tangencial el punto puede ser óptimo.

::: {.definicion}
**Definición 18.3 (lagrangiano).** El **lagrangiano** del problema de la Definición 18.1 es
$$\mathcal{L}(x,\lambda) = f(x) - \lambda\,g(x).$$
Anular sus derivadas parciales reproduce a la vez la condición de la Proposición 18.2 y la restricción:
$$\nabla_x\mathcal{L}=\nabla f-\lambda\nabla g=0, \qquad \frac{\partial\mathcal{L}}{\partial\lambda}=-g(x)=0.$$
:::

El lagrangiano convierte un problema con restricciones en $n$ variables en un problema **sin** restricciones en $n+1$: buscar un punto crítico de $\mathcal{L}$. Y un sistema de $n+1$ ecuaciones no lineales es exactamente lo que el método de Newton de la lección 17 sabe resolver.

```{pyodide}
import numpy as np

# Minimizar f(x,y) = x^2 + 2y^2 sujeto a g(x,y) = x + y - 1 = 0.
# Lagrange: (2x, 4y) = lambda (1,1)  ->  x = l/2, y = l/4, y x+y=1 da l = 4/3.
def f(p):  return p[0]**2 + 2*p[1]**2
def gf(p): return np.array([2*p[0], 4*p[1]])
def g(p):  return p[0] + p[1] - 1.0
def gg(p): return np.array([1.0, 1.0])

x_est = np.array([2/3, 1/3]); lam_est = 4/3
print(f"solucion analitica: x* = ({x_est[0]:.6f}, {x_est[1]:.6f})   "
      f"lambda = {lam_est:.6f}   f = {f(x_est):.6f}")
print(f"cumple la restriccion (|g| < 1e-12): {bool(abs(g(x_est)) < 1e-12)}")
print()

print(f"{'t':>7s}  {'punto sobre la recta':>24s}  {'f':>9s}  {'angulo grad f, grad g':>22s}")
for t in [-0.4, -0.2, 0.0, 0.2, 0.4]:
    p = x_est + t*np.array([1.0, -1.0])          # moverse sobre x+y=1
    u, v = gf(p), gg(p)
    cos = (u @ v) / (np.linalg.norm(u)*np.linalg.norm(v))
    ang = np.degrees(np.arccos(np.clip(cos, -1, 1)))
    print(f"{t:7.2f}  ({p[0]:10.6f},{p[1]:10.6f})  {f(p):9.6f}  {ang:22.4f}")
print()
print("el angulo se anula justo donde f es minima: ahi grad f es multiplo de grad g")
```

El ángulo entre los dos gradientes se anula exactamente en el punto donde $f$ alcanza su mínimo sobre la recta, y crece al alejarse en cualquiera de los dos sentidos. Eso es la Proposición 18.2 medida sobre cinco puntos de la restricción.

```{=html}
<div class="viz-lagrange">
  <div class="controles">
    <label>Posición sobre la restricción <input id="lg-t" type="range" min="-60" max="60" step="2" value="34"> <span id="lg-t-v">0,34</span></label>
    <label>Restricción
      <select id="lg-g">
        <option value="0" selected>recta: x + y = 1</option>
        <option value="1">circunferencia: x² + y² = 1</option>
      </select>
    </label>
  </div>
  <svg id="lg-svg" viewBox="0 0 620 320" role="img" aria-label="Curvas de nivel de una funcion con la restriccion dibujada encima y, en el punto elegido, las flechas del gradiente de la funcion y del gradiente de la restriccion. A la derecha el valor de la funcion y el angulo entre los gradientes segun la posicion sobre la restriccion."></svg>
  <p class="lectura" id="lg-lectura"></p>
</div>
<style>
.viz-lagrange { border: 1px solid var(--r-linea, #ddd); border-radius: 10px; padding: 1rem 1.1rem; margin: 1.7rem 0; }
.viz-lagrange .controles { display: flex; flex-wrap: wrap; gap: .45rem 1.1rem; align-items: center; margin-bottom: .85rem; font-size: .85rem; }
.viz-lagrange label { display: flex; align-items: center; gap: .4rem; }
.viz-lagrange input[type=range] { width: 130px; padding: 0; }
.viz-lagrange select { font-size: .85rem; padding: .1rem .25rem; }
.viz-lagrange svg { width: 100%; height: auto; display: block; }
.viz-lagrange .lectura { font-size: .85rem; margin-top: .75rem; min-height: 5.2em; line-height: 1.6; }
.viz-lagrange .lectura .fx { font-family: var(--bs-font-monospace, monospace); font-size: .95em; }
</style>
<script>
(function () {
  var C_NIVEL = "var(--r-tinta,#1f2430)", C_REST = "#B45309", C_GF = "#0F6E68";
  var svg = document.getElementById("lg-svg"), lec = document.getElementById("lg-lectura");
  var ct = document.getElementById("lg-t"), et = document.getElementById("lg-t-v");
  var cg = document.getElementById("lg-g");

  // Marco constante en los dos paneles.
  var LCX = 158, LCY = 172, LESC = 74, LR = 1.9;
  var RX0 = 352, RX1 = 602, RY0 = 292, RY1 = 28, TMIN = -0.6, TMAX = 0.6;
  var VMAX = 4.2;

  function el(t, at, txt) {
    var e = document.createElementNS("http://www.w3.org/2000/svg", t);
    for (var k in at) e.setAttribute(k, at[k]);
    if (txt !== undefined) e.textContent = txt;
    return e;
  }
  function lx(x) { return LCX + x * LESC; }
  function ly(y) { return LCY - y * LESC; }
  function qx(t) { return RX0 + (t - TMIN) / (TMAX - TMIN) * (RX1 - RX0); }
  function qy(v) { return RY0 - Math.max(0, Math.min(VMAX, v)) / VMAX * (RY0 - RY1); }

  function f(x, y) { return x*x + 2*y*y; }
  function gradf(x, y) { return [2*x, 4*y]; }

  // punto sobre la restriccion y gradiente de la restriccion, para cada opcion
  function sobre(t, modo) {
    if (modo === 0) return [2/3 + t, 1/3 - t];              // recta x+y=1
    var a = Math.PI/4 + t*2.2;                              // circunferencia
    return [Math.cos(a), Math.sin(a)];
  }
  function gradg(p, modo) {
    return modo === 0 ? [1, 1] : [2*p[0], 2*p[1]];
  }

  function dibujar() {
    while (svg.firstChild) svg.removeChild(svg.firstChild);
    var t = (+ct.value) / 100, modo = +cg.value;
    et.textContent = t.toFixed(2).replace(".", ",").replace("-", "−");

    svg.appendChild(el("rect", { x: lx(-LR), y: ly(LR), width: 2*LR*LESC, height: 2*LR*LESC, fill: "none", stroke: "var(--r-linea,#ddd)" }));

    // curvas de nivel de f: elipses x^2 + 2y^2 = c
    [0.25, 0.667, 1.3, 2.4].forEach(function (c) {
      svg.appendChild(el("ellipse", { cx: lx(0).toFixed(1), cy: ly(0).toFixed(1),
        rx: (Math.sqrt(c)*LESC).toFixed(1), ry: (Math.sqrt(c/2)*LESC).toFixed(1),
        fill: "none", stroke: C_NIVEL, "stroke-width": 1, "stroke-opacity": 0.3 }));
    });

    // la restriccion
    var d = "";
    for (var i = 0; i <= 200; i++) {
      var tt = TMIN + (TMAX - TMIN)*i/200, q = sobre(tt, modo);
      d += (i ? " L" : "M") + lx(q[0]).toFixed(1) + "," + ly(q[1]).toFixed(1);
    }
    svg.appendChild(el("path", { d: d, fill: "none", stroke: C_REST, "stroke-width": 2 }));

    var p = sobre(t, modo), u = gradf(p[0], p[1]), v = gradg(p, modo);
    var nu = Math.hypot(u[0], u[1]), nv = Math.hypot(v[0], v[1]);
    var LEN = 0.62;
    [[u, nu, C_GF], [v, nv, C_REST]].forEach(function (par) {
      var w = par[0], n = par[1];
      svg.appendChild(el("line", { x1: lx(p[0]).toFixed(1), y1: ly(p[1]).toFixed(1),
        x2: lx(p[0] + LEN*w[0]/n).toFixed(1), y2: ly(p[1] + LEN*w[1]/n).toFixed(1),
        stroke: par[2], "stroke-width": 2.2 }));
      svg.appendChild(el("circle", { cx: lx(p[0] + LEN*w[0]/n).toFixed(1), cy: ly(p[1] + LEN*w[1]/n).toFixed(1), r: 3, fill: par[2] }));
    });
    svg.appendChild(el("circle", { cx: lx(p[0]).toFixed(1), cy: ly(p[1]).toFixed(1), r: 4, fill: "var(--r-tinta,#222)" }));
    svg.appendChild(el("text", { x: lx(-LR), y: 18, "font-size": 11, "font-weight": 600, fill: "var(--r-tinta,#222)" }, "f(x,y) = x² + 2y²  con la restricción encima"));
    svg.appendChild(el("text", { x: lx(-LR) + 3, y: 34, "font-size": 10, fill: C_GF }, "— ∇f"));
    svg.appendChild(el("text", { x: lx(-LR) + 46, y: 34, "font-size": 10, fill: C_REST }, "— ∇g y la restricción"));

    // panel derecho: f y el angulo a lo largo de la restriccion
    svg.appendChild(el("rect", { x: RX0, y: RY1, width: RX1 - RX0, height: RY0 - RY1, fill: "none", stroke: "var(--r-linea,#ddd)" }));
    var df = "", da = "", mejor = null, mejorV = Infinity;
    for (i = 0; i <= 200; i++) {
      var tt2 = TMIN + (TMAX - TMIN)*i/200, q2 = sobre(tt2, modo);
      var val = f(q2[0], q2[1]);
      var a2 = gradf(q2[0], q2[1]), b2 = gradg(q2, modo);
      var cos = (a2[0]*b2[0] + a2[1]*b2[1]) / (Math.hypot(a2[0], a2[1])*Math.hypot(b2[0], b2[1]));
      var ang = Math.acos(Math.max(-1, Math.min(1, cos)))*180/Math.PI;
      if (val < mejorV) { mejorV = val; mejor = tt2; }
      df += (i ? " L" : "M") + qx(tt2).toFixed(1) + "," + qy(val).toFixed(1);
      da += (i ? " L" : "M") + qx(tt2).toFixed(1) + "," + qy(ang/90*VMAX).toFixed(1);
    }
    svg.appendChild(el("path", { d: df, fill: "none", stroke: C_NIVEL, "stroke-width": 1.9 }));
    svg.appendChild(el("path", { d: da, fill: "none", stroke: C_REST, "stroke-width": 1.5, "stroke-dasharray": "5 4" }));
    svg.appendChild(el("line", { x1: qx(t).toFixed(1), y1: RY0, x2: qx(t).toFixed(1), y2: RY1, stroke: C_GF, "stroke-width": 1, "stroke-dasharray": "3 3" }));
    svg.appendChild(el("text", { x: RX0, y: 18, "font-size": 11, "font-weight": 600, fill: "var(--r-tinta,#222)" }, "f (continua) y el ángulo (punteada)"));

    var cosA = (u[0]*v[0] + u[1]*v[1]) / (nu*nv);
    var angA = Math.acos(Math.max(-1, Math.min(1, cosA)))*180/Math.PI;
    var lam = (u[0]*v[0] + u[1]*v[1]) / (nv*nv);
    lec.innerHTML = "<span class=\"fx\">f = " + f(p[0], p[1]).toFixed(4).replace(".", ",") +
      "   ·   ángulo(∇f, ∇g) = " + angA.toFixed(2).replace(".", ",") + "°" +
      "   ·   λ = " + lam.toFixed(4).replace(".", ",") + "</span><br>" +
      (angA < 1.2
        ? "Los dos gradientes apuntan al mismo sitio: ahí se cumple ∇f = λ∇g y el punto es candidato a óptimo (Proposición 18.2). El mínimo de la curva continua cae justo aquí."
        : "Los gradientes no son paralelos, así que ∇f tiene componente a lo largo de la restricción: moverse en su contra baja f sin salirse, y el punto no puede ser óptimo.");
  }

  ct.addEventListener("input", dibujar);
  cg.addEventListener("change", dibujar);
  dibujar();
})();
</script>
```

## 2. Un caso conocido: la esfera y los eigenvalores

::: {.proposicion}
**Proposición 18.4 (Lagrange sobre la esfera es el problema de eigenvalores).** Sea $A$ simétrica y $f(z)=z^\top Az$. Los puntos críticos del problema
$$\min_z z^\top Az \quad\text{sujeto a}\quad \|z\|^2=1$$
son exactamente los eigenvectores unitarios de $A$, y el multiplicador asociado a cada uno es su eigenvalor. Además $f(z^\star)=\lambda$, así que el mínimo es el menor eigenvalor.
:::

::: {.demostracion}
*Demostración.* Con $g(z)=z^\top z-1$ se tiene $\nabla f(z)=2Az$ y $\nabla g(z)=2z$, el primero porque $A$ es simétrica. La condición de la Proposición 18.2 es
$$2Az = \lambda\cdot 2z \quad\Longleftrightarrow\quad Az=\lambda z,$$
que es la definición de eigenvector con eigenvalor $\lambda$ (lección 9). La restricción $\|z\|=1$ los normaliza. Y evaluando la función en uno de ellos,
$$f(z^\star) = z^{\star\top}Az^\star = z^{\star\top}(\lambda z^\star) = \lambda\,\|z^\star\|^2 = \lambda.$$
Como el mínimo existe, la esfera es compacta y $f$ continua, y todos los candidatos son eigenvalores, el mínimo es el menor de ellos. [∎]{.fin-demo}
:::

Ese resultado ya estaba en la Proposición 12.4, apartado (2), demostrado allí con la diagonalización ortogonal. Aquí sale de un principio distinto y más general, y eso es una comprobación cruzada: dos caminos independientes que llegan al mismo sitio.

```{pyodide}
import numpy as np

# minimizar f(x,y) = x^2 + 2y^2 + xy sobre la circunferencia x^2 + y^2 = 1.
# Las condiciones de Lagrange son n+1 ecuaciones con n+1 incognitas, y se
# resuelven con Newton (leccion 17) sobre el sistema entero.
def F(z):
    x, y, l = z
    return np.array([2*x + y - l*2*x,
                     4*y + x - l*2*y,
                     x*x + y*y - 1.0])

def J(z):
    x, y, l = z
    return np.array([[2 - 2*l, 1.0, -2*x],
                     [1.0, 4 - 2*l, -2*y],
                     [2*x, 2*y, 0.0]])

for z0 in [np.array([0.9, 0.4, 1.0]), np.array([0.3, 0.9, 3.0])]:
    z = z0.copy()
    for _ in range(60):
        z = z - np.linalg.solve(J(z), F(z))
    x, y, l = z
    print(f"arranque ({z0[0]:.1f},{z0[1]:.1f})  ->  x* = ({x:9.6f},{y:9.6f})   "
          f"lambda = {l:9.6f}   f = {x*x + 2*y*y + x*y:9.6f}   "
          f"||F|| < 1e-12: {bool(np.linalg.norm(F(z)) < 1e-12)}")
print()

# f(z) = z^T A z con A simetrica: a11 = 1, a22 = 2, y 2*a12 = 1
A = np.array([[1.0, 0.5], [0.5, 2.0]])
ev = np.linalg.eigvalsh(A)
print(f"eigenvalores de A: ({ev[0]:.6f}, {ev[1]:.6f})")
print("La estacionariedad 2Az = 2*lambda*z es la ecuacion de eigenvalores Az = lambda z,")
print("asi que los multiplicadores SON los eigenvalores de A, y f(z*) = z*^T A z* = lambda:")
print("el minimo sobre la circunferencia es el menor eigenvalor, que es la Proposicion 12.4")
```

Newton sobre el sistema de Lagrange encuentra los dos puntos críticos, con multiplicadores $0{,}792893$ y $2{,}207107$, que son exactamente los eigenvalores de $A$. Y en cada uno el valor de la función coincide con su multiplicador, como dice la Proposición 18.4.

## 3. Desigualdades: las condiciones KKT

::: {.definicion}
**Definición 18.5 (problema con desigualdades y condiciones KKT).** Para el problema
$$\min_x f(x) \quad\text{sujeto a}\quad g_i(x)\le0,\quad i=1,\dots,m,$$
las **condiciones de Karush-Kuhn-Tucker** en un punto $x^\star$ con multiplicadores $\lambda_i^\star$ son:

1. **estacionariedad**: $\nabla f(x^\star) + \sum_i\lambda_i^\star\nabla g_i(x^\star)=0$;
2. **factibilidad**: $g_i(x^\star)\le0$ para todo $i$;
3. **no negatividad**: $\lambda_i^\star\ge0$ para todo $i$;
4. **holgura complementaria**: $\lambda_i^\star\,g_i(x^\star)=0$ para todo $i$.
:::

La cuarta condición es la que distingue este caso del de igualdades, y dice algo muy concreto: para cada restricción, o está **activa** (se cumple con igualdad, $g_i=0$, y entonces su multiplicador puede ser positivo) o está holgada, y entonces su multiplicador es cero y la restricción no interviene en la estacionariedad. Una restricción que no aprieta no influye.

Bajo hipótesis de regularidad sobre las restricciones, las KKT son **necesarias** en todo mínimo local. Esa parte se enuncia aquí sin demostrar. Lo que sí se demuestra es la recíproca en el caso convexo, que es la que se usa en la práctica para certificar un óptimo.

::: {.proposicion}
**Proposición 18.6 (en un problema convexo las KKT son suficientes).** Sean $f$ y todas las $g_i$ convexas y diferenciables. Si $(x^\star,\lambda^\star)$ cumple las cuatro condiciones de la Definición 18.5, entonces $x^\star$ es un **mínimo global** del problema.
:::

::: {.demostracion}
*Demostración.* Sea $x$ cualquier punto factible. Por la Proposición 15.3 aplicada a $f$,
$$f(x) \;\ge\; f(x^\star) + \nabla f(x^\star)\cdot(x-x^\star).$$
Por la estacionariedad, $\nabla f(x^\star)=-\sum_i\lambda_i^\star\nabla g_i(x^\star)$, de donde
$$f(x) \;\ge\; f(x^\star) - \sum_i\lambda_i^\star\,\nabla g_i(x^\star)\cdot(x-x^\star).$$
Aplicando ahora la Proposición 15.3 a cada $g_i$, que es convexa,
$$\nabla g_i(x^\star)\cdot(x-x^\star) \;\le\; g_i(x)-g_i(x^\star),$$
y como $\lambda_i^\star\ge0$ multiplicar por él conserva el sentido de la desigualdad, así que al cambiar el signo,
$$-\sum_i\lambda_i^\star\,\nabla g_i(x^\star)\cdot(x-x^\star) \;\ge\; -\sum_i\lambda_i^\star\big[g_i(x)-g_i(x^\star)\big].$$
Juntando las dos cotas,
$$f(x) \;\ge\; f(x^\star) - \sum_i\lambda_i^\star g_i(x) + \sum_i\lambda_i^\star g_i(x^\star).$$
El último sumando es cero por holgura complementaria. Y en el penúltimo, cada término cumple $\lambda_i^\star\ge0$ y $g_i(x)\le0$ porque $x$ es factible, luego $-\lambda_i^\star g_i(x)\ge0$ y la suma entera es no negativa. Por tanto
$$f(x) \;\ge\; f(x^\star)$$
para todo $x$ factible: $x^\star$ es mínimo global. [∎]{.fin-demo}
:::

Las cuatro condiciones se usan una vez cada una, y la convexidad se usa dos veces. Quitar cualquiera de las piezas rompe la cadena: sin $\lambda_i\ge0$ la desigualdad se invierte, sin holgura complementaria queda un término de signo desconocido, y sin convexidad la Proposición 15.3 no se puede aplicar, que es lo que muestra el Ejercicio 2.

```{pyodide}
import numpy as np

# KKT con desigualdad: minimizar ||x - c||^2 sujeto a ||x||^2 <= 1.
# Si ||c|| <= 1 el minimo es c y la restriccion no esta activa (lambda = 0).
# Si ||c|| > 1 el minimo esta en el borde, x* = c/||c||, y lambda > 0.
def resolver(c):
    nc = np.linalg.norm(c)
    if nc <= 1.0:
        return c.copy(), 0.0
    return c/nc, nc - 1.0

print(f"{'||c||':>8s}  {'x* ':>22s}  {'||x*||':>8s}  {'lambda':>9s}  {'restriccion':>12s}")
for escala in [0.3, 0.9, 1.0, 1.6, 4.0]:
    c = escala*np.array([0.6, 0.8])              # direccion unitaria por escala
    x, lam = resolver(c)
    activa = "activa" if lam > 1e-12 else "inactiva"
    print(f"{np.linalg.norm(c):8.3f}  ({x[0]:9.6f},{x[1]:9.6f})  {np.linalg.norm(x):8.6f}  "
          f"{lam:9.6f}  {activa:>12s}")
print()
print("holgura complementaria lambda*(||x*||^2 - 1) = 0 en todos los casos:")
for escala in [0.3, 0.9, 1.0, 1.6, 4.0]:
    c = escala*np.array([0.6, 0.8])
    x, lam = resolver(c)
    print(f"  ||c|| = {np.linalg.norm(c):5.2f}:  lambda*(||x||^2-1) por debajo de 1e-12: "
          f"{bool(abs(lam*(x @ x - 1.0)) < 1e-12)}")
```

La tabla es la holgura complementaria en acción. Mientras $\|c\|\le1$ el óptimo es $c$ mismo, la restricción está holgada y $\lambda=0$: la bola no influye. En cuanto $\|c\|>1$ el óptimo salta al borde, la restricción pasa a estar activa y $\lambda$ crece con lo que haya que empujar. En ningún caso $\lambda$ y la holgura son ambos distintos de cero.

```{=html}
<div class="viz-kkt">
  <div class="controles">
    <label>c₁ <input id="kk-x" type="range" min="-28" max="28" step="1" value="12"> <span id="kk-x-v">1,2</span></label>
    <label>c₂ <input id="kk-y" type="range" min="-28" max="28" step="1" value="16"> <span id="kk-y-v">1,6</span></label>
  </div>
  <svg id="kk-svg" viewBox="0 0 620 320" role="img" aria-label="El disco unitario con el punto objetivo c y su proyeccion. Cuando c esta dentro la solucion es c mismo y el multiplicador vale cero; cuando esta fuera la solucion salta al borde y el multiplicador se vuelve positivo."></svg>
  <p class="lectura" id="kk-lectura"></p>
</div>
<style>
.viz-kkt { border: 1px solid var(--r-linea, #ddd); border-radius: 10px; padding: 1rem 1.1rem; margin: 1.7rem 0; }
.viz-kkt .controles { display: flex; flex-wrap: wrap; gap: .45rem 1.1rem; align-items: center; margin-bottom: .85rem; font-size: .85rem; }
.viz-kkt label { display: flex; align-items: center; gap: .4rem; }
.viz-kkt input[type=range] { width: 130px; padding: 0; }
.viz-kkt svg { width: 100%; height: auto; display: block; }
.viz-kkt .lectura { font-size: .85rem; margin-top: .75rem; min-height: 5.2em; line-height: 1.6; }
.viz-kkt .lectura .fx { font-family: var(--bs-font-monospace, monospace); font-size: .95em; }
</style>
<script>
(function () {
  var C_DISCO = "#0F6E68", C_C = "var(--r-tinta,#1f2430)", C_SOL = "#B45309";
  var svg = document.getElementById("kk-svg"), lec = document.getElementById("kk-lectura");
  var cx = document.getElementById("kk-x"), ex = document.getElementById("kk-x-v");
  var cy = document.getElementById("kk-y"), ey = document.getElementById("kk-y-v");

  // Marco constante en los dos paneles.
  var LCX = 160, LCY = 160, LESC = 62, LR = 2.2;
  var RX0 = 352, RX1 = 602, RY0 = 292, RY1 = 28, LMAX = 2.2;

  function el(t, at, txt) {
    var e = document.createElementNS("http://www.w3.org/2000/svg", t);
    for (var k in at) e.setAttribute(k, at[k]);
    if (txt !== undefined) e.textContent = txt;
    return e;
  }
  function lx(x) { return LCX + x * LESC; }
  function ly(y) { return LCY - y * LESC; }
  function qx(n) { return RX0 + Math.min(n, 3.2) / 3.2 * (RX1 - RX0); }
  function qy(l) { return RY0 - Math.max(0, Math.min(LMAX, l)) / LMAX * (RY0 - RY1); }

  function dibujar() {
    while (svg.firstChild) svg.removeChild(svg.firstChild);
    var c1 = (+cx.value) / 10, c2 = (+cy.value) / 10;
    ex.textContent = c1.toFixed(1).replace(".", ",").replace("-", "−");
    ey.textContent = c2.toFixed(1).replace(".", ",").replace("-", "−");
    var nc = Math.hypot(c1, c2);
    var dentro = nc <= 1;
    var s1 = dentro ? c1 : c1 / nc, s2 = dentro ? c2 : c2 / nc;
    var lam = dentro ? 0 : nc - 1;

    svg.appendChild(el("rect", { x: lx(-LR), y: ly(LR), width: 2*LR*LESC, height: 2*LR*LESC, fill: "none", stroke: "var(--r-linea,#ddd)" }));
    svg.appendChild(el("circle", { cx: lx(0).toFixed(1), cy: ly(0).toFixed(1), r: (1*LESC).toFixed(1),
      fill: C_DISCO, "fill-opacity": 0.09, stroke: C_DISCO, "stroke-width": 2 }));
    // curvas de nivel de ||x-c||^2 alrededor de c
    [0.3, 0.8, 1.5].forEach(function (r) {
      svg.appendChild(el("circle", { cx: lx(c1).toFixed(1), cy: ly(c2).toFixed(1), r: (Math.sqrt(r)*LESC).toFixed(1),
        fill: "none", stroke: C_C, "stroke-width": 1, "stroke-opacity": 0.22 }));
    });
    svg.appendChild(el("line", { x1: lx(c1).toFixed(1), y1: ly(c2).toFixed(1), x2: lx(s1).toFixed(1), y2: ly(s2).toFixed(1),
      stroke: C_SOL, "stroke-width": 1.4, "stroke-dasharray": "4 3" }));
    svg.appendChild(el("circle", { cx: lx(c1).toFixed(1), cy: ly(c2).toFixed(1), r: 4, fill: C_C }));
    svg.appendChild(el("circle", { cx: lx(s1).toFixed(1), cy: ly(s2).toFixed(1), r: 4.4, fill: C_SOL }));
    svg.appendChild(el("text", { x: lx(-LR), y: 18, "font-size": 11, "font-weight": 600, fill: "var(--r-tinta,#222)" },
      "min ‖x − c‖²  sujeto a  ‖x‖² ≤ 1"));

    // panel derecho: lambda en funcion de ||c||
    svg.appendChild(el("rect", { x: RX0, y: RY1, width: RX1 - RX0, height: RY0 - RY1, fill: "none", stroke: "var(--r-linea,#ddd)" }));
    var d = "";
    for (var i = 0; i <= 200; i++) {
      var n = 3.2*i/200;
      d += (i ? " L" : "M") + qx(n).toFixed(1) + "," + qy(Math.max(0, n - 1)).toFixed(1);
    }
    svg.appendChild(el("path", { d: d, fill: "none", stroke: C_SOL, "stroke-width": 2 }));
    svg.appendChild(el("line", { x1: qx(1).toFixed(1), y1: RY0, x2: qx(1).toFixed(1), y2: RY1,
      stroke: C_DISCO, "stroke-width": 1, "stroke-dasharray": "4 3" }));
    svg.appendChild(el("circle", { cx: qx(nc).toFixed(1), cy: qy(lam).toFixed(1), r: 4, fill: C_C }));
    svg.appendChild(el("text", { x: RX0, y: 18, "font-size": 11, "font-weight": 600, fill: "var(--r-tinta,#222)" }, "λ en función de ‖c‖"));
    svg.appendChild(el("text", { x: (qx(1) + 4).toFixed(1), y: (RY1 + 14).toFixed(1), "font-size": 9, fill: C_DISCO }, "‖c‖ = 1"));

    lec.innerHTML = "<span class=\"fx\">‖c‖ = " + nc.toFixed(3).replace(".", ",") +
      "   ·   x* = (" + s1.toFixed(3).replace(".", ",") + ", " + s2.toFixed(3).replace(".", ",") + ")" +
      "   ·   λ = " + lam.toFixed(3).replace(".", ",") +
      "   ·   restricción " + (dentro ? "inactiva" : "activa") + "</span><br>" +
      (dentro
        ? "El objetivo cae dentro del disco: la solución es c mismo, la restricción está holgada y λ = 0. La bola no influye en nada."
        : "El objetivo cae fuera: la solución salta al borde, la restricción pasa a estar activa y λ = ‖c‖ − 1 crece con lo que haya que empujar. " +
          "En los dos casos λ·(‖x*‖² − 1) = 0, que es la holgura complementaria.");
  }

  cx.addEventListener("input", dibujar);
  cy.addEventListener("input", dibujar);
  dibujar();
})();
</script>
```

## Ejercicios

### Ejercicio 1, Mínimos cuadrados con una restricción lineal

Minimizar $\|y-Xb\|^2$ sujeto a $a^\top b=c$. El lagrangiano es cuadrático, así que anular sus derivadas da un **sistema lineal** de $n+1$ ecuaciones. Resolverlo y comprobar que restringir nunca mejora el ajuste.

```{pyodide}
import numpy as np

rng = np.random.default_rng(180)
X = rng.normal(size=(40, 3))
y = rng.normal(size=40)
a = np.array([1.0, 1.0, 1.0])                  # los coeficientes deben sumar c
c = 2.0

H = 2*X.T @ X                                   # hessiana de ||y - Xb||^2
grad0 = -2*X.T @ y                              # gradiente en b = 0
# [[H, a],[a^T, 0]] [b, -l] = [-grad0, c]
K = np.zeros((4, 4)); K[:3, :3] = H; K[:3, 3] = a; K[3, :3] = a
r = np.concatenate([-grad0, [c]])
sol = np.linalg.solve(K, r)
b_res, lam = sol[:3], -sol[3]
b_libre = np.linalg.lstsq(X, y, rcond=None)[0]

print(f"sin restriccion:  b = {np.round(b_libre, 6)}   suma = {b_libre.sum():.6f}")
print(f"con restriccion:  b = {np.round(b_res, 6)}   suma = {b_res.sum():.6f}")
print(f"la restriccion se cumple (|suma - 2| < 1e-10): {bool(abs(b_res.sum() - c) < 1e-10)}")
print(f"multiplicador lambda = {lam:.6f}")
print()
sce_libre = np.sum((y - X @ b_libre)**2)
sce_res = np.sum((y - X @ b_res)**2)
print(f"suma de cuadrados sin restriccion: {sce_libre:.6f}")
print(f"suma de cuadrados con restriccion: {sce_res:.6f}")
print(f"restringir no puede mejorar el ajuste: {bool(sce_res >= sce_libre)}")
```

### Ejercicio 2, Sin convexidad las KKT solo señalan candidatos

Comprobar la Proposición 18.6 en un problema convexo, y encontrar un punto que cumple las cuatro condiciones y sin embargo es el **peor** punto factible cuando el objetivo no es convexo.

```{pyodide}
import numpy as np

print("problema convexo: min x^2+y^2  s.a.  x+y >= 1")
print("  KKT: (2x,2y) = l(1,1), l >= 0, l(1-x-y) = 0  ->  x = y = 1/2, l = 1")
x = np.array([0.5, 0.5]); l = 1.0
print(f"  estacionariedad (norma < 1e-12): {bool(np.linalg.norm(2*x - l*np.ones(2)) < 1e-12)}")
print(f"  factible: {bool(x.sum() >= 1 - 1e-12)}   l >= 0: {bool(l >= 0)}   "
      f"holgura: {bool(abs(l*(1 - x.sum())) < 1e-12)}")
rng = np.random.default_rng(181)
d = rng.normal(size=(200000, 2))
d /= np.linalg.norm(d, axis=1, keepdims=True)
puntos = x + rng.uniform(0, 3, size=(200000, 1))*d
fact = puntos[puntos.sum(axis=1) >= 1]
print(f"  de {len(fact)} puntos factibles sorteados, ninguno mejora a x*: "
      f"{bool(np.all(np.sum(fact**2, axis=1) >= x @ x - 1e-12))}")
print()
print("problema no convexo: min -(x^2+y^2)  s.a.  x^2+y^2 <= 1")
print("  el origen cumple las KKT con l = 0, y es el PEOR punto factible")
o = np.zeros(2)
obj_o = float(-(o @ o)); obj_o = 0.0 if obj_o == 0 else obj_o     # evitar el -0.0
print(f"  estacionariedad en el origen (norma < 1e-12): {bool(np.linalg.norm(-2*o) < 1e-12)}")
q = rng.normal(size=(200000, 2))
q /= np.linalg.norm(q, axis=1, keepdims=True)
q *= rng.uniform(0, 1, size=(200000, 1))**0.5                     # uniforme en el disco
mejores = int((-(np.sum(q**2, axis=1)) < obj_o - 1e-12).sum())
print(f"  objetivo en el origen: {obj_o:.4f}   en el borde: {-1.0:.4f}")
print(f"  puntos factibles que baten al origen: {mejores} de 200000")
print()
print("con convexidad las KKT certifican el optimo global (Proposicion 18.6);")
print("sin ella solo senalan candidatos")
```

En el problema convexo ninguno de los casi cien mil puntos factibles sorteados mejora al punto que cumple las KKT, como garantiza la Proposición 18.6. En el no convexo, el origen cumple las cuatro condiciones con $\lambda=0$ y **los doscientos mil** puntos factibles sorteados son mejores que él: sin convexidad, las KKT no certifican nada.

## Reto

En `proyectos/notebooks/F1-retos.ipynb`, sección **Mat 18**:

1. Implementar el método de penalización (minimizar $f(x)+\rho\,g(x)^2$ sin restricciones, con $\rho$ creciente) y comprobar que la solución converge a la de Lagrange. Medir cómo crece el número de condición de la hessiana con $\rho$, y relacionarlo con la Proposición 16.5 para explicar por qué el método se vuelve lento.
2. Resolver la proyección sobre el símplex, minimizar $\|x-c\|^2$ sujeto a $\sum x_i=1$ y $x_i\ge0$, escribiendo las KKT a mano y deduciendo el algoritmo de umbral que sale de la holgura complementaria. Comprobarlo contra un solucionador genérico.
3. Tomar el Ejercicio 1 y añadir la restricción de desigualdad $b_i\ge0$. Resolverlo iterando sobre qué restricciones están activas, y anotar en `50-Errores/` cuántas combinaciones hay que probar en el peor caso.

## Del libro

*Mathematics for Machine Learning* trata este material en §7.2 *Constrained Optimization and Lagrange Multipliers*, dentro del capítulo 7 sobre optimización continua. El libro tiene copyright y no es descargable desde el entorno en que se escribió esta página, así que se cita número y título y no se le atribuye ninguna frase.

**Preguntas para leer §7.2 con lápiz:**

1. La sección introduce el lagrangiano y el problema dual asociado. Con la Definición 18.3, ¿por qué el lagrangiano evaluado en un punto factible nunca supera a $f$ en ese punto cuando los multiplicadores son no negativos, y qué dice eso sobre el valor del dual frente al del primal?
2. §7.2 distingue restricciones de igualdad y de desigualdad. Con la holgura complementaria de la Definición 18.5, ¿por qué una igualdad no impone signo al multiplicador y una desigualdad sí?
3. El capítulo conecta esta sección con §7.3 sobre optimización convexa. Con la Proposición 18.6, ¿qué se gana exactamente al suponer convexidad, y por qué el Ejercicio 2 es un contraejemplo y no un fallo del método?

## Para el Cerebro

Nota nueva en `10-Conceptos/lagrange-kkt.md`, enlazada a [[convexidad]], [[eigenvalores]] y [[metodo-de-newton]]. Conviene rehacer a mano la demostración de la Proposición 18.6: usa cada una de las cuatro condiciones KKT exactamente una vez y la convexidad dos, y ver dónde entra cada pieza explica por qué ninguna sobra.

```
¿Qué dice la condición de Lagrange?::En el óptimo con g = 0, ∇f(x*) = λ∇g(x*): los gradientes son paralelos
¿Por qué tienen que ser paralelos?::Si no, ∇f tendría componente tangente a la restricción y moverse en su contra bajaría f
¿Qué es el lagrangiano?::L(x,λ) = f(x) − λg(x); anular sus derivadas da la condición y la restricción a la vez
¿Cuántas ecuaciones son?::n+1 con n+1 incógnitas, y se resuelven con Newton
¿Qué es Lagrange sobre la esfera unitaria?::El problema de eigenvalores: Az = λz, y f(z*) = λ
¿Cuáles son las cuatro condiciones KKT?::Estacionariedad, factibilidad, multiplicadores no negativos, y holgura complementaria
¿Qué dice la holgura complementaria?::λᵢgᵢ(x*) = 0: o la restricción está activa, o su multiplicador es cero
¿Qué es una restricción activa?::Una desigualdad que en el óptimo se cumple con igualdad; solo esas influyen
¿Cuándo son suficientes las KKT?::Cuando f y todas las gᵢ son convexas (Proposición 18.6)
¿Y si no hay convexidad?::Solo son necesarias: el origen cumple las KKT de min −‖x‖² sobre el disco y es el peor punto
¿Qué pasa con λ al aumentar la presión de la restricción?::Crece: en la proyección sobre la bola, λ = ‖c‖ − 1
```

::: {#fuentes .fuentes}
### Fuentes

**Lo que esta página demuestra sola.** Las Proposiciones 18.2, 18.4 y 18.6 se demuestran aquí a partir de la regla de la cadena de la lección 13, del complemento ortogonal de la 5 y de la caracterización de primer orden de la convexidad de la 15. Se comprueban además numéricamente: que el ángulo entre $\nabla f$ y $\nabla g$ se anula exactamente donde $f$ es mínima sobre la recta, y crece al alejarse en los dos sentidos; que Newton sobre el sistema de Lagrange en la circunferencia devuelve multiplicadores $0{,}792893$ y $2{,}207107$, que son los eigenvalores de la matriz de la forma cuadrática, y que en cada punto crítico el valor de la función iguala su multiplicador; la holgura complementaria en cinco escalas del objetivo, con $\lambda$ pasando de cero a positivo justo al salir de la bola; el sistema lineal de mínimos cuadrados con restricción, con la restricción cumplida y el ajuste empeorando; y que en el problema convexo ninguno de 99 458 puntos factibles mejora al punto KKT mientras en el no convexo los 200 000 baten al origen. Los dos visuales se contrastaron antes de publicarse contra las fórmulas en todo el rango de sus controles.

**Lo que se usa de otras lecciones sin repetir.** La regla de la cadena es la Proposición 13.3 y $D_vf=\nabla f\cdot v$ la 13.8. Que un mínimo local de una función de una variable tiene derivada nula es la Proposición 14.7. El complemento ortogonal de un hiperplano es la lección 5. La caracterización de primer orden de la convexidad, usada dos veces en la Proposición 18.6, es la 15.3. Que el mínimo del cociente de Rayleigh sobre la esfera es el menor eigenvalor es la Proposición 12.4, y aquí se reobtiene por otro camino. El método de Newton para sistemas es la lección 17.

**Lo que se enuncia sin demostrar.** El teorema de la función implícita, del que depende la existencia de la curva $\gamma$ en la Proposición 18.2. La **necesidad** de las condiciones KKT en un mínimo local: requiere una hipótesis de regularidad sobre las restricciones y un argumento de separación que quedan fuera de esta lección; aquí solo se demuestra la suficiencia en el caso convexo. En la Proposición 18.4 se usa que el mínimo existe apelando a la compacidad de la esfera sin demostrar el teorema correspondiente.

**Lo que viene de los libros.**

- [*Mathematics for Machine Learning*](https://mml-book.github.io/book/mml-book.pdf) (Deisenroth, Faisal & Ong, Cambridge University Press, 2020), §7.2 *Constrained Optimization and Lagrange Multipliers* — citada por número y título, sin transcribir texto.

**Lo que es mío, no del libro.** La Proposición 18.4, que presenta el problema de eigenvalores como caso particular de Lagrange y cierra el círculo con la lección 12 por un camino independiente. La demostración de la Proposición 18.6 escrita para que se vea cada condición KKT usarse una sola vez. Y el Ejercicio 2, construido para que el contraejemplo no sea un punto raro sino el peor punto factible del problema, batido por los doscientos mil puntos sorteados.

[Índices verificados el 12-09-2026 contra el índice publicado del PDF oficial.]{.sello}
:::
