---
title: "Hessiana y expansión de Taylor"
subtitle: "Álgebra · Lección 14"
nivel: L2
modulo: matematica
fase: F1
prereqs: [matematica/13-derivada-gradiente, matematica/12-formas-cuadraticas]
siguientes: [matematica/15-convexidad]
libro: "MML §5.7 Higher-Order Derivatives · §5.8 Linearization and Multivariate Taylor Series"
engine: markdown
pyodide:
  packages:
    - numpy
---

::: {.meta-leccion data-leccion="matematica/14-hessiana-taylor"}
[[L2]{.nivel .nivel-L2} **Derivación**]{.item}
[**Fase** F1]{.item}
[**Libro** MML §5.7 · §5.8]{.item}
[**Prereqs** [Derivada y gradiente](13-derivada-gradiente.qmd) · [Formas cuadráticas](12-formas-cuadraticas.qmd)]{.item}
:::

## Objetivo

Al terminar esta lección se puede:

1. Definir las derivadas parciales de segundo orden y la [hessiana]{.g}, y demostrar que es simétrica cuando las segundas parciales son continuas.
2. Demostrar la fórmula de [Taylor]{.g data-t="polinomio de Taylor"} de segundo orden, y que el error del polinomio cuadrático es $o(\|h\|^2)$.
3. Demostrar que en un mínimo local el gradiente se anula, y que un [punto crítico]{.g} con hessiana definida positiva es un mínimo local estricto.
4. Clasificar un punto crítico (mínimo, máximo o [punto silla]{.g}) leyendo los signos de los eigenvalores de la hessiana.

:::: {.hilo}
::: {}
#### De dónde viene

- **Álgebra 13**: el gradiente y la aproximación lineal (Definición 13.7). Taylor de segundo orden es esa misma aproximación con un término más, y la regla de la cadena de la Proposición 13.3 es lo que permite demostrarla.
- **Álgebra 12**: las formas cuadráticas y el criterio espectral (Proposición 12.4). La hessiana es simétrica, así que $h^\top H h$ es una forma cuadrática y su signo lo deciden los eigenvalores.
- **Álgebra 9 y 10**: los eigenvalores de una matriz simétrica, que aquí clasifican puntos críticos.
:::

::: {}
#### Para qué sirve después

- **Álgebra 15**: una función con hessiana semidefinida positiva en todo punto es convexa, y ahí todo mínimo local pasa a ser global.
- **Álgebra 16 y 17**: el descenso de gradiente usa el término lineal; el método de Newton minimiza en cada paso el modelo cuadrático completo de la Proposición 14.5.
- **Estadística 8**: la segunda derivada de la log-verosimilitud es lo que decide si un punto crítico es el máximo, y su esperada cambiada de signo es la información de Fisher.
:::
::::

## Notación

::: {.notacion}
| Símbolo | Se lee | Significado |
|---|---|---|
| $\dfrac{\partial^2 f}{\partial x_i\partial x_j}$ | derivada parcial segunda | Derivar respecto a $x_j$ y después respecto a $x_i$ |
| $H_f(a)$ | hache sub efe de a | La hessiana de $f$ en $a$; también se escribe $\nabla^2f(a)$ |
| $T_1(h),\;T_2(h)$ | te uno, te dos | Los polinomios de Taylor de orden uno y dos |
| $r_2(h)$ | resto | El error de $T_2$; cumple $r_2(h)/\|h\|^2\to0$ |
| $\lambda_1\ge\lambda_2$ | lambdas | Los eigenvalores de la hessiana |
| $a$ | a | El punto donde se desarrolla |
| $h$ | hache | El desplazamiento desde $a$; tiende a cero |
:::

## 1. La segunda derivada en varias variables

::: {.definicion}
**Definición 14.1 (derivadas parciales de segundo orden).** Si la derivada parcial $\partial f/\partial x_j$ existe en un entorno de $a$ y ella misma admite derivada parcial respecto a $x_i$, esa derivada es
$$\frac{\partial^2 f}{\partial x_i\,\partial x_j}(a) := \frac{\partial}{\partial x_i}\Big(\frac{\partial f}{\partial x_j}\Big)(a).$$
:::

::: {.definicion}
**Definición 14.2 (hessiana).** La **hessiana** de $f$ en $a$ es la matriz $n\times n$ de todas las segundas parciales,
$$H_f(a) := \begin{pmatrix}
\dfrac{\partial^2 f}{\partial x_1^2}(a) & \cdots & \dfrac{\partial^2 f}{\partial x_1\partial x_n}(a)\\
\vdots & \ddots & \vdots\\
\dfrac{\partial^2 f}{\partial x_n\partial x_1}(a) & \cdots & \dfrac{\partial^2 f}{\partial x_n^2}(a)
\end{pmatrix}.$$
:::

El gradiente de la lección 13 es un vector; la hessiana es una matriz. Que sea **simétrica** no es gratuito: depende de una hipótesis que se puede incumplir.

::: {.proposicion}
**Proposición 14.3 (simetría de la hessiana).** Si las dos parciales cruzadas $\partial^2f/\partial x_i\partial x_j$ y $\partial^2f/\partial x_j\partial x_i$ existen y son continuas en un entorno de $a$, entonces coinciden en $a$. En ese caso $H_f(a)$ es simétrica.
:::

::: {.demostracion}
*Demostración.* Basta el caso de dos variables: las demás coordenadas quedan fijas. Escríbase $a=(a_1,a_2)$ y, para $s,t$ pequeños y no nulos, el **doble incremento**
$$\Delta(s,t) = f(a_1+s,\,a_2+t) - f(a_1+s,\,a_2) - f(a_1,\,a_2+t) + f(a_1,\,a_2).$$
Esa expresión se puede leer de dos maneras, agrupando por la primera variable o por la segunda.

**Agrupando por la primera.** Sea $\varphi(x) = f(x,a_2+t) - f(x,a_2)$. Entonces $\Delta(s,t) = \varphi(a_1+s)-\varphi(a_1)$ y, por el teorema del valor medio, existe $\xi$ entre $a_1$ y $a_1+s$ con
$$\Delta(s,t) = s\,\varphi'(\xi) = s\Big[\frac{\partial f}{\partial x}(\xi,a_2+t) - \frac{\partial f}{\partial x}(\xi,a_2)\Big].$$
Aplicando otra vez el valor medio, ahora a la función $y\mapsto \partial f/\partial x(\xi,y)$, existe $\eta$ entre $a_2$ y $a_2+t$ con
$$\Delta(s,t) = s\,t\;\frac{\partial^2 f}{\partial y\,\partial x}(\xi,\eta).$$

**Agrupando por la segunda.** El mismo argumento con $\psi(y) = f(a_1+s,y)-f(a_1,y)$ produce $\xi',\eta'$ en los mismos intervalos con
$$\Delta(s,t) = s\,t\;\frac{\partial^2 f}{\partial x\,\partial y}(\xi',\eta').$$

Igualando y dividiendo entre $st\ne0$,
$$\frac{\partial^2 f}{\partial y\,\partial x}(\xi,\eta) = \frac{\partial^2 f}{\partial x\,\partial y}(\xi',\eta').$$
Cuando $(s,t)\to(0,0)$ los cuatro puntos intermedios tienden a $a$, porque están atrapados en los intervalos que se encogen. Por la continuidad de las dos parciales cruzadas, cada lado tiende a su valor en $a$, y los límites coinciden. [∎]{.fin-demo}
:::

La hipótesis de continuidad es la que hace el trabajo, y sin ella el resultado es falso. La celda construye el contraejemplo clásico: una función cuyas dos parciales cruzadas existen en el origen y valen $-1$ y $+1$.

```{pyodide}
import numpy as np

def f(x, y):
    return x**3 * y**2 + np.sin(x*y)

def parcial_cruzada(fn, x, y, h=1e-4):
    return (fn(x+h, y+h) - fn(x+h, y-h) - fn(x-h, y+h) + fn(x-h, y-h)) / (4*h*h)

a, b = 1.3, -0.8
exacta = 6*a**2*b + np.cos(a*b) - a*b*np.sin(a*b)
print(f"f(x,y) = x^3 y^2 + sin(xy)   en el punto (1.3, -0.8)")
print(f"  d2f/dx dy medida : {parcial_cruzada(f, a, b):.6f}")
print(f"  d2f/dx dy exacta : {exacta:.6f}")
print(f"  coinciden hasta 1e-4: {abs(parcial_cruzada(f, a, b) - exacta) < 1e-4}")
print()

# Contraejemplo: las dos cruzadas existen en el origen pero no son continuas ahi
def g(x, y):
    r2 = x*x + y*y
    seguro = np.where(r2 == 0, 1.0, r2)
    return np.where(r2 == 0, 0.0, x*y*(x*x - y*y) / seguro)

def g_x_en(y, h=1e-6):
    return (g(h, y) - g(-h, y)) / (2*h)

def g_y_en(x, h=1e-6):
    return (g(x, h) - g(x, -h)) / (2*h)

k = 1e-5
cruzada_1 = (g_x_en(k) - g_x_en(-k)) / (2*k)
cruzada_2 = (g_y_en(k) - g_y_en(-k)) / (2*k)
print("g(x,y) = xy(x^2-y^2)/(x^2+y^2), con g(0,0) = 0:")
print(f"  derivando primero en x y luego en y : {cruzada_1:.3f}   (valor exacto: -1)")
print(f"  derivando primero en y y luego en x : {cruzada_2:.3f}   (valor exacto:  1)")
print(f"  se diferencian en mas de 1.5: {abs(cruzada_1 - cruzada_2) > 1.5}")
```

En la función de arriba las cruzadas coinciden hasta la sexta cifra. En la de abajo difieren en dos unidades enteras: sus segundas parciales existen en el origen pero dan saltos alrededor de él, y la Proposición 14.3 no se les aplica. De aquí en adelante se supone que las segundas parciales son continuas, y por tanto que $H_f(a)$ es simétrica, que es justo lo que hace falta para usar la lección 12.

## 2. Taylor de segundo orden

::: {.definicion}
**Definición 14.4 (polinomios de Taylor).** Para $f$ con gradiente y hessiana en $a$, los polinomios de Taylor de orden uno y dos en el desplazamiento $h$ son
$$T_1(h) = f(a) + \nabla f(a)\cdot h, \qquad
T_2(h) = f(a) + \nabla f(a)\cdot h + \tfrac{1}{2}\,h^\top H_f(a)\,h.$$
:::

$T_1$ es la aproximación lineal de la Definición 13.7. $T_2$ le añade la forma cuadrática que define la hessiana.

::: {.proposicion}
**Proposición 14.5 (Taylor de segundo orden).** Si las segundas parciales de $f$ existen y son continuas en un entorno de $a$, entonces
$$f(a+h) = T_2(h) + r_2(h), \qquad \frac{r_2(h)}{\|h\|^2}\longrightarrow 0 \;\text{ cuando } h\to0.$$
:::

::: {.demostracion}
*Demostración.* Fíjese $h$ y defínase la función de una variable $\phi(t) = f(a+th)$ para $t\in[0,1]$. Por la regla de la cadena (Proposición 13.3) junto con la Proposición 13.8,
$$\phi'(t) = \nabla f(a+th)\cdot h, \qquad \phi''(t) = h^\top H_f(a+th)\,h,$$
donde la segunda igualdad es la primera derivada aplicada de nuevo, componente a componente. Por el desarrollo de Taylor de una variable con resto de Lagrange, que se da por conocido, existe $\theta\in(0,1)$ con
$$\phi(1) = \phi(0) + \phi'(0) + \tfrac{1}{2}\phi''(\theta),$$
es decir
$$f(a+h) = f(a) + \nabla f(a)\cdot h + \tfrac{1}{2}\,h^\top H_f(a+\theta h)\,h.$$
Restando $T_2(h)$ queda
$$r_2(h) = \tfrac{1}{2}\,h^\top\big[H_f(a+\theta h) - H_f(a)\big]h,$$
y acotando la forma cuadrática por la mayor magnitud de sus entradas,
$$\frac{|r_2(h)|}{\|h\|^2} \;\le\; \tfrac{1}{2}\,\big\|H_f(a+\theta h) - H_f(a)\big\|.$$
Cuando $h\to0$ el punto $a+\theta h$ tiende a $a$, y por la continuidad de las segundas parciales la diferencia de matrices tiende a la matriz nula. Luego el cociente tiende a cero. [∎]{.fin-demo}
:::

```{=html}
<div class="viz-taylor">
  <div class="controles">
    <label>Punto a <input id="ta-a" type="range" min="-25" max="25" step="1" value="8"> <span id="ta-a-v">0,8</span></label>
    <label>Desplazamiento h <input id="ta-h" type="range" min="1" max="28" step="1" value="18"> <span id="ta-h-v">1,8</span></label>
  </div>
  <svg id="ta-svg" viewBox="0 0 620 320" role="img" aria-label="Una curva con su recta tangente y su parabola de Taylor en el punto a. En a mas h se marcan los tres valores y se miden los dos errores; al reducir h el error de la parabola se hunde mucho mas rapido que el de la recta."></svg>
  <p class="lectura" id="ta-lectura"></p>
</div>
<style>
.viz-taylor { border: 1px solid var(--r-linea, #ddd); border-radius: 10px; padding: 1rem 1.1rem; margin: 1.7rem 0; }
.viz-taylor .controles { display: flex; flex-wrap: wrap; gap: .45rem 1.1rem; align-items: center; margin-bottom: .85rem; font-size: .85rem; }
.viz-taylor label { display: flex; align-items: center; gap: .4rem; }
.viz-taylor input[type=range] { width: 118px; padding: 0; }
.viz-taylor svg { width: 100%; height: auto; display: block; }
.viz-taylor .lectura { font-size: .85rem; margin-top: .75rem; min-height: 5em; line-height: 1.6; }
.viz-taylor .lectura .fx { font-family: var(--bs-font-monospace, monospace); font-size: .95em; }
</style>
<script>
(function () {
  var C_F = "var(--r-tinta,#1f2430)", C_T1 = "#B45309", C_T2 = "#0F6E68";
  var svg = document.getElementById("ta-svg"), lec = document.getElementById("ta-lectura");
  var ca = document.getElementById("ta-a"), ea = document.getElementById("ta-a-v");
  var ch = document.getElementById("ta-h"), eh = document.getElementById("ta-h-v");

  // Marco constante: no depende ni de a ni de h.
  var X0 = 40, X1 = 600, Y0 = 296, Y1 = 24;
  var RX = 3.4, YMIN = -1.6, YMAX = 3.4;

  function el(t, at, txt) {
    var e = document.createElementNS("http://www.w3.org/2000/svg", t);
    for (var k in at) e.setAttribute(k, at[k]);
    if (txt !== undefined) e.textContent = txt;
    return e;
  }
  function px(x) { return X0 + (x + RX) / (2 * RX) * (X1 - X0); }
  function py(y) { return Y0 - (Math.max(YMIN, Math.min(YMAX, y)) - YMIN) / (YMAX - YMIN) * (Y0 - Y1); }

  function f(x) { return Math.sin(x) + 0.25 * x * x; }
  function fp(x) { return Math.cos(x) + 0.5 * x; }
  function fpp(x) { return -Math.sin(x) + 0.5; }

  function dibujar() {
    while (svg.firstChild) svg.removeChild(svg.firstChild);
    var a = (+ca.value) / 10, h = (+ch.value) / 10;
    ea.textContent = a.toFixed(1).replace(".", ",").replace("-", "−");
    eh.textContent = h.toFixed(1).replace(".", ",");

    svg.appendChild(el("rect", { x: X0, y: Y1, width: X1 - X0, height: Y0 - Y1, fill: "none", stroke: "var(--r-linea,#ddd)" }));
    svg.appendChild(el("line", { x1: X0, y1: py(0), x2: X1, y2: py(0), stroke: "var(--r-linea,#eee)" }));

    var i, x, d = "";
    for (i = 0; i <= 260; i++) {
      x = -RX + 2 * RX * i / 260;
      d += (i ? " L" : "M") + px(x).toFixed(1) + "," + py(f(x)).toFixed(1);
    }
    svg.appendChild(el("path", { d: d, fill: "none", stroke: C_F, "stroke-width": 1.9 }));

    var fa = f(a), d1 = fp(a), d2 = fpp(a);
    var dT1 = "", dT2 = "";
    for (i = 0; i <= 260; i++) {
      x = -RX + 2 * RX * i / 260;
      var u = x - a;
      dT1 += (i ? " L" : "M") + px(x).toFixed(1) + "," + py(fa + d1 * u).toFixed(1);
      dT2 += (i ? " L" : "M") + px(x).toFixed(1) + "," + py(fa + d1 * u + 0.5 * d2 * u * u).toFixed(1);
    }
    svg.appendChild(el("path", { d: dT1, fill: "none", stroke: C_T1, "stroke-width": 1.5, "stroke-dasharray": "6 4" }));
    svg.appendChild(el("path", { d: dT2, fill: "none", stroke: C_T2, "stroke-width": 1.5, "stroke-dasharray": "2 3" }));

    var xb = a + h, real = f(xb), t1 = fa + d1 * h, t2 = fa + d1 * h + 0.5 * d2 * h * h;
    svg.appendChild(el("line", { x1: px(xb).toFixed(1), y1: Y0, x2: px(xb).toFixed(1), y2: Y1, stroke: "var(--r-linea,#ddd)", "stroke-dasharray": "3 3" }));
    svg.appendChild(el("circle", { cx: px(a).toFixed(1), cy: py(fa).toFixed(1), r: 4, fill: C_F }));
    svg.appendChild(el("circle", { cx: px(xb).toFixed(1), cy: py(real).toFixed(1), r: 3.8, fill: C_F }));
    svg.appendChild(el("circle", { cx: px(xb).toFixed(1), cy: py(t1).toFixed(1), r: 3.8, fill: C_T1 }));
    svg.appendChild(el("circle", { cx: px(xb).toFixed(1), cy: py(t2).toFixed(1), r: 3.8, fill: C_T2 }));
    svg.appendChild(el("text", { x: px(a), y: (py(fa) + 18).toFixed(1), "font-size": 10, "text-anchor": "middle", fill: C_F }, "a"));
    svg.appendChild(el("text", { x: X0 + 8, y: 40, "font-size": 11, "font-weight": 600, fill: "var(--r-tinta,#222)" }, "f(x) = sen(x) + 0,25 x²"));
    svg.appendChild(el("text", { x: X0 + 8, y: 56, "font-size": 10, fill: C_T1 }, "— — T₁, la recta tangente"));
    svg.appendChild(el("text", { x: X0 + 8, y: 70, "font-size": 10, fill: C_T2 }, "· · · T₂, la parábola de Taylor"));

    var e1 = Math.abs(real - t1), e2 = Math.abs(real - t2);
    lec.innerHTML = "<span class=\"fx\">error de T₁ = " + e1.toFixed(4).replace(".", ",") +
      "   ·   error de T₂ = " + e2.toFixed(4).replace(".", ",") +
      "   ·   cociente = " + (e2 > 1e-9 ? (e1 / e2).toFixed(1).replace(".", ",") : "—") + "</span><br>" +
      "Los dos errores se miden en a+h. Al acercar h a cero el de la recta cae como h² y el de la parábola como h³, " +
      "así que el cociente entre ambos crece sin tope: por eso T₂ vale y T₁ no, en cuanto la curvatura importa.";
  }

  ca.addEventListener("input", dibujar);
  ch.addEventListener("input", dibujar);
  dibujar();
})();
</script>
```

```{pyodide}
import numpy as np

def f(p):
    x, y = p
    return x**3 * y**2 + np.sin(x*y)

def grad(p):
    x, y = p
    return np.array([3*x**2*y**2 + y*np.cos(x*y), 2*x**3*y + x*np.cos(x*y)])

def hess(p):
    x, y = p
    fxx = 6*x*y**2 - y**2*np.sin(x*y)
    fxy = 6*x**2*y + np.cos(x*y) - x*y*np.sin(x*y)
    fyy = 2*x**3 - x**2*np.sin(x*y)
    return np.array([[fxx, fxy], [fxy, fyy]])

a = np.array([1.3, -0.8])
fa, g, H = f(a), grad(a), hess(a)
u = np.array([0.6, 0.8])

print(f"{'t':>9s}  {'error T1 / t^2':>16s}  {'error T2 / t^3':>16s}")
for t in [0.2, 0.05, 0.01, 0.002, 0.0005]:
    h = t * u
    e1 = abs(f(a + h) - (fa + g @ h))
    e2 = abs(f(a + h) - (fa + g @ h + 0.5 * h @ H @ h))
    print(f"{t:9.4f}  {e1/t**2:16.6f}  {e2/t**3:16.6f}")
print()
print(f"el cociente de la izquierda tiende a |u^T H u| / 2 = {abs(0.5 * u @ H @ u):.6f}")
print("es decir: el termino cuadratico ES el error de la aproximacion lineal")
```

Las dos columnas se estabilizan, y eso es la Proposición 14.5 medida: el error de $T_1$ es de orden $t^2$ y el de $T_2$, de orden $t^3$. La primera columna converge además a $|u^\top H u|/2 = 1{,}210909$, que es exactamente el término cuadrático: lo que a $T_1$ le falta es, hasta orden superior, lo que $T_2$ le añade.

## 3. Puntos críticos y cómo clasificarlos

::: {.definicion}
**Definición 14.6 (punto crítico).** Un punto $a$ es **crítico** para $f$ si $f$ es diferenciable en $a$ y $\nabla f(a)=0$.
:::

::: {.proposicion}
**Proposición 14.7 (condición necesaria de primer orden).** Si $f$ es diferenciable en $a$ y $a$ es un mínimo local, o un máximo local, entonces $a$ es un punto crítico.
:::

::: {.demostracion}
*Demostración.* Supóngase que $a$ es mínimo local y que $\nabla f(a)\ne0$, y tómese la dirección unitaria $v = -\nabla f(a)/\|\nabla f(a)\|$. Por la Proposición 13.9 aplicada a $-v$, esa es la dirección de máximo descenso y
$$D_vf(a) = \nabla f(a)\cdot v = -\|\nabla f(a)\| < 0.$$
Por la Definición 13.6, el cociente $[f(a+tv)-f(a)]/t$ tiende a ese número negativo cuando $t\to0$, así que para todo $t>0$ suficientemente pequeño el cociente es negativo; multiplicando por $t>0$,
$$f(a+tv) < f(a).$$
Hay entonces puntos tan cerca de $a$ como se quiera donde $f$ vale menos, y $a$ no era mínimo local. La contradicción obliga a $\nabla f(a)=0$. El caso del máximo es el mismo con $-f$. [∎]{.fin-demo}
:::

El recíproco es falso, y por eso hace falta el segundo orden: en un punto crítico el gradiente no dice nada más, y quien decide es la hessiana.

::: {.proposicion}
**Proposición 14.8 (condición suficiente de segundo orden).** Sea $a$ un punto crítico de $f$, con segundas parciales continuas, y sea $H=H_f(a)$ con eigenvalores $\lambda_1\ge\dots\ge\lambda_n$.

1. Si $H$ es definida positiva ($\lambda_n>0$), entonces $a$ es un **mínimo local estricto**.
2. Si $H$ es definida negativa ($\lambda_1<0$), entonces $a$ es un **máximo local estricto**.
3. Si $H$ es indefinida ($\lambda_1>0>\lambda_n$), entonces $a$ es un **punto silla**: hay direcciones en las que $f$ crece y direcciones en las que decrece, de modo que no puede ser máximo ni mínimo local.
:::

::: {.demostracion}
*Demostración.* Por la identidad $(\ast)$ de la Proposición 12.4, en la base propia $h^\top Hh=\sum_i\lambda_iy_i^2$ con $\|y\|=\|h\|$, de donde
$$\lambda_n\|h\|^2 \;\le\; h^\top Hh \;\le\; \lambda_1\|h\|^2 \qquad\text{para todo } h.$$

**(1)** Como $a$ es crítico, la Proposición 14.5 deja $f(a+h)-f(a) = \tfrac12h^\top Hh + r_2(h)$, y con la cota anterior
$$f(a+h)-f(a) \;\ge\; \tfrac{\lambda_n}{2}\|h\|^2 + r_2(h).$$
Como $r_2(h)/\|h\|^2\to0$ y $\lambda_n>0$, existe $\delta>0$ tal que $|r_2(h)| < \tfrac{\lambda_n}{4}\|h\|^2$ siempre que $0<\|h\|<\delta$. Para esos $h$,
$$f(a+h)-f(a) \;\ge\; \Big(\tfrac{\lambda_n}{2}-\tfrac{\lambda_n}{4}\Big)\|h\|^2 = \tfrac{\lambda_n}{4}\|h\|^2 \;>\;0,$$
así que $f$ vale estrictamente más en todo el entorno perforado: mínimo local estricto.

**(2)** Aplíquese (1) a $-f$, cuya hessiana es $-H$ y cuyos eigenvalores cambian de signo.

**(3)** Sean $q_+$ y $q_-$ eigenvectores unitarios de $\lambda_1>0$ y $\lambda_n<0$. Tomando $h=tq_+$ y usando $q_+^\top Hq_+=\lambda_1$,
$$\frac{f(a+tq_+)-f(a)}{t^2} = \frac{\lambda_1}{2} + \frac{r_2(tq_+)}{t^2} \;\longrightarrow\; \frac{\lambda_1}{2} > 0,$$
luego $f(a+tq_+)>f(a)$ para $t$ pequeño. El mismo cálculo con $q_-$ da límite $\lambda_n/2<0$ y $f(a+tq_-)<f(a)$. En cualquier entorno de $a$ hay entonces puntos por encima y puntos por debajo. [∎]{.fin-demo}
:::

Los tres casos dejan fuera uno: si el menor eigenvalor es exactamente cero, la forma cuadrática se anula en una dirección entera y el término de orden dos no alcanza para decidir. El Ejercicio 1 muestra los tres desenlaces posibles con la misma segunda derivada nula.

```{=html}
<div class="viz-critico">
  <div class="controles">
    <label>Punto crítico
      <select id="cr-p">
        <option value="0">(0, 0)</option>
        <option value="1" selected>(1, 1)</option>
        <option value="2">(−1, −1)</option>
      </select>
    </label>
    <label>Radio r <input id="cr-r" type="range" min="2" max="60" step="1" value="40"> <span id="cr-r-v">0,40</span></label>
  </div>
  <svg id="cr-svg" viewBox="0 0 620 320" role="img" aria-label="A la izquierda las curvas de nivel de x a la cuarta menos cuatro xy mas y a la cuarta, con sus tres puntos criticos y un circulo de radio r alrededor del elegido. A la derecha la diferencia de la funcion sobre ese circulo dividida entre r al cuadrado, comparada con el modelo cuadratico de Taylor."></svg>
  <p class="lectura" id="cr-lectura"></p>
</div>
<style>
.viz-critico { border: 1px solid var(--r-linea, #ddd); border-radius: 10px; padding: 1rem 1.1rem; margin: 1.7rem 0; }
.viz-critico .controles { display: flex; flex-wrap: wrap; gap: .45rem 1.1rem; align-items: center; margin-bottom: .85rem; font-size: .85rem; }
.viz-critico label { display: flex; align-items: center; gap: .4rem; }
.viz-critico input[type=range] { width: 118px; padding: 0; }
.viz-critico select { font-size: .85rem; padding: .1rem .25rem; }
.viz-critico svg { width: 100%; height: auto; display: block; }
.viz-critico .lectura { font-size: .85rem; margin-top: .75rem; min-height: 5.2em; line-height: 1.6; }
.viz-critico .lectura .fx { font-family: var(--bs-font-monospace, monospace); font-size: .95em; }
</style>
<script>
(function () {
  var C_POS = "#0F6E68", C_NEG = "#B45309", C_TINTA = "var(--r-tinta,#1f2430)";
  var svg = document.getElementById("cr-svg"), lec = document.getElementById("cr-lectura");
  var cp = document.getElementById("cr-p");
  var cr = document.getElementById("cr-r"), er = document.getElementById("cr-r-v");

  // Marco constante en los dos paneles.
  var LCX = 160, LCY = 160, LESC = 68, LR = 1.85;
  var RX0 = 350, RX1 = 600, RY0 = 292, RY1 = 28, VMAX = 12;
  var PUNTOS = [[0, 0], [1, 1], [-1, -1]];
  var NIVELES = [-1.5, -0.6, 0, 0.6, 2];

  function el(t, at, txt) {
    var e = document.createElementNS("http://www.w3.org/2000/svg", t);
    for (var k in at) e.setAttribute(k, at[k]);
    if (txt !== undefined) e.textContent = txt;
    return e;
  }
  function lx(x) { return LCX + x * LESC; }
  function ly(y) { return LCY - y * LESC; }
  function qx(t) { return RX0 + t / (2 * Math.PI) * (RX1 - RX0); }
  function qy(v) { return RY0 - (Math.max(-VMAX, Math.min(VMAX, v)) + VMAX) / (2 * VMAX) * (RY0 - RY1); }

  function f(x, y) { return x * x * x * x - 4 * x * y + y * y * y * y; }
  function hess(x, y) { return [12 * x * x, -4, 12 * y * y]; }   // [fxx, fxy, fyy]

  function dibujar() {
    while (svg.firstChild) svg.removeChild(svg.firstChild);
    var idx = +cp.value, r = (+cr.value) / 100;
    var p = PUNTOS[idx];
    er.textContent = r.toFixed(2).replace(".", ",");

    // --- izquierda: curvas de nivel por muestreo de rejilla ---
    svg.appendChild(el("rect", { x: lx(-LR), y: ly(LR), width: 2 * LR * LESC, height: 2 * LR * LESC, fill: "none", stroke: "var(--r-linea,#ddd)" }));
    var N = 120, i, j;
    for (var n = 0; n < NIVELES.length; n++) {
      var nivel = NIVELES[n];
      for (i = 0; i <= N; i++) {
        for (j = 0; j <= N; j++) {
          var x = -LR + 2 * LR * i / N, y = -LR + 2 * LR * j / N;
          if (Math.abs(f(x, y) - nivel) < 0.06) {
            svg.appendChild(el("rect", { x: lx(x).toFixed(1), y: ly(y).toFixed(1), width: 1.7, height: 1.7,
              fill: C_TINTA, "fill-opacity": 0.28 }));
          }
        }
      }
    }
    PUNTOS.forEach(function (q, k) {
      svg.appendChild(el("circle", { cx: lx(q[0]).toFixed(1), cy: ly(q[1]).toFixed(1), r: k === idx ? 5 : 3,
        fill: k === idx ? C_POS : "var(--r-tinta-suave,#888)" }));
    });
    svg.appendChild(el("circle", { cx: lx(p[0]).toFixed(1), cy: ly(p[1]).toFixed(1), r: (r * LESC).toFixed(1),
      fill: "none", stroke: C_NEG, "stroke-width": 1.5 }));
    svg.appendChild(el("text", { x: lx(-LR), y: 18, "font-size": 11, "font-weight": 600, fill: "var(--r-tinta,#222)" },
      "curvas de nivel de x⁴ − 4xy + y⁴"));

    // --- derecha: [f(p+rv) - f(p)] / r^2 frente al modelo cuadratico ---
    svg.appendChild(el("rect", { x: RX0, y: RY1, width: RX1 - RX0, height: RY0 - RY1, fill: "none", stroke: "var(--r-linea,#ddd)" }));
    svg.appendChild(el("line", { x1: RX0, y1: qy(0).toFixed(1), x2: RX1, y2: qy(0).toFixed(1), stroke: C_TINTA, "stroke-width": 1, "stroke-opacity": 0.45 }));
    var H = hess(p[0], p[1]), fp0 = f(p[0], p[1]);
    var dMed = "", dMod = "", arriba = false, abajo = false;
    for (i = 0; i <= 200; i++) {
      var t = 2 * Math.PI * i / 200, cx = Math.cos(t), cy = Math.sin(t);
      var medido = (f(p[0] + r * cx, p[1] + r * cy) - fp0) / (r * r);
      var modelo = 0.5 * (H[0] * cx * cx + 2 * H[1] * cx * cy + H[2] * cy * cy);
      if (medido > 0.02) arriba = true;
      if (medido < -0.02) abajo = true;
      dMed += (i ? " L" : "M") + qx(t).toFixed(1) + "," + qy(medido).toFixed(1);
      dMod += (i ? " L" : "M") + qx(t).toFixed(1) + "," + qy(modelo).toFixed(1);
    }
    svg.appendChild(el("path", { d: dMod, fill: "none", stroke: C_POS, "stroke-width": 1.4, "stroke-dasharray": "4 3" }));
    svg.appendChild(el("path", { d: dMed, fill: "none", stroke: C_NEG, "stroke-width": 2 }));
    svg.appendChild(el("text", { x: RX0, y: 18, "font-size": 11, "font-weight": 600, fill: "var(--r-tinta,#222)" },
      "[f(p+rv) − f(p)] / r²  según el ángulo de v"));
    svg.appendChild(el("text", { x: RX0 + 2, y: (qy(0) - 5).toFixed(1), "font-size": 9, fill: "var(--r-tinta-suave,#777)" }, "0"));

    var disc = H[0] * H[2] - H[1] * H[1], traza = H[0] + H[2];
    var raiz = Math.sqrt(Math.max(0, traza * traza / 4 - disc));
    var l1 = traza / 2 + raiz, l2 = traza / 2 - raiz;
    var tipo = (l2 > 1e-9) ? "mínimo local" : (l1 < -1e-9 ? "máximo local" : "punto silla");
    var obs = (arriba && abajo) ? "la curva cruza el cero: hay vecinos por encima y por debajo"
            : (arriba ? "la curva no baja del cero: todos los vecinos están por encima"
                      : "la curva no sube del cero: todos los vecinos están por debajo");
    lec.innerHTML = "<span class=\"fx\">p = (" + p[0] + ", " + p[1] + ")   ·   eigenvalores de H = [" +
      l2.toFixed(2) + ", " + l1.toFixed(2) + "]   ·   " + tipo + "</span><br>" +
      "La curva continua es la diferencia medida; la punteada, el modelo cuadrático ½·vᵀHv de la Proposición 14.5. " +
      "Al encoger r las dos se juntan, que es lo que dice el resto o(‖h‖²). Aquí " + obs + ".";
  }

  cp.addEventListener("change", dibujar);
  cr.addEventListener("input", dibujar);
  dibujar();
})();
</script>
```

```{pyodide}
import numpy as np

def f(p):
    x, y = p
    return x**4 - 4*x*y + y**4

def grad(p):
    x, y = p
    return np.array([4*x**3 - 4*y, 4*y**3 - 4*x])

def hess(p):
    x, y = p
    return np.array([[12*x**2, -4.0], [-4.0, 12*y**2]])

# grad = 0 exige y = x^3 y x = y^3, de donde x = x^9: hay tres puntos criticos
criticos = [np.array([0.0, 0.0]), np.array([1.0, 1.0]), np.array([-1.0, -1.0])]
print(f"{'punto':>13s}  {'grad nulo':>10s}  {'eigenvalores de H':>22s}  {'clasificacion':>14s}")
for p in criticos:
    lam = np.linalg.eigvalsh(hess(p))
    tipo = "minimo local" if lam.min() > 0 else ("maximo local" if lam.max() < 0 else "punto silla")
    print(f"  ({p[0]:4.1f},{p[1]:5.1f})  {str(bool(np.linalg.norm(grad(p)) < 1e-12)):>10s}  "
          f"[{lam[0]:8.4f}, {lam[1]:8.4f}]  {tipo:>14s}")
print()

rng = np.random.default_rng(140)
d = rng.normal(size=(4000, 2))
d /= np.linalg.norm(d, axis=1, keepdims=True)
for p in [criticos[0], criticos[1]]:
    dif = np.array([f(p + 1e-3*v) for v in d]) - f(p)
    print(f"  alrededor de ({p[0]:.0f},{p[1]:.0f}), a distancia 0.001: "
          f"{int((dif > 0).sum())} vecinos por encima, {int((dif < 0).sum())} por debajo")
```

La clasificación por eigenvalores y el conteo de vecinos dicen lo mismo. En la silla, de 4000 direcciones sorteadas 1966 suben y 2034 bajan; en el mínimo suben las 4000. La Proposición 14.8 convierte ese experimento en un teorema: no hace falta mirar vecinos, basta con el signo de dos números.

## Ejercicios

### Ejercicio 1, Cuando el segundo orden no decide

Si la hessiana es singular, la Proposición 14.8 no se aplica a ninguno de sus tres casos. Comprobar que $x^4$, $x^3$ y $-x^4$ tienen las tres la segunda derivada nula en el origen y sin embargo son, respectivamente, un mínimo, ni mínimo ni máximo, y un máximo. Demostrar además que si $f''(a)=0$ y $f'''(a)\ne0$ entonces $a$ no puede ser extremo local.

```{pyodide}
for nombre, fn in [("x^4", lambda x: x**4), ("x^3", lambda x: x**3), ("-x^4", lambda x: -x**4)]:
    h = 1e-3
    segunda = (fn(h) - 2*fn(0.0) + fn(-h)) / h**2
    der = [fn(-1e-3) - fn(0.0), fn(1e-3) - fn(0.0)]
    real = "minimo" if all(v > 0 for v in der) else ("maximo" if all(v < 0 for v in der) else "ni minimo ni maximo")
    print(f"f(x) = {nombre:5s}   |f''(0)| por debajo de 1e-5: {str(abs(segunda) < 1e-5):5s}"
          f"   en realidad es: {real}")
print()
print("con la segunda derivada nula el criterio no separa los tres casos")
```

### Ejercicio 2, La cuadrática cuya hessiana no se mueve

Para $f(x)=\tfrac12x^\top Ax - b^\top x$ con $A$ simétrica definida positiva, comprobar que $\nabla f(x)=Ax-b$ y que $H_f(x)=A$ en **todo** punto. Deducir que el único punto crítico resuelve $Ax=b$ y que es mínimo **global**, no solo local, porque el resto de Taylor es idénticamente cero.

```{pyodide}
import numpy as np

rng = np.random.default_rng(141)
M = rng.normal(size=(4, 4))
A = M @ M.T + 3*np.eye(4)          # simetrica definida positiva por construccion
b = rng.normal(size=4)

def f(x):
    return 0.5 * x @ A @ x - b @ x

x_est = np.linalg.solve(A, b)
print(f"A es definida positiva (menor eigenvalor > 0): {bool(np.linalg.eigvalsh(A).min() > 0)}")
print(f"en x* = A^-1 b el gradiente Ax - b se anula (norma < 1e-10): "
      f"{bool(np.linalg.norm(A @ x_est - b) < 1e-10)}")
print()

def hess_numerica(x, h=1e-4):
    n = len(x)
    Hn = np.zeros((n, n))
    for i in range(n):
        for j in range(n):
            ei, ej = np.zeros(n), np.zeros(n)
            ei[i], ej[j] = h, h
            Hn[i, j] = (f(x+ei+ej) - f(x+ei-ej) - f(x-ei+ej) + f(x-ei-ej)) / (4*h*h)
    return Hn

print("la Hessiana no depende del punto: es A en todos ellos")
for etiqueta, punto in [("origen", np.zeros(4)), ("unos", np.ones(4)), ("al azar", rng.normal(size=4))]:
    print(f"  en {etiqueta:8s}: Hessiana numerica = A (tolerancia 1e-4): "
          f"{bool(np.allclose(hess_numerica(punto), A, atol=1e-4))}")
print()

d = rng.normal(size=(2000, 4))
d /= np.linalg.norm(d, axis=1, keepdims=True)
for escala in [1e-2, 1.0, 50.0]:
    peor = min(f(x_est + escala*v) for v in d)
    print(f"  a distancia {escala:5.2f} de x*, el menor valor hallado supera a f(x*): "
          f"{bool(peor > f(x_est))}")
```

El mínimo sigue siéndolo a distancia $50$, no solo en un entorno: con $r_2\equiv0$ la desigualdad de la Proposición 14.8 vale para todo $h$, no solo para los pequeños. Esa es, en miniatura, la diferencia entre local y global que la lección 15 convierte en la definición de convexidad.

## Reto

En `proyectos/notebooks/F1-retos.ipynb`, sección **Mat 14**:

1. Escribir `hessiana(f, a, h=1e-4)` por diferencias finitas y comprobarla contra la hessiana analítica de tres funciones distintas, midiendo cómo empeora el error cuando $h$ baja de $10^{-6}$, el error de redondeo crece más rápido que el de truncamiento.
2. Tomar $f(x,y)=x^4-4xy+y^4$ del segundo visual y dibujar, para cada uno de los tres puntos críticos, el cociente $[f(p+rv)-f(p)]/r^2$ en función de $r$ para varias direcciones fijas. Comprobar que todas convergen a $\tfrac12v^\top Hv$ y estimar a qué $r$ deja de valer el modelo cuadrático.
3. Construir una función con hessiana semidefinida positiva y menor eigenvalor exactamente cero en un punto crítico, tal que el punto sea mínimo; y otra con la misma hessiana donde no lo sea. Anotar en `50-Errores/` por qué el criterio se queda mudo ahí.

## Del libro

*Mathematics for Machine Learning* trata este material al final de su capítulo 5: §5.7 *Higher-Order Derivatives* introduce la hessiana, y §5.8 *Linearization and Multivariate Taylor Series* la aproximación de Taylor en varias variables. El libro tiene copyright y no es descargable desde el entorno en que se escribió esta página, así que se cita número y título y no se le atribuye ninguna frase.

**Preguntas para leer §5.7 y §5.8 con lápiz:**

1. §5.8 escribe la serie de Taylor multivariante con tensores de orden creciente, de los que la hessiana es el de orden dos. Con la Definición 14.4, ¿cuántas entradas independientes tiene el término de orden tres de una función de $n$ variables, y por qué la simetría de la Proposición 14.3 se generaliza a él?
2. La linealización de §5.8 es exactamente $T_1$. ¿Qué hipótesis de la Proposición 14.5 se necesita para que el error sea $o(\|h\|^2)$ y no solo $o(\|h\|)$, y dónde se usa en la demostración?
3. §5.7 define la hessiana para funciones escalares. Para una función vectorial $f:\mathbb{R}^n\to\mathbb{R}^m$ la segunda derivada deja de ser una matriz. ¿Qué objeto es, y cuántos índices tiene?

## Para el Cerebro

Nota nueva en `10-Conceptos/hessiana.md`, enlazada a [[derivada-y-gradiente]], [[formas-cuadraticas]] y [[convexidad]]. Conviene rehacer a mano la demostración de la Proposición 14.5 con $\phi(t)=f(a+th)$: reduce todo el caso multivariante al Taylor de una variable, y es el mismo truco que reaparece en la lección 15 para la convexidad y en la 17 para el método de Newton.

```
¿Qué es la hessiana?::La matriz de todas las segundas derivadas parciales, H_f(a)
¿Cuándo es simétrica?::Cuando las segundas parciales cruzadas son continuas en un entorno del punto
¿Qué pasa si no lo son?::Puede fallar: en xy(x²−y²)/(x²+y²) las dos cruzadas valen −1 y +1 en el origen
¿Qué es T₂?::f(a) + ∇f(a)·h + ½ hᵀH h, el polinomio de Taylor de orden dos
¿Cuánto vale el error de T₂?::Es o(‖h‖²): dividido entre ‖h‖² tiende a cero
¿Cómo se demuestra Taylor multivariante?::Con φ(t)=f(a+th) se reduce al Taylor de una variable, vía regla de la cadena
¿Qué es un punto crítico?::Un punto donde el gradiente se anula
¿Por qué en un mínimo local el gradiente es cero?::Porque si no lo fuera, la dirección −∇f haría bajar a f (Prop 13.9)
¿Qué decide si un punto crítico es mínimo?::El signo de los eigenvalores de la hessiana
¿Definida positiva?::Mínimo local estricto; definida negativa, máximo; indefinida, punto silla
¿Y si el menor eigenvalor es cero?::El criterio no decide: x⁴, x³ y −x⁴ tienen todas segunda derivada nula en 0
¿Qué es un punto silla?::Un punto crítico con vecinos por encima y por debajo, en direcciones distintas
```

::: {#fuentes .fuentes}
### Fuentes

**Lo que esta página demuestra sola.** Las Proposiciones 14.3, 14.5, 14.7 y 14.8 se demuestran aquí a partir de la lección 13, la regla de la cadena y la dirección de máximo descenso, y de la lección 12, el criterio espectral. Se comprueban además numéricamente: la simetría de las cruzadas hasta la sexta cifra en un caso y su fallo por dos unidades enteras en el contraejemplo; que el error de $T_1$ es de orden $t^2$ y el de $T_2$ de orden $t^3$, con la primera columna convergiendo a $|u^\top Hu|/2$; la clasificación de los tres puntos críticos de $x^4-4xy+y^4$ contra el conteo de 4000 vecinos sorteados; los tres desenlaces con segunda derivada nula; y que la hessiana de una cuadrática es constante y su mínimo es global a cualquier distancia. Los dos visuales se contrastaron antes de publicarse contra las fórmulas en todo el rango de sus controles.

**Lo que se usa de otras lecciones sin repetir.** La regla de la cadena es la Proposición 13.3 y la fórmula $D_vf(a)=\nabla f(a)\cdot v$ es la 13.8; la dirección de máximo descenso, la 13.9. La identidad $(\ast)$ que acota $h^\top Hh$ entre $\lambda_n\|h\|^2$ y $\lambda_1\|h\|^2$ es la Proposición 12.4. Que una matriz simétrica tiene eigenvalores reales y base ortonormal de eigenvectores es la lección 10.

**Lo que se enuncia sin demostrar.** El teorema del valor medio, usado dos veces en la Proposición 14.3, y el desarrollo de Taylor de una variable con resto de Lagrange, usado en la 14.5: los dos son resultados de cálculo de una variable que se dan por conocidos. En la 14.5 se acota la forma cuadrática por la magnitud de las entradas de la matriz sin precisar qué norma matricial se usa; cualquiera de las equivalentes sirve, y la conclusión no depende de la elección.

**Lo que viene de los libros.**

- [*Mathematics for Machine Learning*](https://mml-book.github.io/book/mml-book.pdf) (Deisenroth, Faisal & Ong, Cambridge University Press, 2020), §5.7 *Higher-Order Derivatives* y §5.8 *Linearization and Multivariate Taylor Series* — citadas por número y título, sin transcribir texto.

**Lo que es mío, no del libro.** La demostración de la Proposición 14.3 por doble incremento, escrita para que la hipótesis de continuidad se vea usada en el último paso y no antes. El contraejemplo medido en la primera celda, elegido para que el fallo se note en unidades enteras y no en decimales. Y el segundo visual, que dibuja $[f(p+rv)-f(p)]/r^2$ en lugar de la diferencia cruda: así el modelo cuadrático queda fijo mientras $r$ cambia, y se ve converger lo que la Proposición 14.5 afirma.

[Índices verificados el 12-09-2026 contra el índice publicado del PDF oficial.]{.sello}
:::

→ Siguiente: [Convexidad y por qué importa](15-convexidad.qmd)
