---
title: "Convexidad y por qué importa"
subtitle: "Matemática · Lección 15"
nivel: L2
modulo: matematica
fase: F1
prereqs: [matematica/14-hessiana-taylor, matematica/12-formas-cuadraticas]
siguientes: [matematica/16-descenso-de-gradiente]
libro: "MML §7.3 Convex Optimization"
engine: markdown
pyodide:
  packages:
    - numpy
---

::: {.meta-leccion data-leccion="matematica/15-convexidad"}
[[L2]{.nivel .nivel-L2} **Derivación**]{.item}
[**Fase** F1]{.item}
[**Libro** MML §7.3]{.item}
[**Prereqs** [Hessiana y Taylor](14-hessiana-taylor.qmd) · [Formas cuadráticas](12-formas-cuadraticas.qmd)]{.item}
:::

## Objetivo

Al terminar esta lección se puede:

1. Definir [conjunto convexo]{.g} y [función convexa]{.g}, y reconocer cuál de las dos condiciones falla en un caso concreto.
2. Demostrar la caracterización de primer orden: una función convexa diferenciable queda por encima de todos sus planos tangentes.
3. Demostrar la caracterización de segundo orden: convexidad equivale a que la hessiana sea semidefinida positiva en todo punto.
4. Demostrar que en una función convexa todo mínimo local es [global]{.g data-t="mínimo global"}, y que si es [estrictamente convexa]{.g} el minimizador es único.

:::: {.hilo}
::: {}
#### De dónde viene

- **Matemática 14**: la hessiana y el desarrollo de Taylor con resto. La caracterización de segundo orden es ese desarrollo leído al revés.
- **Matemática 12**: el criterio espectral. «Hessiana semidefinida positiva» quiere decir que todos sus eigenvalores son mayores o iguales que cero.
- **Matemática 13**: la derivada direccional como límite del cociente de incrementos, que es lo que hace pasar de la desigualdad de la cuerda a la del plano tangente.
:::

::: {}
#### Para qué sirve después

- **Matemática 16 y 17**: el descenso de gradiente y el método de Newton encuentran mínimos locales. La Proposición 15.5 es lo que convierte ese resultado en la respuesta buscada.
- **Matemática 18**: la teoría de multiplicadores de Lagrange y las condiciones KKT se vuelven suficientes, y no solo necesarias, cuando el problema es convexo.
- **Estadística 8 y 13**: la log-verosimilitud de la normal y la suma de cuadrados de la regresión son cóncava y convexa respectivamente, y por eso sus puntos críticos son el óptimo y no un óptimo cualquiera.
:::
::::

## Notación

::: {.notacion}
| Símbolo | Se lee | Significado |
|---|---|---|
| $C$ | ce | Un conjunto convexo, dominio de $f$ |
| $t$ | te | El peso de la combinación, $t\in[0,1]$ |
| $tx+(1-t)y$ | combinación convexa | Un punto del segmento entre $x$ e $y$ |
| $H_f(x)\succeq0$ | hache semidefinida positiva | $v^\top H_f(x)v\ge0$ para todo $v$ |
| $\nabla f(x)\cdot(y-x)$ | — | El término lineal del plano tangente en $x$, evaluado en $y$ |
| $x^\star$ | equis estrella | Un minimizador de $f$ |
:::

## 1. Las dos definiciones

::: {.definicion}
**Definición 15.1 (conjunto convexo).** Un conjunto $C\subseteq\mathbb{R}^n$ es **convexo** si para todos $x,y\in C$ y todo $t\in[0,1]$ se cumple $tx+(1-t)y\in C$: el segmento que une dos puntos del conjunto se queda dentro.
:::

::: {.definicion}
**Definición 15.2 (función convexa).** Una función $f$ definida en un conjunto convexo $C$ es **convexa** si para todos $x,y\in C$ y todo $t\in[0,1]$,
$$f\big(tx+(1-t)y\big) \;\le\; t\,f(x) + (1-t)\,f(y).$$
Es **estrictamente convexa** si la desigualdad es estricta siempre que $x\ne y$ y $t\in(0,1)$. Y es **cóncava** si $-f$ es convexa.
:::

La desigualdad dice que la gráfica queda por debajo de cualquiera de sus cuerdas: el lado izquierdo es la función en un punto del segmento y el derecho es la cuerda sobre ese mismo punto. Sin derivadas de por medio, la definición se puede comprobar sorteando pares de puntos.

```{pyodide}
import numpy as np

def convexa(p):
    x, y = p[..., 0], p[..., 1]
    return np.exp(x) + x*x + y*y

def no_convexa(p):
    x, y = p[..., 0], p[..., 1]
    return x**4 - 4*x*y + y**4          # la de la leccion 14: tiene una silla

rng = np.random.default_rng(150)
X = rng.uniform(-1.5, 1.5, size=(20000, 2))
Y = rng.uniform(-1.5, 1.5, size=(20000, 2))
t = rng.uniform(0, 1, size=(20000, 1))
M = t*X + (1-t)*Y

for nombre, f in [("e^x + x^2 + y^2", convexa), ("x^4 - 4xy + y^4", no_convexa)]:
    izq = f(M)
    der = t[:, 0]*f(X) + (1-t[:, 0])*f(Y)
    fallos = int((izq > der + 1e-9).sum())
    peor = float((der - izq).min())
    print(f"{nombre:18s}  pares que violan Jensen: {fallos:5d} de 20000   "
          f"peor violacion: {(0.0 if peor > -1e-9 else peor):8.4f}")
print()
print("la primera nunca se sale de la cuerda; la segunda si, y por eso no es convexa")
```

Veinte mil pares no demuestran nada, pero uno solo sí refuta: los $2712$ casos de la segunda función bastan para descartarla. La convexidad se demuestra con las proposiciones que siguen; se **refuta** con un contraejemplo.

## 2. La función queda por encima de sus tangentes

::: {.proposicion}
**Proposición 15.3 (caracterización de primer orden).** Sea $f$ diferenciable en un conjunto convexo abierto $C$. Entonces $f$ es convexa si y solo si
$$f(y) \;\ge\; f(x) + \nabla f(x)\cdot(y-x) \qquad\text{para todos } x,y\in C.$$
:::

::: {.demostracion}
*Demostración.* **($\Rightarrow$)** Sea $f$ convexa y $t\in(0,1]$. Escribiendo $x+t(y-x) = (1-t)x+ty$, la Definición 15.2 da
$$f\big(x+t(y-x)\big) \le (1-t)f(x)+t\,f(y),$$
y restando $f(x)$ y dividiendo entre $t>0$,
$$\frac{f\big(x+t(y-x)\big)-f(x)}{t} \;\le\; f(y)-f(x).$$
El lado izquierdo es el cociente de incrementos en la dirección $v=y-x$. El mismo cálculo de la Proposición 13.8 —tomando allí $h=tv$— muestra que tiende a $\nabla f(x)\cdot v$ cuando $t\to0^+$, sin que haga falta que $v$ sea unitario. Pasando al límite,
$$\nabla f(x)\cdot(y-x) \le f(y)-f(x),$$
que es la desigualdad buscada.

**($\Leftarrow$)** Supóngase la desigualdad para todos los pares, y sean $x,y\in C$, $t\in[0,1]$ y $z=tx+(1-t)y$, que está en $C$ por la Definición 15.1. Aplicándola dos veces desde $z$,
$$f(x)\ge f(z)+\nabla f(z)\cdot(x-z), \qquad f(y)\ge f(z)+\nabla f(z)\cdot(y-z).$$
Multiplicando la primera por $t\ge0$, la segunda por $1-t\ge0$ y sumando,
$$t\,f(x)+(1-t)f(y) \;\ge\; f(z) + \nabla f(z)\cdot\big[t(x-z)+(1-t)(y-z)\big].$$
El corchete vale $tx+(1-t)y-z = 0$ por la definición de $z$, así que el término lineal desaparece y queda $t\,f(x)+(1-t)f(y)\ge f(z)$, que es la Definición 15.2. [∎]{.fin-demo}
:::

Esa equivalencia es la que hace útil la convexidad: el plano tangente deja de ser una aproximación local y pasa a ser una **cota inferior válida en todo el dominio**. Sabiendo el valor y el gradiente en un punto, se sabe algo cierto sobre todos los demás.

```{=html}
<div class="viz-convexa">
  <div class="controles">
    <label>Función
      <select id="cv-f">
        <option value="0" selected>convexa: x²/2 + eˣ/3</option>
        <option value="1">no convexa: x⁴ − 3x² + x</option>
      </select>
    </label>
    <label>x <input id="cv-x" type="range" min="-20" max="20" step="1" value="-11"> <span id="cv-x-v">−1,1</span></label>
    <label>y <input id="cv-y" type="range" min="-20" max="20" step="1" value="13"> <span id="cv-y-v">1,3</span></label>
  </div>
  <svg id="cv-svg" viewBox="0 0 620 320" role="img" aria-label="Una curva con la cuerda entre dos puntos elegidos y la recta tangente en el primero. En la funcion convexa la cuerda queda por encima de la curva y la tangente por debajo; en la no convexa las dos condiciones fallan en algun tramo."></svg>
  <p class="lectura" id="cv-lectura"></p>
</div>
<style>
.viz-convexa { border: 1px solid var(--r-linea, #ddd); border-radius: 10px; padding: 1rem 1.1rem; margin: 1.7rem 0; }
.viz-convexa .controles { display: flex; flex-wrap: wrap; gap: .45rem 1.1rem; align-items: center; margin-bottom: .85rem; font-size: .85rem; }
.viz-convexa label { display: flex; align-items: center; gap: .4rem; }
.viz-convexa input[type=range] { width: 110px; padding: 0; }
.viz-convexa select { font-size: .85rem; padding: .1rem .25rem; }
.viz-convexa svg { width: 100%; height: auto; display: block; }
.viz-convexa .lectura { font-size: .85rem; margin-top: .75rem; min-height: 5em; line-height: 1.6; }
.viz-convexa .lectura .fx { font-family: var(--bs-font-monospace, monospace); font-size: .95em; }
</style>
<script>
(function () {
  var C_F = "var(--r-tinta,#1f2430)", C_CUERDA = "#B45309", C_TAN = "#0F6E68";
  var svg = document.getElementById("cv-svg"), lec = document.getElementById("cv-lectura");
  var cf = document.getElementById("cv-f");
  var cx = document.getElementById("cv-x"), ex = document.getElementById("cv-x-v");
  var cy = document.getElementById("cv-y"), ey = document.getElementById("cv-y-v");

  // Marco constante: no depende de la funcion ni de los puntos elegidos.
  var X0 = 40, X1 = 600, Y0 = 292, Y1 = 26, RX = 2.1, YMIN = -3.2, YMAX = 4.2;

  function el(t, at, txt) {
    var e = document.createElementNS("http://www.w3.org/2000/svg", t);
    for (var k in at) e.setAttribute(k, at[k]);
    if (txt !== undefined) e.textContent = txt;
    return e;
  }
  function px(x) { return X0 + (x + RX) / (2 * RX) * (X1 - X0); }
  function py(v) { return Y0 - (Math.max(YMIN, Math.min(YMAX, v)) - YMIN) / (YMAX - YMIN) * (Y0 - Y1); }

  var FS = [
    { f: function (x) { return 0.5 * x * x + Math.exp(x) / 3; },
      d: function (x) { return x + Math.exp(x) / 3; } },
    { f: function (x) { return x * x * x * x - 3 * x * x + x; },
      d: function (x) { return 4 * x * x * x - 6 * x + 1; } }
  ];

  function dibujar() {
    while (svg.firstChild) svg.removeChild(svg.firstChild);
    var F = FS[+cf.value], xa = (+cx.value) / 10, xb = (+cy.value) / 10;
    ex.textContent = xa.toFixed(1).replace(".", ",").replace("-", "−");
    ey.textContent = xb.toFixed(1).replace(".", ",").replace("-", "−");

    svg.appendChild(el("rect", { x: X0, y: Y1, width: X1 - X0, height: Y0 - Y1, fill: "none", stroke: "var(--r-linea,#ddd)" }));
    svg.appendChild(el("line", { x1: X0, y1: py(0), x2: X1, y2: py(0), stroke: "var(--r-linea,#eee)" }));

    var i, x, d = "";
    for (i = 0; i <= 280; i++) {
      x = -RX + 2 * RX * i / 280;
      d += (i ? " L" : "M") + px(x).toFixed(1) + "," + py(F.f(x)).toFixed(1);
    }
    svg.appendChild(el("path", { d: d, fill: "none", stroke: C_F, "stroke-width": 1.9 }));

    // cuerda entre (xa, f(xa)) y (xb, f(xb))
    var fa = F.f(xa), fb = F.f(xb);
    svg.appendChild(el("line", { x1: px(xa).toFixed(1), y1: py(fa).toFixed(1), x2: px(xb).toFixed(1), y2: py(fb).toFixed(1),
      stroke: C_CUERDA, "stroke-width": 2 }));

    // tangente en xa, sobre todo el ancho
    var da = F.d(xa), dt = "";
    for (i = 0; i <= 280; i++) {
      x = -RX + 2 * RX * i / 280;
      dt += (i ? " L" : "M") + px(x).toFixed(1) + "," + py(fa + da * (x - xa)).toFixed(1);
    }
    svg.appendChild(el("path", { d: dt, fill: "none", stroke: C_TAN, "stroke-width": 1.5, "stroke-dasharray": "6 4" }));

    svg.appendChild(el("circle", { cx: px(xa).toFixed(1), cy: py(fa).toFixed(1), r: 4, fill: C_CUERDA }));
    svg.appendChild(el("circle", { cx: px(xb).toFixed(1), cy: py(fb).toFixed(1), r: 4, fill: C_CUERDA }));

    // ¿se cumplen las dos condiciones?
    var peorCuerda = 0, peorTangente = 0, lo = Math.min(xa, xb), hi = Math.max(xa, xb);
    for (i = 0; i <= 300; i++) {
      x = -RX + 2 * RX * i / 300;
      var tang = fa + da * (x - xa);
      peorTangente = Math.min(peorTangente, F.f(x) - tang);
      if (x >= lo && x <= hi && hi > lo) {
        var s = (x - lo) / (hi - lo);
        var cuerda = (lo === xa ? fa + s * (fb - fa) : fb + s * (fa - fb));
        peorCuerda = Math.min(peorCuerda, cuerda - F.f(x));
      }
    }
    svg.appendChild(el("text", { x: X0 + 8, y: 44, "font-size": 10, fill: C_CUERDA }, "— la cuerda entre los dos puntos"));
    svg.appendChild(el("text", { x: X0 + 8, y: 58, "font-size": 10, fill: C_TAN }, "— — la tangente en x"));

    var okC = peorCuerda > -1e-9, okT = peorTangente > -1e-9;
    lec.innerHTML = "<span class=\"fx\">cuerda por encima de la curva: " + (okC ? "sí" : "NO") +
      "   ·   curva por encima de la tangente: " + (okT ? "sí" : "NO") + "</span><br>" +
      (okC && okT
        ? "Las dos condiciones se cumplen, y son la Definición 15.2 y la Proposición 15.3 respectivamente: la misma propiedad vista desde la cuerda y desde el plano tangente."
        : "Al menos una falla. Basta un par de puntos donde la cuerda se hunda por debajo de la curva —o un tramo donde la curva caiga por debajo de su propia tangente— para que la función no sea convexa.");
  }

  cf.addEventListener("change", dibujar);
  cx.addEventListener("input", dibujar);
  cy.addEventListener("input", dibujar);
  dibujar();
})();
</script>
```

## 3. El criterio que se puede calcular

::: {.proposicion}
**Proposición 15.4 (caracterización de segundo orden).** Sea $f$ con segundas parciales continuas en un conjunto convexo abierto $C$. Entonces $f$ es convexa si y solo si $H_f(x)$ es semidefinida positiva para todo $x\in C$.
:::

::: {.demostracion}
*Demostración.* La demostración de la Proposición 14.5 da, para $x,y\in C$ y algún $\xi$ del segmento que los une —que está en $C$ porque $C$ es convexo—,
$$f(y) = f(x) + \nabla f(x)\cdot(y-x) + \tfrac12\,(y-x)^\top H_f(\xi)\,(y-x). \tag{$\dagger$}$$

**($\Leftarrow$)** Si toda hessiana es semidefinida positiva, el último término de $(\dagger)$ es mayor o igual que cero, luego $f(y)\ge f(x)+\nabla f(x)\cdot(y-x)$ para todos $x,y$, y la Proposición 15.3 da la convexidad.

**($\Rightarrow$)** Por contrarrecíproco. Supóngase que existen $x_0\in C$ y $v$ con $v^\top H_f(x_0)v<0$. Como las segundas parciales son continuas, la función $\xi\mapsto v^\top H_f(\xi)v$ también lo es, así que sigue siendo negativa en toda una bola alrededor de $x_0$. Tomando $y=x_0+tv$ con $t>0$ lo bastante pequeño para que el segmento entero quede en esa bola, $(\dagger)$ da
$$f(y) = f(x_0) + \nabla f(x_0)\cdot(y-x_0) + \tfrac{t^2}{2}\,v^\top H_f(\xi)\,v \;<\; f(x_0)+\nabla f(x_0)\cdot(y-x_0),$$
que contradice la Proposición 15.3. Luego $f$ no es convexa. [∎]{.fin-demo}
:::

Por el criterio espectral de la Proposición 12.4, «semidefinida positiva» quiere decir que el menor eigenvalor no es negativo. Eso convierte la convexidad en algo que una máquina puede comprobar punto por punto.

```{pyodide}
import numpy as np

def hess_convexa(x, y):
    return np.array([[np.exp(x) + 2.0, 0.0], [0.0, 2.0]])

def hess_no_convexa(x, y):
    return np.array([[12*x*x, -4.0], [-4.0, 12*y*y]])

rng = np.random.default_rng(151)
puntos = rng.uniform(-1.5, 1.5, size=(4000, 2))
for nombre, H in [("e^x + x^2 + y^2", hess_convexa), ("x^4 - 4xy + y^4", hess_no_convexa)]:
    menores = np.array([np.linalg.eigvalsh(H(p[0], p[1])).min() for p in puntos])
    print(f"{nombre:18s}  menor eigenvalor de H sobre 4000 puntos: "
          f"minimo {menores.min():8.4f}   negativos: {int((menores < 0).sum()):4d}")
print()
print("semidefinida positiva en todas partes equivale a convexa (Proposicion 15.4)")
```

En la primera función el menor eigenvalor vale $2$ en todo punto —la hessiana es $\operatorname{diag}(e^x+2,\,2)$— y nunca baja de ahí. En la segunda, $1709$ de los $4000$ puntos tienen un eigenvalor negativo, y uno solo ya habría bastado.

## 4. Por qué importa

::: {.proposicion}
**Proposición 15.5 (todo mínimo local es global).** Sea $f$ convexa en un conjunto convexo $C$. Si $a\in C$ es un mínimo local de $f$, entonces $f(a)\le f(y)$ para **todo** $y\in C$.
:::

::: {.demostracion}
*Demostración.* Sea $y\in C$ cualquiera y $t\in(0,1]$. El punto $(1-t)a+ty$ está en $C$ y, cuando $t\to0^+$, tiende a $a$; así que para $t$ suficientemente pequeño cae dentro del entorno donde $a$ es mínimo local, y por tanto
$$f(a) \;\le\; f\big((1-t)a+ty\big).$$
Acotando el lado derecho con la Definición 15.2,
$$f(a) \;\le\; (1-t)f(a) + t\,f(y).$$
Restando $(1-t)f(a)$ queda $t\,f(a)\le t\,f(y)$, y dividiendo entre $t>0$, $f(a)\le f(y)$. Como $y$ era arbitrario, $a$ es mínimo global. [∎]{.fin-demo}
:::

La demostración no usa derivadas en ningún paso: la convexidad sola ya prohíbe que exista un valle separado del fondo verdadero. Esa es la razón de fondo por la que se persigue la convexidad al plantear un problema.

::: {.proposicion}
**Proposición 15.6 (unicidad del minimizador).** Si $f$ es estrictamente convexa en $C$ convexo, entonces tiene a lo sumo un minimizador.
:::

::: {.demostracion}
*Demostración.* Supóngase que $a\ne b$ son dos minimizadores, con $f(a)=f(b)=m$. El punto medio $\tfrac12a+\tfrac12b$ está en $C$, y por convexidad estricta —con $t=\tfrac12$ y $a\ne b$—,
$$f\big(\tfrac12a+\tfrac12b\big) \;<\; \tfrac12 f(a)+\tfrac12 f(b) = m,$$
lo que contradice que $m$ sea el valor mínimo. Luego no puede haber dos. [∎]{.fin-demo}
:::

```{pyodide}
import numpy as np

def grad_convexa(P):
    return np.column_stack([np.exp(P[:, 0]) + 2*P[:, 0], 2*P[:, 1]])

def grad_no_convexa(P):
    return np.column_stack([4*P[:, 0]**3 - 4*P[:, 1], 4*P[:, 1]**3 - 4*P[:, 0]])

def descender(grad, P0, pasos=4000, eta=0.02):
    P = P0.copy()
    for _ in range(pasos):
        P = P - eta*grad(P)
    return P

rng = np.random.default_rng(152)
arranques = rng.uniform(-1.4, 1.4, size=(40, 2))
for nombre, g in [("e^x + x^2 + y^2", grad_convexa), ("x^4 - 4xy + y^4", grad_no_convexa)]:
    finales = np.round(descender(g, arranques), 3)
    finales[finales == 0] = 0.0          # -0.0 y 0.0 son el mismo punto
    destinos = sorted({tuple(q) for q in finales})
    print(f"{nombre:18s}  40 arranques al azar  ->  {len(destinos)} destino(s) distinto(s)")
    for d in destinos:
        print(f"      ({d[0]:6.3f}, {d[1]:6.3f})")
print()
print("en la convexa todos los caminos terminan en el mismo punto: el minimo local es global")
```

Cuarenta puntos de partida al azar y un solo destino en la función convexa; dos destinos distintos en la que no lo es. Eso es la Proposición 15.5 en la práctica: con convexidad, dónde se empiece a buscar deja de importar.

```{=html}
<div class="viz-caminos">
  <div class="controles">
    <label>Función
      <select id="cm-f">
        <option value="0" selected>convexa: eˣ + x² + y²</option>
        <option value="1">no convexa: x⁴ − 4xy + y⁴</option>
      </select>
    </label>
    <label>Pasos <input id="cm-n" type="range" min="0" max="240" step="10" value="120"> <span id="cm-n-v">120</span></label>
  </div>
  <svg id="cm-svg" viewBox="0 0 620 320" role="img" aria-label="Curvas de nivel de la funcion elegida con doce caminos de descenso que arrancan repartidos por el borde. En la funcion convexa los doce convergen al mismo punto; en la no convexa se reparten entre dos minimos distintos."></svg>
  <p class="lectura" id="cm-lectura"></p>
</div>
<style>
.viz-caminos { border: 1px solid var(--r-linea, #ddd); border-radius: 10px; padding: 1rem 1.1rem; margin: 1.7rem 0; }
.viz-caminos .controles { display: flex; flex-wrap: wrap; gap: .45rem 1.1rem; align-items: center; margin-bottom: .85rem; font-size: .85rem; }
.viz-caminos label { display: flex; align-items: center; gap: .4rem; }
.viz-caminos input[type=range] { width: 130px; padding: 0; }
.viz-caminos select { font-size: .85rem; padding: .1rem .25rem; }
.viz-caminos svg { width: 100%; height: auto; display: block; }
.viz-caminos .lectura { font-size: .85rem; margin-top: .75rem; min-height: 5em; line-height: 1.6; }
.viz-caminos .lectura .fx { font-family: var(--bs-font-monospace, monospace); font-size: .95em; }
</style>
<script>
(function () {
  var C_NIVEL = "var(--r-tinta,#1f2430)", C_CAM = "#B45309", C_FIN = "#0F6E68";
  var svg = document.getElementById("cm-svg"), lec = document.getElementById("cm-lectura");
  var cf = document.getElementById("cm-f");
  var cn = document.getElementById("cm-n"), en = document.getElementById("cm-n-v");

  // Marco constante en los dos paneles y para las dos funciones.
  var LCX = 165, LCY = 160, LESC = 78, LR = 1.6;
  var RX0 = 360, RX1 = 600, RY0 = 288, RY1 = 30;

  function el(t, at, txt) {
    var e = document.createElementNS("http://www.w3.org/2000/svg", t);
    for (var k in at) e.setAttribute(k, at[k]);
    if (txt !== undefined) e.textContent = txt;
    return e;
  }
  function lx(x) { return LCX + x * LESC; }
  function ly(y) { return LCY - y * LESC; }

  var FS = [
    { f: function (x, y) { return Math.exp(x) + x*x + y*y; },
      g: function (x, y) { return [Math.exp(x) + 2*x, 2*y]; },
      niveles: [1.2, 2, 3.2, 5], eta: 0.05, vmin: 0.6, vmax: 6 },
    { f: function (x, y) { return x*x*x*x - 4*x*y + y*y*y*y; },
      g: function (x, y) { return [4*x*x*x - 4*y, 4*y*y*y - 4*x]; },
      niveles: [-1.5, -0.6, 0, 1], eta: 0.03, vmin: -2.4, vmax: 2 }
  ];

  function dibujar() {
    while (svg.firstChild) svg.removeChild(svg.firstChild);
    var F = FS[+cf.value], pasos = +cn.value;
    en.textContent = pasos;

    svg.appendChild(el("rect", { x: lx(-LR), y: ly(LR), width: 2*LR*LESC, height: 2*LR*LESC, fill: "none", stroke: "var(--r-linea,#ddd)" }));
    var N = 110, i, j, k;
    for (k = 0; k < F.niveles.length; k++) {
      for (i = 0; i <= N; i++) {
        for (j = 0; j <= N; j++) {
          var x = -LR + 2*LR*i/N, y = -LR + 2*LR*j/N;
          if (Math.abs(F.f(x, y) - F.niveles[k]) < 0.07) {
            svg.appendChild(el("rect", { x: lx(x).toFixed(1), y: ly(y).toFixed(1), width: 1.7, height: 1.7,
              fill: C_NIVEL, "fill-opacity": 0.26 }));
          }
        }
      }
    }

    // doce arranques repartidos por una circunferencia fija
    var finales = [], valores = [];
    for (k = 0; k < 12; k++) {
      var ang = 2*Math.PI*k/12, p = [1.45*Math.cos(ang), 1.45*Math.sin(ang)];
      var d = "M" + lx(p[0]).toFixed(1) + "," + ly(p[1]).toFixed(1);
      for (i = 0; i < pasos; i++) {
        var gr = F.g(p[0], p[1]);
        p = [p[0] - F.eta*gr[0], p[1] - F.eta*gr[1]];
        d += " L" + lx(p[0]).toFixed(1) + "," + ly(p[1]).toFixed(1);
      }
      svg.appendChild(el("path", { d: d, fill: "none", stroke: C_CAM, "stroke-width": 1.2, "stroke-opacity": 0.75 }));
      svg.appendChild(el("circle", { cx: lx(p[0]).toFixed(1), cy: ly(p[1]).toFixed(1), r: 3.2, fill: C_FIN }));
      finales.push(p);
      valores.push(F.f(p[0], p[1]));
    }
    svg.appendChild(el("text", { x: lx(-LR), y: 18, "font-size": 11, "font-weight": 600, fill: "var(--r-tinta,#222)" },
      "curvas de nivel y doce caminos de descenso"));

    // panel derecho: el valor final de cada camino
    svg.appendChild(el("rect", { x: RX0, y: RY1, width: RX1 - RX0, height: RY0 - RY1, fill: "none", stroke: "var(--r-linea,#ddd)" }));
    function vy(v) { return RY0 - (Math.max(F.vmin, Math.min(F.vmax, v)) - F.vmin) / (F.vmax - F.vmin) * (RY0 - RY1); }
    for (k = 0; k < 12; k++) {
      var cxp = RX0 + (k + 0.5) / 12 * (RX1 - RX0);
      svg.appendChild(el("line", { x1: cxp.toFixed(1), y1: RY0, x2: cxp.toFixed(1), y2: vy(valores[k]).toFixed(1),
        stroke: C_CAM, "stroke-width": 1, "stroke-opacity": 0.5 }));
      svg.appendChild(el("circle", { cx: cxp.toFixed(1), cy: vy(valores[k]).toFixed(1), r: 3, fill: C_FIN }));
    }
    svg.appendChild(el("text", { x: RX0, y: 18, "font-size": 11, "font-weight": 600, fill: "var(--r-tinta,#222)" },
      "valor alcanzado por cada camino"));

    var lo = Math.min.apply(null, valores), hi = Math.max.apply(null, valores);
    var iguales = (hi - lo) < 1e-3;
    lec.innerHTML = "<span class=\"fx\">pasos = " + pasos + "   ·   peor valor − mejor valor = " +
      (hi - lo).toFixed(4).replace(".", ",") + "</span><br>" +
      (iguales
        ? "Los doce caminos llegan al mismo valor. Con la función convexa eso está garantizado por la Proposición 15.5: no hay ningún valle donde quedarse atrapado."
        : "Los caminos terminan en valores distintos: hay más de un mínimo local y el punto de partida decide a cuál se llega. La Proposición 15.5 no se aplica porque la función no es convexa.");
  }

  cf.addEventListener("change", dibujar);
  cn.addEventListener("input", dibujar);
  dibujar();
})();
</script>
```

## Ejercicios

### Ejercicio 1 — Qué operaciones conservan la convexidad

Demostrar, directamente desde la Definición 15.2, que si $f$ y $g$ son convexas entonces también lo son $f+g$, $\alpha f$ con $\alpha\ge0$, y $\max\{f,g\}$. Comprobar con un contraejemplo que el producto $fg$ **no** la conserva.

```{pyodide}
import numpy as np

rng = np.random.default_rng(153)
x = rng.uniform(-2, 2, size=(30000,))
y = rng.uniform(-2, 2, size=(30000,))
t = rng.uniform(0, 1, size=(30000,))
m = t*x + (1-t)*y

def jensen(f):
    return int((f(m) > t*f(x) + (1-t)*f(y) + 1e-9).sum())

f = lambda z: z**2
g = lambda z: np.exp(z)
casos = [("f = x^2", f), ("g = e^x", g), ("f + g", lambda z: f(z) + g(z)),
         ("3f", lambda z: 3*f(z)), ("max(f, g)", lambda z: np.maximum(f(z), g(z))),
         ("f * g", lambda z: f(z) * g(z))]
for nombre, h in casos:
    print(f"{nombre:12s}  pares que violan Jensen: {jensen(h):6d} de 30000")
print()
print("el producto de dos convexas puede dejar de serlo: x^2 e^x se hunde bajo su cuerda")
```

### Ejercicio 2 — La suma de cuadrados es convexa

Demostrar que $S(b)=\|y-Xb\|^2$ es convexa en $b$ calculando su hessiana, y que es **estrictamente** convexa si y solo si $X$ tiene rango columna completo. Relacionarlo con la unicidad de la solución de mínimos cuadrados de la lección 6.

```{pyodide}
import numpy as np

rng = np.random.default_rng(154)
X = rng.normal(size=(30, 3))
y = rng.normal(size=30)

# S(b) = ||y - Xb||^2  ->  grad = -2 X^T (y - Xb),  H = 2 X^T X, constante
H = 2 * X.T @ X
print(f"eigenvalores de la hessiana 2 X^T X, todos positivos: "
      f"{bool(np.linalg.eigvalsh(H).min() > 1e-9)}")
print(f"rango columna de X completo (3 de 3): {int(np.linalg.matrix_rank(X))}")
print()

# con una columna dependiente el rango cae y la hessiana deja de ser definida
Xd = np.column_stack([X[:, 0], X[:, 1], X[:, 0] + X[:, 1]])
Hd = 2 * Xd.T @ Xd
print(f"con una columna dependiente, rango de X: {int(np.linalg.matrix_rank(Xd))}")
print(f"  menor eigenvalor de la hessiana por debajo de 1e-9: "
      f"{bool(np.linalg.eigvalsh(Hd).min() < 1e-9)}")
print(f"  sigue siendo semidefinida positiva (por encima de -1e-9): "
      f"{bool(np.linalg.eigvalsh(Hd).min() > -1e-9)}")
print()
print("rango completo -> estrictamente convexa -> minimizador unico (leccion 6)")
```

Sin rango completo la función sigue siendo convexa —la hessiana no deja de ser semidefinida positiva—, pero pierde la convexidad estricta, y con ella la unicidad: hay una dirección entera en la que $S$ no cambia, que es exactamente el núcleo de $X$.

## Reto

En `proyectos/notebooks/F1-retos.ipynb`, sección **Mat 15**:

1. Escribir `es_convexa(f, grad, hess, caja, n=5000)` que muestree la caja y devuelva el menor eigenvalor hallado junto con el punto donde ocurre. Probarla con cinco funciones de convexidad conocida y anotar cuántas muestras hicieron falta para encontrar el punto malo en la que menos lo enseña.
2. Comprobar la Proposición 15.3 en la práctica: para una función convexa, tomar el plano tangente en un punto y verificar que acota por debajo a la función en toda una rejilla, no solo cerca. Repetirlo con una no convexa y medir a qué distancia empieza a fallar la cota.
3. Demostrar que la composición $g(f(x))$ con $f$ convexa y $g$ convexa **creciente** es convexa, y construir un contraejemplo donde $g$ sea convexa pero decreciente. Anotarlo en `50-Errores/`.

## Del libro

*Mathematics for Machine Learning* dedica §7.3 *Convex Optimization* a este material, dentro del capítulo 7 sobre optimización continua. El libro tiene copyright y no es descargable desde el entorno en que se escribió esta página, así que se cita número y título y no se le atribuye ninguna frase.

**Preguntas para leer §7.3 con lápiz:**

1. La sección trata problemas de optimización convexa, donde tanto la función objetivo como el conjunto factible son convexos. Con la Definición 15.1 y la Proposición 15.5, ¿por qué hacen falta las dos condiciones, y qué puede salir mal si el conjunto factible no es convexo aunque la función sí lo sea?
2. §7.3 llega a la dualidad y al problema dual. Antes de leerla: con la Proposición 15.3, ¿por qué el plano tangente en cualquier punto da una cota inferior del mínimo global, y en qué se parece eso a una función dual?
3. La programación lineal es el caso donde la función y las restricciones son afines. ¿Qué dice la Proposición 15.4 sobre la hessiana de una función afín, y por qué entonces toda función afín es a la vez convexa y cóncava?

## Para el Cerebro

Nota nueva en `10-Conceptos/convexidad.md`, enlazada a [[hessiana]], [[descenso-de-gradiente]] y [[minimos-cuadrados]]. Conviene rehacer a mano la Proposición 15.5: son cuatro líneas, no usa derivadas, y es la razón por la que toda esta lección existe.

```
¿Qué es un conjunto convexo?::Uno donde el segmento entre dos puntos cualesquiera se queda dentro
¿Qué es una función convexa?::f(tx+(1−t)y) ≤ t f(x) + (1−t) f(y): la gráfica queda bajo sus cuerdas
¿Qué dice la caracterización de primer orden?::f(y) ≥ f(x) + ∇f(x)·(y−x): la función queda por encima de sus tangentes
¿Para qué sirve esa desigualdad?::El plano tangente pasa de aproximación local a cota inferior válida en todo el dominio
¿Cuál es el criterio de segundo orden?::Convexa ⟺ hessiana semidefinida positiva en todo punto
¿Cómo se comprueba ese criterio?::Mirando que el menor eigenvalor de la hessiana no sea negativo (Prop 12.4)
¿Qué gana uno con la convexidad?::Todo mínimo local es global (Proposición 15.5)
¿Esa demostración usa derivadas?::No: sale solo de la desigualdad de la definición, en cuatro líneas
¿Cuándo es único el minimizador?::Cuando la función es estrictamente convexa
¿Qué operaciones conservan la convexidad?::Suma, producto por escalar no negativo, y máximo; el producto no
¿Es convexa la suma de cuadrados ‖y−Xb‖²?::Sí, con hessiana 2XᵀX; estrictamente si X tiene rango columna completo
```

::: {#fuentes .fuentes}
### Fuentes

**Lo que esta página demuestra sola.** Las Proposiciones 15.3, 15.4, 15.5 y 15.6 se demuestran aquí a partir de las Definiciones 15.1 y 15.2 y del desarrollo de Taylor de la lección 14. Se comprueban además numéricamente: la desigualdad de la Definición 15.2 en 20 000 pares sorteados, con cero violaciones en la función convexa y 2712 en la que no lo es; el menor eigenvalor de la hessiana sobre 4000 puntos, constante en 2 en un caso y negativo en 1709 puntos en el otro; y que 40 descensos desde arranques al azar llegan a un solo destino en la convexa y a dos en la otra. Los dos visuales se contrastaron antes de publicarse contra las fórmulas en todo el rango de sus controles.

**Lo que se usa de otras lecciones sin repetir.** El desarrollo de Taylor con resto de Lagrange, en la forma $(\dagger)$, es la demostración de la Proposición 14.5. Que el cociente de incrementos tiende a $\nabla f(x)\cdot v$ también para $v$ no unitario es el cálculo de la Proposición 13.8. El criterio espectral que traduce «semidefinida positiva» a «ningún eigenvalor negativo» es la Proposición 12.4.

**Lo que se enuncia sin demostrar.** En la Proposición 15.4, dirección ($\Rightarrow$), se usa que si una función continua es negativa en un punto lo sigue siendo en un entorno; es la definición de continuidad aplicada con $\varepsilon$ igual a la mitad del valor, y no se detalla. El Ejercicio 1 pide la demostración de que suma, escalado no negativo y máximo conservan la convexidad, que no se hace en el cuerpo de la lección.

**Lo que viene de los libros.**

- [*Mathematics for Machine Learning*](https://mml-book.github.io/book/mml-book.pdf) (Deisenroth, Faisal & Ong, Cambridge University Press, 2020), §7.3 *Convex Optimization* — citada por número y título, sin transcribir texto.

**Lo que es mío, no del libro.** La organización en tres caracterizaciones equivalentes —cuerda, tangente y hessiana— con la demostración de ida y vuelta de cada una, y el orden que deja la Proposición 15.5 al final como la razón de todo lo anterior. El segundo visual, que muestra doce descensos simultáneos con un control de pasos en lugar de un solo camino: el reparto en dos destinos solo se ve cuando se miran varios a la vez.

[Índices verificados el 12-09-2026 contra el índice publicado del PDF oficial.]{.sello}
:::

→ Siguiente: [Descenso de gradiente y sus variantes](16-descenso-de-gradiente.qmd)
