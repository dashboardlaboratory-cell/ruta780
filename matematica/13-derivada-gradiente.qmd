---
title: "Derivada y gradiente"
subtitle: "Álgebra · Lección 13"
nivel: L2
modulo: matematica
fase: F1
prereqs: [matematica/04-producto-interno]
siguientes: [matematica/14-hessiana-taylor]
libro: "MML §5.1 Differentiation of Univariate Functions · §5.2 Partial Differentiation and Gradients"
engine: markdown
pyodide:
  packages:
    - numpy
---

::: {.meta-leccion data-leccion="matematica/13-derivada-gradiente"}
[[L2]{.nivel .nivel-L2} **Derivación**]{.item}
[**Fase** F1]{.item}
[**Libro** MML §5.1 · §5.2]{.item}
[**Prereqs** [Producto interno](04-producto-interno.qmd)]{.item}
:::

## Objetivo

Al terminar esta lección se puede:

1. Definir la [derivada]{.g} como límite del cociente de incrementos, y demostrar que toda función diferenciable es continua.
2. Demostrar la [regla de la cadena]{.g} desde la definición, sin suponer que el incremento de la función interior nunca se anula.
3. Definir la [derivada parcial]{.g}, la [derivada direccional]{.g} y el [gradiente]{.g}, y demostrar que, para una función [diferenciable]{.g}, la derivada direccional es el producto interno del gradiente con la dirección.
4. Demostrar que el gradiente apunta hacia la dirección de máximo crecimiento y que su norma es la tasa de ese crecimiento, usando la desigualdad de Cauchy-Schwarz.

:::: {.hilo}
::: {}
#### De dónde viene

- **Álgebra 4**: el producto interno y la desigualdad de Cauchy-Schwarz (Proposición 4.5), que aquí deciden en qué dirección crece más rápido una función.
- **Álgebra 1**: la base canónica $e_1,\dots,e_n$, usada para definir la derivada parcial como el caso particular de la derivada direccional en la dirección de un eje.
:::

::: {}
#### Para qué sirve después

- **Álgebra 14 a 17**: la Hessiana, la aproximación de Taylor y el descenso de gradiente, un algoritmo que en cada paso sigue exactamente la dirección de la Proposición 13.9.
- **Estadística 8**: el estimador de máxima verosimilitud se encuentra igualando a cero el gradiente de la log-verosimilitud.
- **Estadística 13**: los coeficientes de mínimos cuadrados también se obtienen igualando a cero el gradiente de la suma de cuadrados.
:::
::::

## Notación

::: {.notacion}
| Símbolo | Se lee | Significado |
|---|---|---|
| $f'(a)$ | f prima de a | La derivada de $f$ en el punto $a$ |
| $h$ | hache | El incremento; en toda esta lección tiende a cero |
| $e_i$ | e sub i | El $i$-ésimo vector de la base canónica |
| $\nabla f(a)$ | gradiente de f en a | El vector de las derivadas parciales de $f$ en $a$ |
| $\partial f/\partial x_i$ | derivada parcial de f respecto a x sub i | La derivada de $f$ moviendo solo la coordenada $i$ |
| $v$ | uve | Un vector unitario, $\|v\|=1$ |
| $D_vf(a)$ | derivada direccional de f en a según v | El límite de la razón de cambio de $f$ en $a$ en la dirección $v$ |
| $r(h)$ | resto | El error de la aproximación lineal; $r(h)/\|h\|\to0$ |
:::

## 1. La derivada es un límite

::: {.definicion}
**Definición 13.1 (derivada).** Sea $f$ definida en un intervalo abierto que contiene a $a\in\mathbb{R}$. La **derivada** de $f$ en $a$ es
$$f'(a) \;:=\; \lim_{h\to0}\frac{f(a+h)-f(a)}{h},$$
si ese límite existe. En ese caso se dice que $f$ es diferenciable en $a$.
:::

::: {.proposicion}
**Proposición 13.2 (diferenciable implica continua).** Si $f$ es diferenciable en $a$, entonces $f$ es continua en $a$.
:::

::: {.demostracion}
*Demostración.* Para $h\ne0$,
$$f(a+h)-f(a) = \frac{f(a+h)-f(a)}{h}\cdot h.$$
Cuando $h\to0$, el primer factor tiende a $f'(a)$ por hipótesis, y el segundo tiende a $0$; el límite del producto es el producto de los límites, así que $f(a+h)-f(a)\to f'(a)\cdot 0=0$, es decir $f(a+h)\to f(a)$. Eso es continuidad en $a$. [∎]{.fin-demo}
:::

El recíproco es falso, $|x|$ es continua en $0$ y no diferenciable ahí, y por eso el orden de la Proposición 13.2 no se invierte: hace falta un poco más que continuidad para poder hablar de pendiente.

```{=html}
<div class="viz-secante">
  <div class="controles">
    <label>Punto a <input id="se-a" type="range" min="-30" max="30" step="1" value="10"> <span id="se-a-v">1,0</span></label>
    <label>h <input id="se-h" type="range" min="1" max="35" step="1" value="25"> <span id="se-h-v">2,5</span></label>
  </div>
  <svg id="se-svg" viewBox="0 0 480 300" role="img" aria-label="La curva seno con una recta secante entre el punto a y el punto a+h, y la recta tangente en a de referencia. Al reducir h la secante se acerca a la tangente."></svg>
  <p class="lectura" id="se-lectura"></p>
</div>
<style>
.viz-secante { border: 1px solid var(--r-linea, #ddd); border-radius: 10px; padding: 1rem 1.1rem; margin: 1.7rem 0; }
.viz-secante .controles { display: flex; flex-wrap: wrap; gap: .45rem 1.1rem; align-items: center; margin-bottom: .85rem; font-size: .85rem; }
.viz-secante label { display: flex; align-items: center; gap: .4rem; }
.viz-secante input[type=range] { width: 120px; padding: 0; }
.viz-secante svg { width: 100%; height: auto; display: block; }
.viz-secante .lectura { font-size: .85rem; margin-top: .75rem; min-height: 4.4em; line-height: 1.6; }
.viz-secante .lectura .fx { font-family: var(--bs-font-monospace, monospace); font-size: .95em; }
</style>
<script>
(function () {
  var C_CURVA = "var(--r-tinta,#1f2430)", C_SEC = "#B45309", C_TAN = "#0F6E68";
  var svg = document.getElementById("se-svg"), lec = document.getElementById("se-lectura");
  var ca = document.getElementById("se-a"), ea = document.getElementById("se-a-v");
  var ch = document.getElementById("se-h"), eh = document.getElementById("se-h-v");

  // Marco constante: el eje y la escala no dependen de a ni de h.
  var CX = 240, CY = 150, ESC = 52, RX = 4.1, RY = 2.3;

  function el(t, at, txt) {
    var e = document.createElementNS("http://www.w3.org/2000/svg", t);
    for (var k in at) e.setAttribute(k, at[k]);
    if (txt !== undefined) e.textContent = txt;
    return e;
  }
  function px(x) { return CX + x * ESC; }
  function py(y) { return CY - y * ESC; }

  function dibujar() {
    while (svg.firstChild) svg.removeChild(svg.firstChild);
    var a = (+ca.value) / 10, h = (+ch.value) / 10;
    ea.textContent = a.toFixed(1).replace(".", ",").replace("-", "−");
    eh.textContent = h.toFixed(1).replace(".", ",");

    svg.appendChild(el("rect", { x: px(-RX), y: py(RY), width: 2 * RX * ESC, height: 2 * RY * ESC, fill: "none", stroke: "var(--r-linea,#ddd)" }));
    svg.appendChild(el("line", { x1: px(-RX), y1: py(0), x2: px(RX), y2: py(0), stroke: "var(--r-linea,#eee)" }));
    svg.appendChild(el("line", { x1: px(0), y1: py(-RY), x2: px(0), y2: py(RY), stroke: "var(--r-linea,#eee)" }));

    var d = "";
    for (var i = 0; i <= 200; i++) {
      var x = -RX + 2 * RX * i / 200;
      d += (i ? " L" : "M") + px(x).toFixed(1) + "," + py(Math.sin(x)).toFixed(1);
    }
    svg.appendChild(el("path", { d: d, fill: "none", stroke: C_CURVA, "stroke-width": 1.6 }));

    var fa = Math.sin(a), fah = Math.sin(a + h);
    var pendSec = (fah - fa) / h;
    var pendTan = Math.cos(a);

    svg.appendChild(el("line", {
      x1: px(a - 1.1).toFixed(1), y1: py(fa - 1.1 * pendTan).toFixed(1),
      x2: px(a + 1.1).toFixed(1), y2: py(fa + 1.1 * pendTan).toFixed(1),
      stroke: C_TAN, "stroke-width": 1.6, "stroke-dasharray": "5 4"
    }));

    var ext = 0.4, x0 = a - ext, x1 = a + h + ext;
    svg.appendChild(el("line", {
      x1: px(x0).toFixed(1), y1: py(fa + pendSec * (x0 - a)).toFixed(1),
      x2: px(x1).toFixed(1), y2: py(fa + pendSec * (x1 - a)).toFixed(1),
      stroke: C_SEC, "stroke-width": 1.8
    }));

    svg.appendChild(el("circle", { cx: px(a).toFixed(1), cy: py(fa).toFixed(1), r: 3.6, fill: C_SEC }));
    svg.appendChild(el("circle", { cx: px(a + h).toFixed(1), cy: py(fah).toFixed(1), r: 3.6, fill: C_SEC }));
    svg.appendChild(el("text", { x: (px(a) - 6).toFixed(1), y: (py(fa) - 8).toFixed(1), "font-size": 10, "text-anchor": "end", fill: C_SEC }, "a"));
    svg.appendChild(el("text", { x: (px(a + h) + 6).toFixed(1), y: (py(fah) - 8).toFixed(1), "font-size": 10, fill: C_SEC }, "a+h"));
    svg.appendChild(el("text", { x: px(-RX), y: 20, "font-size": 11, "font-weight": 600, fill: "var(--r-tinta,#222)" }, "f(x) = sen(x)"));

    var dif = Math.abs(pendSec - pendTan);
    lec.innerHTML = "<span class=\"fx\">pendiente secante = " + pendSec.toFixed(4).replace(".", ",") +
      "   ·   f'(a) = cos(a) = " + pendTan.toFixed(4).replace(".", ",") +
      "   ·   diferencia = " + dif.toFixed(4).replace(".", ",") + "</span><br>" +
      "La secante une (a, f(a)) con (a+h, f(a+h)); la tangente, fija en a, marca hacia dónde converge la secante cuando h se hace pequeño.";
  }

  ca.addEventListener("input", dibujar);
  ch.addEventListener("input", dibujar);
  dibujar();
})();
</script>
```

```{pyodide}
def f(x):
    return x**3 - 2*x + 1

def fprime_exacta(x):
    return 3*x**2 - 2

a = 1.3
exacta = fprime_exacta(a)
print(f"f'(a) exacta en a = {a}:  {exacta:.6f}")
print()
print(f"{'h':>10s}  {'cociente [f(a+h)-f(a)]/h':>26s}  {'error absoluto':>16s}")
hs = [1.0, 0.5, 0.1, 0.01, 0.001, 0.0001]
errores = []
for h in hs:
    cociente = (f(a + h) - f(a)) / h
    error = abs(cociente - exacta)
    errores.append(error)
    print(f"{h:10.4f}  {cociente:26.6f}  {error:16.8f}")

print()
print("el error decrece en cada paso:", bool(all(errores[i+1] < errores[i] for i in range(len(errores)-1))))
print(f"con h=0.0001 el error ya es menor que 1e-3: {errores[-1] < 1e-3}")
```

El cociente de incrementos se acerca a la derivada exacta a medida que $h$ se reduce: con $h=0{,}0001$ el error ya es menor que una milésima. La celda mide exactamente lo que dibuja el visual: la secante convirtiéndose en tangente.

## 2. La regla de la cadena

::: {.proposicion}
**Proposición 13.3 (regla de la cadena).** Sea $g$ diferenciable en $a$ y $f$ diferenciable en $b:=g(a)$. Entonces $f\circ g$ es diferenciable en $a$ y
$$(f\circ g)'(a) = f'(g(a))\cdot g'(a).$$
:::

::: {.demostracion}
*Demostración.* Como $f$ es diferenciable en $b$, defínase
$$\varphi(k) = \begin{cases}\dfrac{f(b+k)-f(b)}{k} & k\ne0\\[4pt] f'(b) & k=0.\end{cases}$$
Por la Definición 13.1, $\varphi(k)\to f'(b)=\varphi(0)$ cuando $k\to0$: $\varphi$ es continua en $0$. Además, para **todo** $k$ (incluido $k=0$, donde ambos lados son cero),
$$f(b+k)-f(b) = \varphi(k)\cdot k. \tag{$\ast$}$$

Sea $k(h):=g(a+h)-g(a)$. Por la Proposición 13.2, $g$ es continua en $a$, así que $k(h)\to0$ cuando $h\to0$. Sustituyendo $k=k(h)$ en $(\ast)$,
$$f(g(a+h))-f(g(a)) = f(b+k(h))-f(b) = \varphi(k(h))\cdot k(h).$$
Dividiendo entre $h\ne0$,
$$\frac{f(g(a+h))-f(g(a))}{h} = \varphi(k(h))\cdot\frac{g(a+h)-g(a)}{h}.$$
Cuando $h\to0$: $k(h)\to0$ y $\varphi$ es continua en $0$, luego $\varphi(k(h))\to\varphi(0)=f'(g(a))$; y el segundo factor tiende a $g'(a)$ por definición. El límite del producto es el producto de los límites:
$$(f\circ g)'(a) = f'(g(a))\cdot g'(a). \;\;[\blacksquare]$$
:::

La función auxiliar $\varphi$ es lo que evita el problema de siempre: si se divide $f(g(a+h))-f(g(a))$ entre $g(a+h)-g(a)$ directamente, esa expresión no está definida cuando $g(a+h)=g(a)$, y con $g$ constante en un entorno de $a$ eso pasa en cada $h$. La igualdad $(\ast)$ es válida incluso con $k=0$, así que la demostración no necesita descartar ese caso.

```{pyodide}
import numpy as np

def f(x):
    return np.sin(x)

def g(x):
    return x**2 + 1

def compuesta(x):
    return f(g(x))

a = 0.8
h = 1e-6
derivada_numerica = (compuesta(a + h) - compuesta(a)) / h
derivada_regla = np.cos(g(a)) * (2 * a)   # f'(g(a)) * g'(a)

print(f"g(a) = {g(a):.6f}")
print(f"(f o g)'(a) por diferencia finita:  {derivada_numerica:.6f}")
print(f"f'(g(a)) * g'(a) por la regla:      {derivada_regla:.6f}")
print(f"coinciden hasta 1e-5: {abs(derivada_numerica - derivada_regla) < 1e-5}")
print()

rng = np.random.default_rng(13)
puntos = rng.uniform(-3, 3, size=500)
num = np.array([(compuesta(x + h) - compuesta(x)) / h for x in puntos])
regla = np.cos(g(puntos)) * (2 * puntos)
print(f"coinciden en los 500 puntos (tolerancia 1e-4): {bool(np.all(np.abs(num - regla) < 1e-4))}")
print(f"error maximo sobre los 500 puntos: {np.max(np.abs(num - regla)):.2e}")
```

## 3. Muchas variables: derivada parcial, direccional y gradiente

::: {.definicion}
**Definición 13.4 (derivada parcial).** Sea $f$ definida en un abierto de $\mathbb{R}^n$ que contiene a $a$. La derivada parcial de $f$ respecto a $x_i$ en $a$ es
$$\frac{\partial f}{\partial x_i}(a) \;:=\; \lim_{h\to0}\frac{f(a+he_i)-f(a)}{h},$$
si el límite existe: la derivada ordinaria de la función de una variable que se obtiene moviendo solo la coordenada $i$-ésima.
:::

::: {.definicion}
**Definición 13.5 (gradiente).** Si existen las $n$ derivadas parciales de $f$ en $a$, el **gradiente** de $f$ en $a$ es el vector
$$\nabla f(a) := \Big(\frac{\partial f}{\partial x_1}(a),\dots,\frac{\partial f}{\partial x_n}(a)\Big).$$
:::

::: {.definicion}
**Definición 13.6 (derivada direccional).** Para un vector unitario $v$ ($\|v\|=1$), la derivada direccional de $f$ en $a$ según $v$ es
$$D_vf(a) := \lim_{h\to0}\frac{f(a+hv)-f(a)}{h},$$
si el límite existe. En particular $D_{e_i}f(a)=\partial f/\partial x_i(a)$: la derivada parcial es la derivada direccional en la dirección de un vector de la base canónica.
:::

::: {.definicion}
**Definición 13.7 (diferenciabilidad multivariable).** $f$ es diferenciable en $a$ si existe un vector $c\in\mathbb{R}^n$ tal que
$$f(a+h) = f(a) + c\cdot h + r(h), \qquad \text{con } \frac{r(h)}{\|h\|}\to0 \text{ cuando } h\to0.$$
Es decir, $f$ admite en $a$ una aproximación lineal cuyo error se hace despreciable frente a la distancia $\|h\|$ al punto.
:::

::: {.proposicion}
**Proposición 13.8 (el gradiente es esa aproximación).** Si $f$ es diferenciable en $a$ según la Definición 13.7, el vector $c$ es único, existen las $n$ derivadas parciales de $f$ en $a$, y $c=\nabla f(a)$. Además, para **todo** vector unitario $v$, no solo los de la base canónica,
$$D_vf(a) = \nabla f(a)\cdot v.$$
:::

::: {.demostracion}
*Demostración.* Fíjese $i$ y tómese $h=te_i$ con $t\to0$, $t\ne0$. Como $\|te_i\|=|t|$, la Definición 13.7 da
$$\frac{f(a+te_i)-f(a)}{t} = c_i + \frac{r(te_i)}{t}, \qquad \Big|\frac{r(te_i)}{t}\Big| = \frac{|r(te_i)|}{\|te_i\|}\to0.$$
El lado izquierdo tiende entonces a $c_i$; pero por la Definición 13.4 también tiende a $\partial f/\partial x_i(a)$. Los límites son únicos, así que $\partial f/\partial x_i(a)=c_i$ para cada $i$: existen las $n$ derivadas parciales, y $c$ es exactamente $\nabla f(a)$ de la Definición 13.5. Eso prueba a la vez la unicidad de $c$, porque un límite que existe tiene un solo valor.

Para la fórmula general, tómese ahora $h=tv$ con $v$ unitario arbitrario y $t\to0$. El mismo argumento da
$$\frac{f(a+tv)-f(a)}{t} = c\cdot v + \frac{r(tv)}{t} \;\longrightarrow\; c\cdot v = \nabla f(a)\cdot v,$$
porque $\|tv\|=|t|$ y $r(tv)/\|tv\|\to0$. El lado izquierdo es, por la Definición 13.6, $D_vf(a)$. Luego $D_vf(a)=\nabla f(a)\cdot v$. [∎]{.fin-demo}
:::

La Proposición 13.8 es la que hace útil al gradiente: convierte una derivada direccional, un límite distinto para cada una de infinitas direcciones, en un solo producto interno.

```{pyodide}
import numpy as np

def f(p):
    x, y = p
    return x**2 * y + np.sin(x * y)

def grad_f(p):
    x, y = p
    return np.array([2*x*y + y*np.cos(x*y), x**2 + x*np.cos(x*y)])

a = np.array([1.5, -0.7])
h = 1e-6
g = grad_f(a)
print(f"gradiente en a = (1.5, -0.7):  ({g[0]:.6f}, {g[1]:.6f})")
print()

rng = np.random.default_rng(130)
angulos = rng.uniform(0, 2*np.pi, size=2000)
V = np.column_stack([np.cos(angulos), np.sin(angulos)])

Dv_numerica = np.array([(f(a + h*v) - f(a)) / h for v in V])
Dv_formula = V @ g
print("D_v f(a) por diferencia finita comparado con ∇f(a)·v, sobre 2000 direcciones unitarias:")
print(f"  error maximo:  {np.max(np.abs(Dv_numerica - Dv_formula)):.2e}")
print(f"  coinciden con tolerancia 1e-4: {bool(np.all(np.abs(Dv_numerica - Dv_formula) < 1e-4))}")
```

## 4. El gradiente apunta hacia donde más crece

::: {.proposicion}
**Proposición 13.9 (dirección de máximo crecimiento).** Sea $f$ diferenciable en $a$ con $\nabla f(a)\ne0$. Entre todos los vectores unitarios $v$, $D_vf(a)$ alcanza su **máximo** cuando $v=\nabla f(a)/\|\nabla f(a)\|$, con valor $\|\nabla f(a)\|$; y su **mínimo** cuando $v=-\nabla f(a)/\|\nabla f(a)\|$, con valor $-\|\nabla f(a)\|$.
:::

::: {.demostracion}
*Demostración.* Por la Proposición 13.8, $D_vf(a)=\nabla f(a)\cdot v$. Por la Proposición 4.5 (Cauchy-Schwarz),
$$|\nabla f(a)\cdot v| \le \|\nabla f(a)\|\,\|v\| = \|\nabla f(a)\|,$$
porque $v$ es unitario. Ningún valor de $D_vf(a)$ supera $\|\nabla f(a)\|$ en valor absoluto, sea cual sea $v$. La misma proposición dice que la igualdad se da exactamente cuando $v$ es múltiplo de $\nabla f(a)$; como además $\|v\|=1$, ese múltiplo solo puede ser $\pm\nabla f(a)/\|\nabla f(a)\|$. Sustituyendo el signo $+$,
$$\nabla f(a)\cdot\frac{\nabla f(a)}{\|\nabla f(a)\|} = \frac{\|\nabla f(a)\|^2}{\|\nabla f(a)\|} = \|\nabla f(a)\|,$$
que por la cota es el máximo posible; y el signo $-$ da el valor opuesto, que por la misma cota es el mínimo posible. [∎]{.fin-demo}
:::

Nada de esto pide calcular una derivada nueva: la Proposición 4.5 ya estaba demostrada, y aquí solo decide un signo.

```{=html}
<div class="viz-gradiente">
  <div class="controles">
    <label>x₀ <input id="gr-x" type="range" min="-30" max="30" step="1" value="18"> <span id="gr-x-v">1,8</span></label>
    <label>y₀ <input id="gr-y" type="range" min="-30" max="30" step="1" value="-15"> <span id="gr-y-v">−1,5</span></label>
    <label>Ángulo de v <input id="gr-t" type="range" min="0" max="355" step="5" value="60"> <span id="gr-t-v">60°</span></label>
  </div>
  <svg id="gr-svg" viewBox="0 0 620 300" role="img" aria-label="A la izquierda las curvas de nivel de un campo escalar, con el punto elegido, la flecha del gradiente y la flecha de la direccion v. A la derecha la derivada direccional en funcion del angulo de v, maxima cuando v coincide con el gradiente."></svg>
  <p class="lectura" id="gr-lectura"></p>
</div>
<style>
.viz-gradiente { border: 1px solid var(--r-linea, #ddd); border-radius: 10px; padding: 1rem 1.1rem; margin: 1.7rem 0; }
.viz-gradiente .controles { display: flex; flex-wrap: wrap; gap: .45rem 1.1rem; align-items: center; margin-bottom: .85rem; font-size: .85rem; }
.viz-gradiente label { display: flex; align-items: center; gap: .4rem; }
.viz-gradiente input[type=range] { width: 100px; padding: 0; }
.viz-gradiente svg { width: 100%; height: auto; display: block; }
.viz-gradiente .lectura { font-size: .85rem; margin-top: .75rem; min-height: 5.2em; line-height: 1.6; }
.viz-gradiente .lectura .fx { font-family: var(--bs-font-monospace, monospace); font-size: .95em; }
</style>
<script>
(function () {
  var C_NIVEL = "var(--r-tinta-suave,#999)", C_GRAD = "#0F6E68", C_DIR = "#B45309";
  var svg = document.getElementById("gr-svg"), lec = document.getElementById("gr-lectura");
  var cx0 = document.getElementById("gr-x"), ex0 = document.getElementById("gr-x-v");
  var cy0 = document.getElementById("gr-y"), ey0 = document.getElementById("gr-y-v");
  var ct = document.getElementById("gr-t"), et = document.getElementById("gr-t-v");

  // Centro fijo del campo escalar f(x,y) = 0.4(x-CX)^2 + 0.9(y-CY)^2.
  var CENX = 0.6, CENY = -0.4;

  // Panel izquierdo: marco constante.
  var LCX = 150, LCY = 150, LESC = 34, LR = 3.6;
  // Panel derecho: marco constante, incluido el eje vertical (no depende del punto).
  var RX0 = 350, RX1 = 600, RY0 = 270, RY1 = 30, VMAX = 7.5;

  function el(t, at, txt) {
    var e = document.createElementNS("http://www.w3.org/2000/svg", t);
    for (var k in at) e.setAttribute(k, at[k]);
    if (txt !== undefined) e.textContent = txt;
    return e;
  }
  function lx(x) { return LCX + x * LESC; }
  function ly(y) { return LCY - y * LESC; }
  function qx(t) { return RX0 + t / (2 * Math.PI) * (RX1 - RX0); }
  function qy(v) { var c = Math.max(-VMAX, Math.min(VMAX, v)); return RY0 - (c + VMAX) / (2 * VMAX) * (RY0 - RY1); }

  function grad(x, y) { return [0.8 * (x - CENX), 1.8 * (y - CENY)]; }

  function dibujar() {
    while (svg.firstChild) svg.removeChild(svg.firstChild);
    var x0 = (+cx0.value) / 10, y0 = (+cy0.value) / 10, ang = (+ct.value) * Math.PI / 180;
    ex0.textContent = x0.toFixed(1).replace(".", ",").replace("-", "−");
    ey0.textContent = y0.toFixed(1).replace(".", ",").replace("-", "−");
    et.textContent = (+ct.value) + "°";

    // --- panel izquierdo: curvas de nivel, punto, gradiente y direccion ---
    svg.appendChild(el("rect", { x: lx(-LR), y: ly(LR), width: 2 * LR * LESC, height: 2 * LR * LESC, fill: "none", stroke: "var(--r-linea,#ddd)" }));
    [0.4, 1.2, 2.4, 3.5].forEach(function (c) {
      var ax = Math.sqrt(c / 0.4), ay = Math.sqrt(c / 0.9);
      svg.appendChild(el("ellipse", {
        cx: lx(CENX).toFixed(1), cy: ly(CENY).toFixed(1), rx: (ax * LESC).toFixed(1), ry: (ay * LESC).toFixed(1),
        fill: "none", stroke: C_NIVEL, "stroke-width": 1, "stroke-opacity": 0.6
      }));
    });
    svg.appendChild(el("circle", { cx: lx(CENX).toFixed(1), cy: ly(CENY).toFixed(1), r: 2.2, fill: C_NIVEL }));

    var g = grad(x0, y0);
    var gnorm = Math.sqrt(g[0] * g[0] + g[1] * g[1]);
    var gu = gnorm > 1e-9 ? [g[0] / gnorm, g[1] / gnorm] : [1, 0];
    var vu = [Math.cos(ang), Math.sin(ang)];
    var LEN = 0.95;

    if (gnorm > 1e-9) {
      svg.appendChild(el("line", {
        x1: lx(x0).toFixed(1), y1: ly(y0).toFixed(1),
        x2: lx(x0 + LEN * gu[0]).toFixed(1), y2: ly(y0 + LEN * gu[1]).toFixed(1),
        stroke: C_GRAD, "stroke-width": 2.2
      }));
      svg.appendChild(el("circle", { cx: lx(x0 + LEN * gu[0]).toFixed(1), cy: ly(y0 + LEN * gu[1]).toFixed(1), r: 3.2, fill: C_GRAD }));
    }
    svg.appendChild(el("line", {
      x1: lx(x0).toFixed(1), y1: ly(y0).toFixed(1),
      x2: lx(x0 + LEN * vu[0]).toFixed(1), y2: ly(y0 + LEN * vu[1]).toFixed(1),
      stroke: C_DIR, "stroke-width": 2.2, "stroke-dasharray": "6 3"
    }));
    svg.appendChild(el("circle", { cx: lx(x0 + LEN * vu[0]).toFixed(1), cy: ly(y0 + LEN * vu[1]).toFixed(1), r: 3.2, fill: C_DIR }));
    svg.appendChild(el("circle", { cx: lx(x0).toFixed(1), cy: ly(y0).toFixed(1), r: 3.6, fill: "var(--r-tinta,#222)" }));
    svg.appendChild(el("text", { x: lx(-LR), y: 20, "font-size": 11, "font-weight": 600, fill: "var(--r-tinta,#222)" }, "curvas de nivel y campo de gradientes"));

    // --- panel derecho: D_v f(a) en funcion del angulo de v ---
    svg.appendChild(el("rect", { x: RX0, y: RY1, width: RX1 - RX0, height: RY0 - RY1, fill: "none", stroke: "var(--r-linea,#ddd)" }));
    svg.appendChild(el("line", { x1: RX0, y1: qy(0).toFixed(1), x2: RX1, y2: qy(0).toFixed(1), stroke: "var(--r-linea,#eee)" }));
    var d = "";
    for (var i = 0; i <= 180; i++) {
      var t = 2 * Math.PI * i / 180;
      var val = g[0] * Math.cos(t) + g[1] * Math.sin(t);
      d += (i ? " L" : "M") + qx(t).toFixed(1) + "," + qy(val).toFixed(1);
    }
    svg.appendChild(el("path", { d: d, fill: "none", stroke: C_GRAD, "stroke-width": 1.8 }));
    if (gnorm > 1e-9) {
      svg.appendChild(el("line", { x1: RX0, y1: qy(gnorm).toFixed(1), x2: RX1, y2: qy(gnorm).toFixed(1), stroke: C_NIVEL, "stroke-width": 1, "stroke-dasharray": "4 3" }));
      svg.appendChild(el("line", { x1: RX0, y1: qy(-gnorm).toFixed(1), x2: RX1, y2: qy(-gnorm).toFixed(1), stroke: C_NIVEL, "stroke-width": 1, "stroke-dasharray": "4 3" }));
    }
    var dv = g[0] * vu[0] + g[1] * vu[1];
    svg.appendChild(el("line", { x1: qx(ang).toFixed(1), y1: RY0, x2: qx(ang).toFixed(1), y2: RY1, stroke: C_DIR, "stroke-width": 1, "stroke-dasharray": "3 3", "stroke-opacity": 0.7 }));
    svg.appendChild(el("circle", { cx: qx(ang).toFixed(1), cy: qy(dv).toFixed(1), r: 3.4, fill: C_DIR }));
    svg.appendChild(el("text", { x: RX0, y: 20, "font-size": 11, "font-weight": 600, fill: "var(--r-tinta,#222)" }, "D_v f(a) según el ángulo de v"));

    var angGrad = ((Math.atan2(g[1], g[0]) * 180 / Math.PI) + 360) % 360;
    lec.innerHTML = "<span class=\"fx\">∇f(a) = (" + g[0].toFixed(3) + ", " + g[1].toFixed(3) + ")   ·   ‖∇f(a)‖ = " + gnorm.toFixed(3) +
      "   ·   ángulo del gradiente ≈ " + angGrad.toFixed(0) + "°   ·   D_v f(a) = " + dv.toFixed(3) + "</span><br>" +
      "La curva de la derecha es ∇f(a)·v(θ): un coseno cuya cresta está exactamente en el ángulo del gradiente y cuyo valle está a 180° de distancia.";
  }

  cx0.addEventListener("input", dibujar);
  cy0.addEventListener("input", dibujar);
  ct.addEventListener("input", dibujar);
  dibujar();
})();
</script>
```

```{pyodide}
import numpy as np

def grad_f(p):
    x, y = p
    return np.array([2*p[0]*p[1] + p[1]*np.cos(p[0]*p[1]), p[0]**2 + p[0]*np.cos(p[0]*p[1])])

a = np.array([1.5, -0.7])
grad = grad_f(a)

rng = np.random.default_rng(131)
angulos = rng.uniform(0, 2*np.pi, size=4000)
V = np.column_stack([np.cos(angulos), np.sin(angulos)])
Dv = V @ grad

i_max = np.argmax(Dv)
vmax = V[i_max]
grad_norm = grad / np.linalg.norm(grad)
print(f"direccion que maximiza D_v f entre 4000 muestreadas: ({vmax[0]:.4f}, {vmax[1]:.4f})")
print(f"gradiente normalizado:                               ({grad_norm[0]:.4f}, {grad_norm[1]:.4f})")
print(f"apuntan al mismo lugar (producto interno > 0.999):   {bool(vmax @ grad_norm > 0.999)}")
print()
print(f"valor maximo muestreado de D_v f:  {Dv[i_max]:.6f}")
print(f"norma del gradiente:               {np.linalg.norm(grad):.6f}")
print(f"coinciden hasta 3 decimales: {abs(Dv[i_max] - np.linalg.norm(grad)) < 5e-4}")
print()
i_min = np.argmin(Dv)
vmin = V[i_min]
print(f"direccion que minimiza D_v f: ({vmin[0]:.4f}, {vmin[1]:.4f})   (opuesta al gradiente: {bool(vmin @ grad_norm < -0.999)})")
print(f"valor minimo muestreado: {Dv[i_min]:.6f}   -norma del gradiente: {-np.linalg.norm(grad):.6f}")
```

Entre 4000 direcciones muestreadas al azar, la que más hace crecer a $f$ coincide con el gradiente normalizado, y la que más lo hace decrecer es su opuesta: exactamente lo que dice la Proposición 13.9, medido en vez de solo enunciado.

## Ejercicios

### Ejercicio 1, La regla del producto, sin memorizarla

Demostrar, a partir de la Definición 13.1, la regla del producto $(fg)'(a)=f'(a)g(a)+f(a)g'(a)$, sumando y restando $f(a+h)g(a)$ en el numerador del cociente de incrementos de $fg$. Comprobar el resultado para $f(x)=x^2$ y $g(x)=\operatorname{sen}(x)$.

```{pyodide}
import numpy as np

def f(x): return x**2
def fp(x): return 2*x
def g(x): return np.sin(x)
def gp(x): return np.cos(x)

h = 1e-6
rng = np.random.default_rng(1300)
puntos = rng.uniform(-3, 3, size=500)

num = np.array([((f(a+h)*g(a+h)) - (f(a)*g(a))) / h for a in puntos])
formula = fp(puntos)*g(puntos) + f(puntos)*gp(puntos)
print("(fg)'(a) por diferencia finita vs f'(a)g(a)+f(a)g'(a), en 500 puntos:")
print(f"  error maximo:  {np.max(np.abs(num - formula)):.2e}")
print(f"  coinciden con tolerancia 1e-4: {bool(np.all(np.abs(num - formula) < 1e-4))}")
```

### Ejercicio 2, El plano tangente es mejor que cualquier otro

El plano tangente a la gráfica de $f$ en $a$ es $z=f(a)+\nabla f(a)\cdot(x-a)$. La Definición 13.7 dice que su error es $o(\|h\|)$: se hace pequeño más rápido que $\|h\|$. Comprobarlo contra un plano que use un vector cercano al gradiente pero no igual, y observar que ese segundo error **no** se acelera de la misma forma.

```{pyodide}
import numpy as np

def f(p):
    x, y = p
    return x**2 * y + np.sin(x * y)

def grad_f(p):
    x, y = p
    return np.array([2*p[0]*p[1] + p[1]*np.cos(p[0]*p[1]), p[0]**2 + p[0]*np.cos(p[0]*p[1])])

a = np.array([1.5, -0.7])
fa = f(a)
grad = grad_f(a)
c_falso = grad + np.array([0.5, 0.0])   # un plano cercano, pero no el tangente

u = np.array([0.6, 0.8])
print(f"{'t':>8s}  {'error tangente / t':>20s}  {'error plano falso / t':>22s}")
for t in [0.4, 0.1, 0.01, 0.001, 0.0001]:
    h = t * u
    error_tangente = abs(f(a + h) - (fa + grad @ h))
    error_falso = abs(f(a + h) - (fa + c_falso @ h))
    print(f"{t:8.4f}  {error_tangente/t:20.6f}  {error_falso/t:22.6f}")
```

El error del plano tangente dividido entre $t$ se va a cero con $t$: el error de verdad es $O(t^2)$. El del plano falso se estabiliza en $0{,}3$ (exactamente $|(c_{\text{falso}}-\nabla f(a))\cdot u|=|0{,}5\cdot 0{,}6|$), porque ese plano difiere del tangente en una dirección que $u$ sí detecta. Ser tangente exige coincidir exactamente con el gradiente, no solo estar cerca de él.

## Reto

En `proyectos/notebooks/F1-retos.ipynb`, sección **Mat 13**:

1. Escribir `derivada_parcial(f, a, i, h=1e-6)` que aproxime $\partial f/\partial x_i(a)$ por diferencia central, y verificarla contra `grad_f` a mano para al menos tres funciones y tres puntos.
2. Reproducir el Ejercicio 2 con una función distinta (por ejemplo $f(x,y)=e^{x}\cos(y)$) y encontrar en qué potencia de $t$ se estabiliza el error del plano tangente dividido entre $t^2$ en vez de entre $t$.
3. Implementar el descenso de gradiente más simple, $a_{k+1}=a_k-\eta\nabla f(a_k)$, sobre $f(x,y)=0{,}4(x-0{,}6)^2+0{,}9(y+0{,}4)^2$ del segundo visual, y comprobar que la trayectoria, vista desde cualquier punto de partida, cruza cada curva de nivel perpendicularmente. Anotar en `50-Errores/` qué pasa con $\eta$ demasiado grande.

## Del libro

*Mathematics for Machine Learning* dedica el capítulo 5, *Vector Calculus*, a este material: §5.1 *Differentiation of Univariate Functions* trata la derivada de una variable, y §5.2 *Partial Differentiation and Gradients* la extiende a varias. El libro tiene copyright y no es descargable desde el entorno en que se escribió esta página, así que se cita número y título y no se le atribuye ninguna frase.

**Preguntas para leer §5.1 y §5.2 con lápiz:**

1. §5.1 recorre las reglas de derivación de una variable (suma, producto, cociente, potencia) además de la regla de la cadena. Con el mismo argumento de la Proposición 13.3 (la función auxiliar continua en el punto), demostrar la regla del cociente $(f/g)'(a)=[f'(a)g(a)-f(a)g'(a)]/g(a)^2$ para $g(a)\ne0$.
2. §5.2 introduce el gradiente exactamente como la Definición 13.5 de esta página: el vector de las $n$ derivadas parciales. Comprobar que, si $f$ depende de una sola coordenada, esa definición colapsa en la derivada ordinaria de la Definición 13.1.
3. El resto del capítulo 5 (gradientes de funciones vectoriales, retropropagación) encadena exactamente lo que aquí son la Proposición 13.3 y la Proposición 13.8. Adelantar qué forma tendría la derivada de $f(g(t))$ si $g:\mathbb{R}\to\mathbb{R}^n$ y $f:\mathbb{R}^n\to\mathbb{R}$.

## Para el Cerebro

Nota nueva en `10-Conceptos/derivada-y-gradiente.md`, enlazada a [[producto-interno]], [[hessiana]] y [[descenso-de-gradiente]]. Conviene rehacer a mano la demostración de la Proposición 13.3: la función auxiliar $\varphi$ es la idea que evita el caso incómodo de $g(a+h)=g(a)$, y reaparece cada vez que se deriva una composición con cuidado.

```
¿Qué es la derivada de f en a?::El límite del cociente [f(a+h)-f(a)]/h cuando h tiende a cero
¿Diferenciable implica continua, o al revés?::Diferenciable implica continua; el recíproco es falso (|x| en 0)
¿Qué evita la funcion auxiliar φ en la regla de la cadena?::Dividir por g(a+h)-g(a), que puede valer cero
¿Qué es la derivada parcial?::La derivada ordinaria de f moviendo solo una coordenada, las demás fijas
¿Qué es el gradiente?::El vector de las n derivadas parciales, ∇f(a)
¿Qué es la derivada direccional D_v f(a)?::La tasa de cambio de f en a en la dirección unitaria v
¿Cómo se relaciona D_v f(a) con el gradiente?::D_v f(a) = ∇f(a)·v, si f es diferenciable en a
¿En qué dirección crece más rápido f?::En la del gradiente normalizado, ∇f(a)/‖∇f(a)‖
¿Cuánto vale la máxima derivada direccional?::‖∇f(a)‖; el mínimo es -‖∇f(a)‖, en la dirección opuesta
¿Qué desigualdad decide la Proposición 13.9?::Cauchy-Schwarz (Proposición 4.5) y su condición de igualdad
```

::: {#fuentes .fuentes}
### Fuentes

**Lo que esta página demuestra sola.** Las Proposiciones 13.2, 13.3, 13.8 y 13.9 se demuestran aquí desde la Definición 13.1 y desde la desigualdad de Cauchy-Schwarz de la lección 4. Se comprueban además numéricamente: la convergencia del cociente de incrementos a la derivada exacta en seis escalas de $h$; la regla de la cadena contra diferencia finita en 500 puntos; la fórmula $D_vf(a)=\nabla f(a)\cdot v$ contra diferencia finita en 2000 direcciones; que la dirección que maximiza $D_vf(a)$ entre 4000 muestreadas coincide con el gradiente normalizado y que el máximo vale su norma; la regla del producto en 500 puntos; y que el error del plano tangente cae con $t^2$ mientras el de un plano cercano pero distinto se estabiliza en un valor no nulo. Los dos visuales se contrastaron antes de publicarse contra las fórmulas en todo el rango de sus controles.

**Lo que se usa de otras lecciones sin repetir.** La desigualdad de Cauchy-Schwarz y su condición de igualdad son la Proposición 4.5. La base canónica $e_1,\dots,e_n$ es de la lección 1.

**Lo que se enuncia sin demostrar.** Que el límite de un producto es el producto de los límites, usado dos veces (Proposiciones 13.2 y 13.3), es un resultado de cálculo de una variable que se da por conocido. La regla del producto, del cociente y las demás reglas elementales de derivación no se demuestran aquí: se proponen como ejercicio con el mismo método que la regla de la cadena.

**Lo que viene de los libros.**

- [*Mathematics for Machine Learning*](https://mml-book.github.io/book/mml-book.pdf) (Deisenroth, Faisal & Ong, Cambridge University Press, 2020), §5.1 *Differentiation of Univariate Functions* y §5.2 *Partial Differentiation and Gradients* — citadas por número y título, sin transcribir texto.

**Lo que es mío, no del libro.** La demostración de la regla de la cadena mediante la función auxiliar $\varphi$, presentada para que la Definición 13.7 y la Proposición 13.8 encajen sin fisuras con lo que sigue en Álgebra 14 en adelante. La Proposición 13.9 como consecuencia directa, sin demostración nueva, de la desigualdad de Cauchy-Schwarz ya probada en la lección 4. El Ejercicio 2, construido para hacer visible la diferencia entre "cercano al gradiente" y "el gradiente".

[Índices verificados el 12-09-2026 contra el índice publicado del PDF oficial.]{.sello}
:::

→ Siguiente: [Hessiana y expansión de Taylor](14-hessiana-taylor.qmd)
