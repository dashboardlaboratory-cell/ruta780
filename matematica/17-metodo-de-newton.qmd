---
title: "Método de Newton"
subtitle: "Álgebra · Lección 17"
nivel: L3
modulo: matematica
fase: F1
prereqs: [matematica/16-descenso-de-gradiente, matematica/14-hessiana-taylor]
siguientes: [matematica/18-lagrange-kkt]
libro: "MML §7.1 Optimization Using Gradient Descent"
engine: markdown
pyodide:
  packages:
    - numpy
    - scipy
---

::: {.meta-leccion data-leccion="matematica/17-metodo-de-newton"}
[[L3]{.nivel .nivel-L3} **From scratch**]{.item}
[**Fase** F1]{.item}
[**Libro** MML §7.1]{.item}
[**Prereqs** [Descenso de gradiente](16-descenso-de-gradiente.qmd) · [Hessiana y Taylor](14-hessiana-taylor.qmd)]{.item}
:::

## Objetivo

Al terminar esta lección se puede:

1. Definir el paso del [método de Newton]{.g} y demostrar que es el minimizador exacto del modelo cuadrático de Taylor.
2. Demostrar que en una cuadrática converge en un solo paso desde cualquier arranque, sin que el número de condición intervenga.
3. Demostrar que cerca del óptimo la convergencia es [cuadrática]{.g data-t="convergencia cuadrática"}: el número de dígitos correctos se duplica en cada paso.
4. Explicar por qué el método falla cuando la hessiana no es definida positiva, y cómo lo arregla el [Newton regularizado]{.g}.

:::: {.hilo}
::: {}
#### De dónde viene

- **Álgebra 16**: el descenso de gradiente y su tasa $(\kappa-1)/(\kappa+1)$. Esta lección existe porque esa tasa es mala cuando $\kappa$ es grande.
- **Álgebra 14**: el modelo cuadrático de Taylor (Definición 14.4) y la condición de segundo orden. El paso de Newton es «minimizar el modelo en lugar de la función».
- **Álgebra 15**: la convexidad estricta, que es lo que hace único al minimizador del modelo.
- **Álgebra 12**: el criterio espectral, que decide si el modelo tiene mínimo o no lo tiene.
:::

::: {}
#### Para qué sirve después

- **Álgebra 18**: las condiciones KKT se resuelven con una variante de Newton sobre el sistema que forman.
- **Estadística 8**: la regresión logística se ajusta por Newton, donde el método tiene nombre propio, *iteratively reweighted least squares*, y la hessiana se conoce en forma cerrada.
- **Estadística 9 y 14**: la inversa de la hessiana de la log-verosimilitud en el óptimo es la matriz de covarianza asintótica del estimador, así que Newton entrega de paso los errores estándar.
:::
::::

## Notación

::: {.notacion}
| Símbolo | Se lee | Significado |
|---|---|---|
| $H_k = H_f(x_k)$ | hache sub ka | La hessiana evaluada en el punto actual |
| $m(h)$ | eme de hache | El modelo cuadrático de Taylor alrededor de $x_k$ |
| $e_k = x_k-x^\star$ | error en el paso ka | La distancia al minimizador |
| $\mu$ | mu | El menor eigenvalor de la hessiana en el óptimo; también el regularizador |
| $L$ | ele | La constante de Lipschitz de la hessiana |
| $t$ | te | El factor de amortiguación, $t\in(0,1]$ |
:::

## 1. Minimizar el modelo en vez de la función

::: {.definicion}
**Definición 17.1 (paso de Newton).** Si $H_k=H_f(x_k)$ es invertible, el **método de Newton** genera
$$x_{k+1} = x_k - H_k^{-1}\,\nabla f(x_k).$$
:::

El descenso de gradiente usa solo el término lineal del desarrollo de Taylor y compensa con un paso $\eta$ elegido a mano. Newton usa además el término cuadrático, y entonces el paso ya no hace falta elegirlo: lo fija la propia hessiana.

::: {.proposicion}
**Proposición 17.2 (Newton minimiza el modelo).** Sea $m(h)=f(x_k)+\nabla f(x_k)\cdot h+\tfrac12h^\top H_kh$ el modelo cuadrático de la Definición 14.4. Si $H_k$ es definida positiva, $m$ tiene un único minimizador, y es
$$h^\star = -H_k^{-1}\nabla f(x_k),$$
es decir, exactamente el paso de la Definición 17.1.
:::

::: {.demostracion}
*Demostración.* La hessiana de $m$ respecto a $h$ es $H_k$ en todo punto, el Ejercicio 2 de la lección 14, y es definida positiva por hipótesis. Por la Proposición 15.4, $m$ es convexa, y por ser $H_k$ definida positiva lo es estrictamente, así que la Proposición 15.6 da a lo sumo un minimizador. Por la Proposición 14.7 ese minimizador ha de ser un punto crítico, y
$$\nabla m(h) = \nabla f(x_k) + H_kh = 0 \quad\Longleftrightarrow\quad h = -H_k^{-1}\nabla f(x_k),$$
donde la inversa existe porque los eigenvalores son positivos. Ese punto crítico existe y es único, luego es el minimizador. [∎]{.fin-demo}
:::

De la Proposición 17.2 salen las dos caras del método. La buena: si el modelo se parece a la función, el paso cae cerca del óptimo. La mala: si $H_k$ **no** es definida positiva, el modelo no tiene mínimo y el paso resuelve un problema que no es el que se quería resolver.

::: {.proposicion}
**Proposición 17.3 (una cuadrática se resuelve en un paso).** Sea $f(x)=\tfrac12x^\top Ax-b^\top x$ con $A$ simétrica definida positiva y $x^\star=A^{-1}b$. Entonces, desde cualquier $x_0$,
$$x_1 = x^\star.$$
:::

::: {.demostracion}
*Demostración.* Aquí $\nabla f(x)=Ax-b$ y $H_f(x)=A$ en todo punto, de modo que
$$x_1 = x_0 - A^{-1}(Ax_0-b) = x_0 - x_0 + A^{-1}b = A^{-1}b = x^\star. \;\;[\blacksquare]$$
:::

Compárese con la Proposición 16.5: sobre el mismo problema, el descenso de gradiente necesita del orden de $\kappa$ iteraciones y Newton necesita una, siempre. El número de condición desaparece porque $H^{-1}$ deshace exactamente el estiramiento que $\kappa$ mide.

```{pyodide}
import numpy as np

rng = np.random.default_rng(170)

# Se trabaja en la base propia, que por la Proposicion 16.5 no pierde
# generalidad: ahi la hessiana es diagonal y todo son cuentas elemento a
# elemento, sin rutinas de algebra lineal cuya ultima cifra cambie de una
# version a otra.
print(f"{'kappa':>9s}  {'Newton':>8s}  {'error de Newton':>17s}  {'descenso hasta 1e-10':>21s}")
for kappa in [2.0, 50.0, 5000.0]:
    d = np.linspace(1.0, kappa, 4)          # los eigenvalores de la hessiana
    b = rng.normal(size=4)
    x_est = b / d                            # el minimo: d*x = b
    x0 = rng.normal(size=4) * 3

    # Un paso de Newton: x0 - H^-1 grad, con H diagonal
    x1 = x0 - (d*x0 - b)/d
    err_newton = np.linalg.norm(x1 - x_est)

    # El objetivo es una tolerancia fija. Medirlo contra el error que deja
    # Newton no serviria: ese es del tamano del epsilon de maquina.
    umbral = 1e-10 * np.linalg.norm(x_est)
    eta = 2.0 / (1.0 + kappa)                # el paso optimo de la leccion 16
    x, pasos = x0.copy(), 0
    while np.linalg.norm(x - x_est) > umbral and pasos < 500000:
        x = x - eta*(d*x - b); pasos += 1

    cota = "por debajo de 1e-13" if err_newton < 1e-13 else f"{err_newton:.1e}"
    print(f"{kappa:9.0f}  {1:8d}  {cota:>17s}  {pasos:21d}")
print()
print("Newton no mira el numero de condicion; el descenso de gradiente lo paga entero")
```

Con $\kappa=5000$ el descenso necesita $77\,652$ iteraciones para llegar a una tolerancia que Newton cruza en una. Del error de Newton solo se afirma que queda por debajo de $10^{-13}$: es del tamaño del épsilon de máquina, y sus dígitos cambian de una versión de la biblioteca a otra, así que citarlos sería citar ruido.

```{=html}
<div class="viz-newton1d">
  <div class="controles">
    <label>Función
      <select id="n1-f">
        <option value="0" selected>convexa: x⁴/8 + x²/2 − x</option>
        <option value="1">con tramo cóncavo: x⁴/8 − x² + x</option>
      </select>
    </label>
    <label>Punto x <input id="n1-x" type="range" min="-22" max="22" step="1" value="16"> <span id="n1-x-v">1,6</span></label>
  </div>
  <svg id="n1-svg" viewBox="0 0 620 320" role="img" aria-label="Una curva con la parabola de Taylor en el punto elegido y el paso de Newton marcado hacia el vertice de esa parabola. Cuando la segunda derivada es negativa la parabola abre hacia abajo y el paso apunta cuesta arriba."></svg>
  <p class="lectura" id="n1-lectura"></p>
</div>
<style>
.viz-newton1d { border: 1px solid var(--r-linea, #ddd); border-radius: 10px; padding: 1rem 1.1rem; margin: 1.7rem 0; }
.viz-newton1d .controles { display: flex; flex-wrap: wrap; gap: .45rem 1.1rem; align-items: center; margin-bottom: .85rem; font-size: .85rem; }
.viz-newton1d label { display: flex; align-items: center; gap: .4rem; }
.viz-newton1d input[type=range] { width: 118px; padding: 0; }
.viz-newton1d select { font-size: .85rem; padding: .1rem .25rem; }
.viz-newton1d svg { width: 100%; height: auto; display: block; }
.viz-newton1d .lectura { font-size: .85rem; margin-top: .75rem; min-height: 5.2em; line-height: 1.6; }
.viz-newton1d .lectura .fx { font-family: var(--bs-font-monospace, monospace); font-size: .95em; }
</style>
<script>
(function () {
  var C_F = "var(--r-tinta,#1f2430)", C_MOD = "#0F6E68", C_PASO = "#B45309";
  var svg = document.getElementById("n1-svg"), lec = document.getElementById("n1-lectura");
  var cf = document.getElementById("n1-f");
  var cx = document.getElementById("n1-x"), ex = document.getElementById("n1-x-v");

  // Marco constante: no depende de la funcion ni del punto.
  var X0 = 40, X1 = 600, Y0 = 292, Y1 = 26, RX = 2.4, YMIN = -3.4, YMAX = 3.6;

  function el(t, at, txt) {
    var e = document.createElementNS("http://www.w3.org/2000/svg", t);
    for (var k in at) e.setAttribute(k, at[k]);
    if (txt !== undefined) e.textContent = txt;
    return e;
  }
  function px(x) { return X0 + (x + RX) / (2 * RX) * (X1 - X0); }
  function py(v) { return Y0 - (Math.max(YMIN, Math.min(YMAX, v)) - YMIN) / (YMAX - YMIN) * (Y0 - Y1); }

  var FS = [
    { f: function (x) { return x*x*x*x/8 + x*x/2 - x; },
      d1: function (x) { return x*x*x/2 + x - 1; },
      d2: function (x) { return 1.5*x*x + 1; } },
    { f: function (x) { return x*x*x*x/8 - x*x + x; },
      d1: function (x) { return x*x*x/2 - 2*x + 1; },
      d2: function (x) { return 1.5*x*x - 2; } }
  ];

  function dibujar() {
    while (svg.firstChild) svg.removeChild(svg.firstChild);
    var F = FS[+cf.value], a = (+cx.value) / 10;
    ex.textContent = a.toFixed(1).replace(".", ",").replace("-", "−");

    svg.appendChild(el("rect", { x: X0, y: Y1, width: X1 - X0, height: Y0 - Y1, fill: "none", stroke: "var(--r-linea,#ddd)" }));
    svg.appendChild(el("line", { x1: X0, y1: py(0), x2: X1, y2: py(0), stroke: "var(--r-linea,#eee)" }));

    var i, x, d = "";
    for (i = 0; i <= 280; i++) {
      x = -RX + 2 * RX * i / 280;
      d += (i ? " L" : "M") + px(x).toFixed(1) + "," + py(F.f(x)).toFixed(1);
    }
    svg.appendChild(el("path", { d: d, fill: "none", stroke: C_F, "stroke-width": 1.9 }));

    var fa = F.f(a), g = F.d1(a), h = F.d2(a);
    var dm = "";
    for (i = 0; i <= 280; i++) {
      x = -RX + 2 * RX * i / 280;
      var u = x - a;
      dm += (i ? " L" : "M") + px(x).toFixed(1) + "," + py(fa + g * u + 0.5 * h * u * u).toFixed(1);
    }
    svg.appendChild(el("path", { d: dm, fill: "none", stroke: C_MOD, "stroke-width": 1.5, "stroke-dasharray": "5 4" }));

    var paso = Math.abs(h) > 1e-9 ? -g / h : 0;
    var xn = a + paso;
    svg.appendChild(el("circle", { cx: px(a).toFixed(1), cy: py(fa).toFixed(1), r: 4.2, fill: C_F }));
    if (Math.abs(h) > 1e-9 && Math.abs(xn) < RX * 1.6) {
      svg.appendChild(el("line", { x1: px(a).toFixed(1), y1: py(fa).toFixed(1),
        x2: px(Math.max(-RX, Math.min(RX, xn))).toFixed(1), y2: py(F.f(Math.max(-RX, Math.min(RX, xn)))).toFixed(1),
        stroke: C_PASO, "stroke-width": 1.6, "stroke-dasharray": "3 3" }));
      svg.appendChild(el("circle", { cx: px(Math.max(-RX, Math.min(RX, xn))).toFixed(1),
        cy: py(F.f(Math.max(-RX, Math.min(RX, xn)))).toFixed(1), r: 4.2, fill: C_PASO }));
    }
    svg.appendChild(el("text", { x: X0 + 8, y: 42, "font-size": 10, fill: C_MOD }, "— — el modelo cuadrático en x"));
    svg.appendChild(el("text", { x: X0 + 8, y: 56, "font-size": 10, fill: C_PASO }, "· · · a dónde lleva el paso de Newton"));

    var sube = F.f(xn) > fa + 1e-9;
    lec.innerHTML = "<span class=\"fx\">f′(x) = " + g.toFixed(3).replace(".", ",") +
      "   ·   f″(x) = " + h.toFixed(3).replace(".", ",") +
      "   ·   paso = " + paso.toFixed(3).replace(".", ",") +
      "   ·   f(x) = " + fa.toFixed(3).replace(".", ",") + " → " + F.f(xn).toFixed(3).replace(".", ",") + "</span><br>" +
      (h > 0
        ? "Con f″ > 0 la parábola abre hacia arriba, tiene vértice, y el paso de Newton salta directo a él: eso es la Proposición 17.2."
        : "Con f″ < 0 la parábola abre hacia abajo y no tiene mínimo. El paso sigue resolviendo ∇m = 0, pero ese punto es el máximo del modelo, " +
          "así que Newton apunta " + (sube ? "cuesta arriba" : "a un sitio que el modelo no justifica") + ". Es el fallo que arregla la Definición 17.5.");
  }

  cf.addEventListener("change", dibujar);
  cx.addEventListener("input", dibujar);
  dibujar();
})();
</script>
```

## 2. Cerca del óptimo, los dígitos se duplican

::: {.proposicion}
**Proposición 17.4 (convergencia cuadrática local).** Sea $x^\star$ un punto crítico con $H_f(x^\star)$ definida positiva de menor eigenvalor $\mu>0$, y sea $H_f$ Lipschitz de constante $L$ en un entorno de $x^\star$. Entonces existen $\delta>0$ y $C>0$ tales que, si $\|e_k\|<\delta$,
$$\|e_{k+1}\| \;\le\; C\,\|e_k\|^2, \qquad\text{con } C\approx\frac{L}{2\mu}.$$
:::

::: {.demostracion}
*Demostración.* Restando $x^\star$ en la Definición 17.1 y sacando $H_k^{-1}$ factor común,
$$e_{k+1} = e_k - H_k^{-1}\nabla f(x_k) = H_k^{-1}\big[H_ke_k-\nabla f(x_k)\big].$$
Como $\nabla f(x^\star)=0$, el teorema fundamental del cálculo aplicado a $t\mapsto\nabla f(x^\star+te_k)$ da
$$\nabla f(x_k) = \int_0^1 H_f(x^\star+te_k)\,e_k\,dt,$$
de donde
$$H_ke_k-\nabla f(x_k) = \int_0^1\big[H_f(x_k)-H_f(x^\star+te_k)\big]e_k\,dt.$$
El punto $x^\star+te_k$ dista de $x_k$ exactamente $(1-t)\|e_k\|$, así que por la hipótesis de Lipschitz la norma del corchete no pasa de $L(1-t)\|e_k\|$ y
$$\big\|H_ke_k-\nabla f(x_k)\big\| \;\le\; \int_0^1 L(1-t)\|e_k\|^2\,dt = \frac{L}{2}\,\|e_k\|^2.$$
Falta acotar $H_k^{-1}$. Por la Proposición 12.4, para una matriz simétrica definida positiva la norma de la inversa es el recíproco del menor eigenvalor; y como las segundas parciales son continuas, ese menor eigenvalor está cerca de $\mu$ si $x_k$ está cerca de $x^\star$: tomando $\delta$ bastante pequeño se puede asegurar que pasa de $\mu/2$. Juntando las dos cotas,
$$\|e_{k+1}\| \;\le\; \frac{1}{\mu/2}\cdot\frac{L}{2}\|e_k\|^2 = \frac{L}{\mu}\|e_k\|^2,$$
que es la forma anunciada con $C=L/\mu$; cuando $x_k$ está tan cerca que $H_k$ es prácticamente $H_f(x^\star)$, la constante baja hasta $L/2\mu$. [∎]{.fin-demo}
:::

Elevar al cuadrado el error significa **duplicar los dígitos correctos en cada paso**: de tres decimales a seis, y de seis a doce. El descenso de gradiente de la Proposición 16.5 se comporta de otro modo: multiplica el error por un factor fijo y por tanto gana siempre el mismo número de dígitos por paso.

```{pyodide}
import numpy as np

def grad(p):
    x, y = p
    return np.array([np.exp(x) + 2*x + 0.3*y, 2*y + 0.3*x])

def hess(p):
    x, y = p
    return np.array([[np.exp(x) + 2.0, 0.3], [0.3, 2.0]])

# el minimizador, hallado con Newton hasta precision de maquina
x_est = np.zeros(2)
for _ in range(60):
    x_est = x_est - np.linalg.solve(hess(x_est), grad(x_est))

x = np.array([2.5, -2.0])
print(f"{'paso':>5s}  {'||e_k||':>12s}  {'||e_k|| / ||e_{k-1}||^2':>24s}")
prev = None
for k in range(6):
    e = np.linalg.norm(x - x_est)
    if prev is None:
        print(f"{k:5d}  {e:12.3e}  {'—':>24s}")
    else:
        print(f"{k:5d}  {e:12.3e}  {e/prev**2:24.4f}")
    prev = e
    x = x - np.linalg.solve(hess(x), grad(x))
print(f"    6  {'por debajo de 1e-15':>12s}")
print()
print("la razon ||e_k||/||e_{k-1}||^2 se queda acotada: eso es convergencia cuadratica")
print("y el numero de digitos correctos se duplica en cada paso")
```

La columna de la derecha se queda entre $0{,}13$ y $0{,}20$ en lugar de crecer, que es lo que afirma la Proposición 17.4. La izquierda cuenta la historia: $3{,}5$, luego $1{,}7$, luego $0{,}55$, y a partir de ahí $0{,}049$, $3\cdot10^{-4}$, $1{,}3\cdot10^{-8}$ y precisión de máquina. Los tres primeros pasos son lentos porque el arranque está lejos; los tres últimos son el régimen cuadrático.

## 3. Lejos del óptimo el método se pierde

La Proposición 17.2 pide que $H_k$ sea definida positiva. Cuando no lo es, el modelo cuadrático no tiene mínimo y resolver $\nabla m=0$ localiza un máximo o una silla del modelo. El método sigue dando un paso, pero ese paso ya no significa lo que se quería.

```{pyodide}
import numpy as np

def f(p):
    x, y = p
    return x**4 - 4*x*y + y**4          # la de la leccion 14: silla en el origen

def grad(p):
    x, y = p
    return np.array([4*x**3 - 4*y, 4*y**3 - 4*x])

def hess(p):
    x, y = p
    return np.array([[12*x*x, -4.0], [-4.0, 12*y*y]])

for x0 in [np.array([0.9, 0.9]), np.array([0.2, -0.15]), np.array([0.05, 0.04])]:
    x = x0.copy()
    subio = False
    for _ in range(40):
        antes = f(x)
        try:
            x = x - np.linalg.solve(hess(x), grad(x))
        except np.linalg.LinAlgError:
            break
        if f(x) > antes + 1e-12:
            subio = True
    lam0 = np.linalg.eigvalsh(hess(x0))
    print(f"arranque ({x0[0]:5.2f},{x0[1]:6.2f})  hessiana definida positiva ahi: "
          f"{str(bool(lam0.min() > 0)):5s}  ->  llega a ({x[0]:6.3f},{x[1]:6.3f})  "
          f"f = {f(x):8.4f}   subio alguna vez: {subio}")
print()
print("el minimo vale -2; el origen es una silla y vale 0")
```

Desde $(0{,}9,\,0{,}9)$, donde la hessiana sí es definida positiva, Newton encuentra el mínimo. Desde los otros dos arranques termina **en la silla**, con $f=0$ en lugar de $-2$, y desde el tercero además sube en algún paso. El método converge, pero a un punto crítico que no es un mínimo: anular el gradiente es lo único que Newton persigue de verdad.

::: {.definicion}
**Definición 17.5 (Newton regularizado y amortiguado).** Con $\mu\ge0$ el menor valor que hace $H_k+\mu I$ definida positiva y $t\in(0,1]$ un factor de amortiguación,
$$x_{k+1} = x_k - t\,\big(H_k+\mu I\big)^{-1}\nabla f(x_k).$$
:::

Los dos ingredientes arreglan cosas distintas. El **regularizador** $\mu$ garantiza, por el criterio espectral de la Proposición 12.4, que la matriz sea definida positiva y que por tanto la Proposición 17.2 vuelva a aplicarse: el paso apunta al mínimo de un modelo que sí lo tiene. La **amortiguación** $t$ se recorta a la mitad hasta que la función baje de verdad, lo que la Proposición 16.2 garantiza que ocurre con $t$ suficientemente pequeño. Con $\mu=0$ y $t=1$ se recupera la Definición 17.1.

```{pyodide}
import numpy as np

def f(p):
    x, y = p
    return x**4 - 4*x*y + y**4

def grad(p):
    x, y = p
    return np.array([4*x**3 - 4*y, 4*y**3 - 4*x])

def hess(p):
    return np.array([[12*p[0]*p[0], -4.0], [-4.0, 12*p[1]*p[1]]])

def newton_regularizado(x0, pasos=60):
    x = x0.copy()
    for _ in range(pasos):
        H, g = hess(x), grad(x)
        lam_min = np.linalg.eigvalsh(H).min()
        mu = 0.0 if lam_min > 1e-6 else (1e-6 - lam_min)     # la vuelve definida positiva
        paso = np.linalg.solve(H + mu*np.eye(2), g)
        t = 1.0                                              # amortiguacion: recortar hasta que baje
        while t > 1e-10 and f(x - t*paso) > f(x):
            t /= 2
        x = x - t*paso
    return x

for x0 in [np.array([0.9, 0.9]), np.array([0.2, -0.15]), np.array([0.05, 0.04])]:
    x = newton_regularizado(x0)
    print(f"arranque ({x0[0]:5.2f},{x0[1]:6.2f})  ->  ({x[0]:6.3f},{x[1]:6.3f})   f = {f(x):8.4f}")
print()
print("los tres arranques terminan en un minimo de verdad, con f = -2")
```

Los tres arranques que antes se repartían entre la silla y el mínimo ahora llegan los tres a un mínimo con $f=-2$. El precio es que cerca del óptimo, donde $\mu=0$ y $t=1$, el método vuelve a ser Newton puro y conserva la convergencia cuadrática; lejos, se comporta como un descenso con paso corto.

```{=html}
<div class="viz-newton2d">
  <div class="controles">
    <label>Condición κ <input id="n2-k" type="range" min="2" max="40" step="2" value="20"> <span id="n2-k-v">20</span></label>
    <label>Pasos <input id="n2-n" type="range" min="0" max="40" step="1" value="12"> <span id="n2-n-v">12</span></label>
  </div>
  <svg id="n2-svg" viewBox="0 0 620 320" role="img" aria-label="Curvas de nivel de una cuadratica con dos caminos desde el mismo arranque: el descenso de gradiente zigzagueando y el paso de Newton llegando al centro de un salto. A la derecha el error de los dos en escala logaritmica."></svg>
  <p class="lectura" id="n2-lectura"></p>
</div>
<style>
.viz-newton2d { border: 1px solid var(--r-linea, #ddd); border-radius: 10px; padding: 1rem 1.1rem; margin: 1.7rem 0; }
.viz-newton2d .controles { display: flex; flex-wrap: wrap; gap: .45rem 1.1rem; align-items: center; margin-bottom: .85rem; font-size: .85rem; }
.viz-newton2d label { display: flex; align-items: center; gap: .4rem; }
.viz-newton2d input[type=range] { width: 122px; padding: 0; }
.viz-newton2d svg { width: 100%; height: auto; display: block; }
.viz-newton2d .lectura { font-size: .85rem; margin-top: .75rem; min-height: 5em; line-height: 1.6; }
.viz-newton2d .lectura .fx { font-family: var(--bs-font-monospace, monospace); font-size: .95em; }
</style>
<script>
(function () {
  var C_NIVEL = "var(--r-tinta,#1f2430)", C_GRAD = "#B45309", C_NEW = "#0F6E68";
  var svg = document.getElementById("n2-svg"), lec = document.getElementById("n2-lectura");
  var ck = document.getElementById("n2-k"), ek = document.getElementById("n2-k-v");
  var cn = document.getElementById("n2-n"), en = document.getElementById("n2-n-v");

  // Marco constante en los dos paneles.
  var LCX = 162, LCY = 160, LESC = 96, LR = 1.45;
  var RX0 = 355, RX1 = 602, RY0 = 292, RY1 = 28;
  var MAXP = 40, LOGMIN = -17, LOGMAX = 1;
  var AX = 1.15, AY = 0.92;

  function el(t, at, txt) {
    var e = document.createElementNS("http://www.w3.org/2000/svg", t);
    for (var k in at) e.setAttribute(k, at[k]);
    if (txt !== undefined) e.textContent = txt;
    return e;
  }
  function lx(x) { return LCX + x * LESC; }
  function ly(y) { return LCY - y * LESC; }
  function qx(k) { return RX0 + k / MAXP * (RX1 - RX0); }
  function qy(lg) { return RY0 - (Math.max(LOGMIN, Math.min(LOGMAX, lg)) - LOGMIN) / (LOGMAX - LOGMIN) * (RY0 - RY1); }

  function dibujar() {
    while (svg.firstChild) svg.removeChild(svg.firstChild);
    var kap = +ck.value, pasos = +cn.value;
    ek.textContent = kap;
    en.textContent = pasos;
    var eta = 2 / (1 + kap);                 // paso optimo de la leccion 16

    svg.appendChild(el("rect", { x: lx(-LR), y: ly(LR), width: 2 * LR * LESC, height: 2 * LR * LESC, fill: "none", stroke: "var(--r-linea,#ddd)" }));
    [0.06, 0.25, 0.6, 1.1].forEach(function (c) {
      svg.appendChild(el("ellipse", { cx: lx(0).toFixed(1), cy: ly(0).toFixed(1),
        rx: (Math.sqrt(2 * c) * LESC).toFixed(1), ry: (Math.sqrt(2 * c / kap) * LESC).toFixed(1),
        fill: "none", stroke: C_NIVEL, "stroke-width": 1, "stroke-opacity": 0.32 }));
    });

    // descenso de gradiente
    var x = AX, y = AY, dg = "M" + lx(x).toFixed(1) + "," + ly(y).toFixed(1), eg = [Math.hypot(x, y)];
    for (var i = 0; i < pasos; i++) {
      x = x - eta * x; y = y - eta * kap * y;
      eg.push(Math.hypot(x, y));
      dg += " L" + lx(x).toFixed(1) + "," + ly(y).toFixed(1);
    }
    svg.appendChild(el("path", { d: dg, fill: "none", stroke: C_GRAD, "stroke-width": 1.5 }));

    // Newton: un paso exacto al centro (la hessiana es diag(1, kappa))
    var en_ = [Math.hypot(AX, AY)];
    for (i = 0; i < pasos; i++) en_.push(0);
    var dn = "M" + lx(AX).toFixed(1) + "," + ly(AY).toFixed(1);
    if (pasos > 0) dn += " L" + lx(0).toFixed(1) + "," + ly(0).toFixed(1);
    svg.appendChild(el("path", { d: dn, fill: "none", stroke: C_NEW, "stroke-width": 2, "stroke-dasharray": "6 3" }));

    svg.appendChild(el("circle", { cx: lx(AX).toFixed(1), cy: ly(AY).toFixed(1), r: 4, fill: "var(--r-tinta,#222)" }));
    svg.appendChild(el("circle", { cx: lx(0).toFixed(1), cy: ly(0).toFixed(1), r: 3.4, fill: C_NEW }));
    svg.appendChild(el("text", { x: lx(-LR), y: 18, "font-size": 11, "font-weight": 600, fill: "var(--r-tinta,#222)" }, "f(x,y) = (x² + κy²)/2"));
    svg.appendChild(el("text", { x: lx(-LR) + 4, y: 34, "font-size": 10, fill: C_GRAD }, "— descenso de gradiente"));
    svg.appendChild(el("text", { x: lx(-LR) + 4, y: 47, "font-size": 10, fill: C_NEW }, "— — Newton"));

    // panel derecho
    svg.appendChild(el("rect", { x: RX0, y: RY1, width: RX1 - RX0, height: RY0 - RY1, fill: "none", stroke: "var(--r-linea,#ddd)" }));
    [[eg, C_GRAD, 1.7, "none"], [en_, C_NEW, 1.9, "6 3"]].forEach(function (par) {
      var d = "";
      for (var j = 0; j < par[0].length; j++) {
        d += (j ? " L" : "M") + qx(j).toFixed(1) + "," + qy(Math.log10(Math.max(par[0][j], 1e-300))).toFixed(1);
      }
      svg.appendChild(el("path", { d: d, fill: "none", stroke: par[1], "stroke-width": par[2], "stroke-dasharray": par[3] }));
    });
    svg.appendChild(el("text", { x: RX0, y: 18, "font-size": 11, "font-weight": 600, fill: "var(--r-tinta,#222)" }, "log₁₀‖eₖ‖ de los dos métodos"));

    lec.innerHTML = "<span class=\"fx\">κ = " + kap + "   ·   η óptimo = " + eta.toFixed(4).replace(".", ",") +
      "   ·   tras " + pasos + " pasos:  descenso ‖e‖ = " + eg[eg.length - 1].toExponential(2) +
      "   ·   Newton ‖e‖ = " + (pasos > 0 ? "0" : eg[0].toExponential(2)) + "</span><br>" +
      (pasos === 0
        ? "Con cero pasos los dos están en el arranque: mover el número de pasos separa las dos curvas."
        : "Newton llega al centro en el primer paso y se queda ahí, sea cual sea κ (Proposición 17.3). El descenso necesita " +
          "tantos más pasos cuanto mayor es κ, porque su factor de contracción es (κ−1)/(κ+1).");
  }

  ck.addEventListener("input", dibujar);
  cn.addEventListener("input", dibujar);
  dibujar();
})();
</script>
```

## Ejercicios

### Ejercicio 1, El mismo paso, dos problemas distintos

Newton para hallar raíces resuelve $f(x)=0$ con $x_{k+1}=x_k-f(x_k)/f'(x_k)$; Newton para optimizar resuelve $f'(x)=0$. Comprobar que el segundo es el primero aplicado a la derivada, y que los dos duplican dígitos.

```{pyodide}
def f(x): return x**3 - 2*x - 5
def fp(x): return 3*x**2 - 2

x = 2.0
print("Newton para la raiz de f(x) = x^3 - 2x - 5:")
for k in range(5):
    print(f"  paso {k}:  x = {x:.12f}   f(x) = {f(x):12.3e}")
    x = x - f(x)/fp(x)

def g(x): return x**4 - 3*x**2 + x        # ahora se minimiza
def gp(x): return 4*x**3 - 6*x + 1
def gpp(x): return 12*x**2 - 6

y = 1.5
print()
print("Newton para el minimo de g(x) = x^4 - 3x^2 + x:")
for k in range(5):
    print(f"  paso {k}:  x = {y:.12f}   g'(x) = {gp(y):12.3e}")
    y = y - gp(y)/gpp(y)
print()
print("el segundo es el primero aplicado a g': anular la derivada es hallar su raiz")
```

### Ejercicio 2, Cuándo conviene cada método

Newton necesita muchísimos menos pasos, pero cada paso resuelve un sistema $n\times n$: del orden de $n^3/3$ operaciones, contra las $2n^2$ de un producto matriz-vector. Encontrar dónde está el cruce.

```{pyodide}
import numpy as np

rng = np.random.default_rng(171)

def pasos_descenso(n, kappa, obj=1e-10):
    Q = np.linalg.qr(rng.normal(size=(n, n)))[0]
    A = Q @ np.diag(np.linspace(1.0, kappa, n)) @ Q.T
    b = rng.normal(size=n)
    xe = np.linalg.solve(A, b)
    umbral = obj*np.linalg.norm(xe)
    eta = 2.0/(1.0 + kappa)
    x, k = np.zeros(n), 0
    while np.linalg.norm(x - xe) > umbral and k < 500000:
        x = x - eta*(A @ x - b); k += 1
    return k

print(f"{'n':>5s} {'kappa':>7s}  {'Newton':>7s} {'descenso':>9s}  "
      f"{'coste Newton':>13s} {'coste descenso':>15s}  {'gana':>9s}")
registro = {}
for n, kappa in [(10, 10.0), (10, 1000.0), (300, 10.0), (300, 1000.0)]:
    pd = pasos_descenso(n, kappa)
    registro[kappa] = pd
    cn, cd = n**3/3.0, pd*2.0*n**2
    print(f"{n:5d} {kappa:7.0f}  {1:7d} {pd:9d}  {cn:13.3e} {cd:15.3e}  "
          f"{'Newton' if cn < cd else 'descenso':>9s}")
print()
print("los pasos del descenso apenas dependen de n, asi que el cruce sale de")
print("n^3/3 = pasos * 2n^2, es decir n = 6 * pasos:")
for kappa, pd in sorted(registro.items()):
    print(f"  con kappa = {kappa:6.0f}  el descenso gana a partir de n = {6*pd}")
```

En los cuatro casos medidos gana Newton. El cruce está en $n=6\cdot(\text{pasos del descenso})$: con $\kappa=10$ hace falta $n$ por encima de unos $650$ para que convenga el descenso, y con $\kappa=1000$ habría que llegar a $n\approx68\,000$. Por eso en dimensión moderada y problemas mal condicionados Newton es la opción, y en dimensión muy alta, donde ni la hessiana cabe en memoria, no lo es.

## Contraste con la librería

Esta lección es de nivel **L3**. El método de Newton se implementa arriba en dos líneas; aquí se aplica a la función de Rosenbrock (no cuadrática, y el banco de pruebas clásico) y se enfrenta al `Newton-CG` de `scipy`.

```{pyodide}
import numpy as np
from scipy.optimize import minimize

def f(z):    return (1 - z[0])**2 + 100 * (z[1] - z[0]**2)**2
def grad(z): return np.array([-2*(1 - z[0]) - 400*z[0]*(z[1] - z[0]**2),
                              200*(z[1] - z[0]**2)])
def hess(z): return np.array([[2 - 400*(z[1] - 3*z[0]**2), -400*z[0]],
                              [-400*z[0], 200.0]])

optimo = np.array([1.0, 1.0])
z = np.array([-1.2, 1.0])

print("Newton a mano, distancia al optimo en cada iteracion:")
errores = []
for k in range(60):
    z = z - np.linalg.solve(hess(z), grad(z))    # el paso de Newton
    e = float(np.linalg.norm(z - optimo))
    errores.append(e)
    if k < 7:
        print("   iteracion %d: %.3e" % (k + 1, e))
    if e < 1e-14:
        break

print("   converge en %d iteraciones a %s" % (len(errores), np.round(z, 12)))
print()

r = minimize(f, np.array([-1.2, 1.0]), jac=grad, hess=hess, method="Newton-CG", tol=1e-12)
print("scipy Newton-CG: %s  en %d iteraciones" % (np.round(r.x, 8), r.nit))
print()
print("los dos llegan al mismo punto:", bool(np.abs(r.x - optimo).max() < 1e-6))
print("el de mano usa menos iteraciones:", bool(len(errores) < r.nit))
```

La columna de errores es la que conviene mirar. Entre las iteraciones cuarta y sexta pasa de $5{,}6\times10^{-2}$ a $9{,}6\times10^{-6}$ y de ahí a $1{,}9\times10^{-11}$: **el número de dígitos correctos se duplica en cada paso.** Esa es la convergencia cuadrática de la Proposición 17.4, y se ve sin necesidad de medir nada más.

El contraste con la librería sorprende al revés de lo habitual: **la versión de mano converge en menos iteraciones que la de `scipy`**. No es que la librería sea peor. `Newton-CG` no resuelve el sistema de Newton exactamente, sino que lo aproxima con gradiente conjugado, y por eso cada una de sus iteraciones cuesta mucho menos que la de aquí. En dos dimensiones resolver el sistema exacto es trivial y gana el método directo; en un problema con miles de variables, formar y factorizar la hessiana en cada paso deja de ser posible, y ahí la aproximación es la única vía.

## Reto

En `proyectos/notebooks/F1-retos.ipynb`, sección **Mat 17**:

1. Implementar `newton(f, grad, hess, x0)` con regularización y amortiguación como en la Definición 17.5, registrando en cada paso $\mu$, $t$ y el menor eigenvalor. Correrlo sobre $x^4-4xy+y^4$ desde una rejilla de arranques y dibujar de qué color es la cuenca de cada mínimo.
2. Reproducir la tabla de la Proposición 17.4 con una función de tres variables y verificar que $\|e_{k+1}\|/\|e_k\|^2$ se estabiliza en un número cercano a $L/2\mu$, estimando $L$ por diferencias de hessianas.
3. Implementar Newton con hessiana aproximada por diferencias finitas del gradiente y medir cuántos dígitos se pierden frente a la hessiana exacta. Anotar en `50-Errores/` a partir de qué paso de diferenciación la convergencia cuadrática se degrada a lineal.

## Del libro

*Mathematics for Machine Learning* trata la optimización continua en §7.1 *Optimization Using Gradient Descent*, donde presenta el descenso de gradiente y menciona los métodos que usan información de segundo orden. El libro tiene copyright y no es descargable desde el entorno en que se escribió esta página, así que se cita número y título y no se le atribuye ninguna frase.

**Preguntas para leer §7.1 con lápiz:**

1. La sección trata el descenso de gradiente con paso fijo, cuya tasa depende del número de condición. Con la Proposición 17.3, ¿por qué multiplicar por $H^{-1}$ elimina esa dependencia, y qué relación tiene eso con reescalar las coordenadas para que las curvas de nivel sean circunferencias?
2. Los métodos cuasi-Newton, BFGS y L-BFGS, aproximan $H^{-1}$ sin calcularla. Con el Ejercicio 2, ¿por qué esa aproximación es interesante justo en el régimen donde Newton pierde, y qué coste por iteración habría que conseguir para ganarle al descenso?
3. Con la Proposición 17.2, ¿qué le pasa al paso de Newton si la hessiana es definida positiva pero está mal condicionada? Distinguir entre que el paso apunte mal y que su cálculo pierda precisión.

## Para el Cerebro

Nota nueva en `10-Conceptos/metodo-de-newton.md`, enlazada a [[descenso-de-gradiente]], [[hessiana]] y [[convexidad]]. Conviene rehacer a mano la Proposición 17.2: el paso de Newton no se postula, se deduce de minimizar el modelo cuadrático, y ese único argumento explica a la vez por qué funciona cerca del óptimo y por qué falla lejos.

```
¿Cuál es el paso de Newton?::x_{k+1} = x_k − H_k⁻¹∇f(x_k)
¿De dónde sale ese paso?::De minimizar el modelo cuadrático de Taylor: ∇m(h) = ∇f + Hh = 0
¿Qué hipótesis necesita?::Que la hessiana sea definida positiva; si no, el modelo no tiene mínimo
¿Cuántos pasos necesita en una cuadrática?::Uno, desde cualquier arranque y sea cual sea κ
¿Por qué desaparece el número de condición?::Porque H⁻¹ deshace exactamente el estiramiento que κ mide
¿Qué es la convergencia cuadrática?::‖e_{k+1}‖ ≤ C‖e_k‖²: el número de dígitos correctos se duplica cada paso
¿Cuánto vale esa constante C?::Del orden de L/2μ, con L el Lipschitz de la hessiana y μ su menor eigenvalor en el óptimo
¿Qué persigue Newton en realidad?::Anular el gradiente; puede converger a una silla y no al mínimo
¿Qué hace el regularizador μ?::Sumar μI para que H+μI sea definida positiva y el modelo vuelva a tener mínimo
¿Qué hace la amortiguación t?::Recortar el paso hasta que la función baje de verdad
¿Cuándo conviene Newton y cuándo el descenso?::Newton en dimensión moderada o mal condicionado; el descenso cuando n supera unas 6 veces los pasos que necesitaría
```

::: {#fuentes .fuentes}
### Fuentes

**Lo que esta página demuestra sola.** Las Proposiciones 17.2, 17.3 y 17.4 se demuestran aquí a partir del modelo cuadrático de la lección 14, de la convexidad estricta de la 15 y del criterio espectral de la 12. Se comprueban además numéricamente: que un paso de Newton resuelve una cuadrática con error por debajo de $10^{-13}$ para $\kappa$ igual a 2, 50 y 5000, frente a las 22, 648 y 77 652 iteraciones que necesita el descenso de gradiente para una tolerancia fija de $10^{-10}$; la convergencia cuadrática con la razón $\|e_k\|/\|e_{k-1}\|^2$ estable entre $0{,}13$ y $0{,}20$ mientras el error cae de $3{,}5$ a precisión de máquina en seis pasos; que desde dos de tres arranques Newton puro termina en la silla de $x^4-4xy+y^4$ con $f=0$ en lugar de $-2$, y que desde uno de ellos sube en algún paso; que la versión regularizada y amortiguada lleva los tres arranques a un mínimo verdadero; la equivalencia entre Newton para raíces y Newton para optimizar; y el cruce de coste total en $n=6\cdot(\text{pasos del descenso})$. Los dos visuales se contrastaron antes de publicarse contra las fórmulas en todo el rango de sus controles.

**Lo que se usa de otras lecciones sin repetir.** El modelo cuadrático y que su hessiana es constante son la Definición 14.4 y el Ejercicio 2 de la lección 14. La condición necesaria de primer orden es la Proposición 14.7. Que hessiana definida positiva implica convexidad estricta y minimizador único son las Proposiciones 15.4 y 15.6. Que la norma de la inversa de una simétrica definida positiva es el recíproco de su menor eigenvalor sale de la Proposición 12.4. El paso óptimo $2/(\lambda_1+\lambda_n)$ y la tasa $(\kappa-1)/(\kappa+1)$ con que se compara son la Proposición 16.5, y que un paso corto siempre baja es la 16.2.

**Lo que se enuncia sin demostrar.** El teorema fundamental del cálculo en forma vectorial, usado en la Proposición 17.4 para escribir $\nabla f(x_k)$ como integral de hessianas. En la misma demostración se usa la desigualdad de la norma de una integral sin justificarla, y se afirma que el menor eigenvalor de $H_k$ pasa de $\mu/2$ cerca de $x^\star$ apelando a la continuidad sin precisar el $\delta$. La convergencia global del Newton regularizado de la Definición 17.5 se comprueba en tres arranques pero no se demuestra.

**Lo que viene de los libros.**

- [*Mathematics for Machine Learning*](https://mml-book.github.io/book/mml-book.pdf) (Deisenroth, Faisal & Ong, Cambridge University Press, 2020), §7.1 *Optimization Using Gradient Descent* — citada por número y título, sin transcribir texto.

**Lo que es mío, no del libro.** Presentar el paso de Newton como consecuencia de la Proposición 17.2 en lugar de como una fórmula dada, para que el fallo de la sección 3 se lea como la misma proposición aplicada donde su hipótesis no vale. La celda que mide el fallo con tres arranques del ejemplo de la lección 14, incluida la comprobación de que el método sube en algún paso. Y el cálculo del cruce de coste dentro de la propia celda, en lugar de afirmarlo: la regla $n=6\cdot\text{pasos}$ sale de igualar $n^3/3$ con $2n^2$ por iteración.

[Índices verificados el 12-09-2026 contra el índice publicado del PDF oficial.]{.sello}
:::

→ Siguiente: [Multiplicadores de Lagrange y condiciones KKT](18-lagrange-kkt.qmd)
