#!/bin/sh
# Sube lo que haya pendiente. Uso:  ./subir
cd "$(dirname "$0")" || exit 1
if [ -n "$(git status --porcelain)" ]; then
  echo "Hay cambios sin commitear:"
  git status --short
  echo
  echo "Commitealos primero, o pideselo a Claude."
  exit 1
fi
git push origin main
