---
title: "Intervalos de confianza: lo que prometen y cuándo fallan"
subtitle: "Estadística · Lección 9"
nivel: L2
modulo: estadistica
fase: F1
prereqs: [estadistica/07-estimacion, estadistica/06-teorema-central-limite]
siguientes: [estadistica/10-pruebas-de-hipotesis]
libro: "Think Stats 3e §8.6 Confidence Intervals"
engine: markdown
pyodide:
  packages:
    - numpy
---

::: {.meta-leccion data-leccion="estadistica/09-intervalos-de-confianza"}
[[L2]{.nivel .nivel-L2} **Derivación**]{.item}
[**Fase** F1]{.item}
[**Libro** TS 3e §8.6]{.item}
[**Prereqs** [Estimación](07-estimacion.qmd) · [Teorema central del límite](06-teorema-central-limite.qmd)]{.item}
:::

## Objetivo

Al terminar esta lección se puede:

1. Definir [intervalo de confianza]{.g data-t="intervalo de confianza"} y decir con precisión qué es aleatorio en él y qué no.
2. Construir el intervalo para la media con $\sigma$ conocida y demostrar que su [cobertura]{.g} es exactamente la nominal.
3. Explicar por qué «hay un 95 % de probabilidad de que $\mu$ esté en este intervalo» no es lo que afirma el método.
4. Distinguir cobertura nominal de cobertura real, y medirla en dos casos clásicos donde no coinciden.

:::: {.hilo}
::: {}
#### De dónde viene

- **Lección 1**: la [distribución muestral]{.g} de $\bar{x}$ (Definición 1.6) es el objeto que se recorta para formar el intervalo.
- **Lección 6**: la Proposición 6.1 dio $\text{Var}(\bar{x}) = \sigma^2/n$, y el teorema central del límite dio la forma normal. Sin esos dos resultados no hay fórmula.
- **Lección 7**: el intervalo se construye alrededor de un estimador; el [error estándar]{.g data-t="error estándar"} que fija su anchura es la raíz de la varianza de la Proposición 7.5.
:::

::: {}
#### Para qué sirve después

- **Lección 10**: un intervalo de confianza y una prueba de hipótesis son el mismo objeto visto de dos maneras: el intervalo contiene exactamente los valores que la prueba no rechaza.
- **Lección 11**: el bootstrap construye intervalos sin fórmula, cuando la distribución muestral no se conoce.
- **Lección 14**: los intervalos para los coeficientes de una regresión son este mismo cálculo, con el error estándar que sale de la matriz de diseño.
- **Lección 17**: la mayor parte de los errores de interpretación que se catalogan allí son la confusión que ataca la sección 3 de esta lección.
:::
::::

## Notación

::: {.notacion}
| Símbolo | Se lee | Significado |
|---|---|---|
| $\alpha$ | alfa | La proporción de veces que se admite fallar; $1-\alpha$ es la confianza |
| $z_{\alpha/2}$ | zeta sub alfa medios | El número tal que una normal estándar cae en $[-z_{\alpha/2}, z_{\alpha/2}]$ con probabilidad $1-\alpha$ |
| $\Phi$ | fi mayúscula | La función acumulada de la normal estándar (Definición 3.6) |
| $\text{EE}$ | error estándar | La desviación típica del estimador, $\sigma/\sqrt{n}$ para $\bar{x}$ |
| $T_\nu$ | te sub nu | Una variable con distribución $t$ de Student de $\nu$ grados de libertad |
| $\nu$ | nu | Grados de libertad; aquí siempre $n-1$ |
| $\hat{p}$ | pe sombrero | La proporción muestral, $k/n$ |
:::

Una advertencia de lectura antes de empezar. En $P(A \le \theta \le B) = 1-\alpha$, las letras aleatorias son $A$ y $B$; $\theta$ es una constante desconocida. La probabilidad se refiere a dónde caen los extremos, no a dónde está $\theta$. Toda la sección 3 desarrolla esta frase.

## 1. La definición

::: {.definicion}
**Definición 9.1 (intervalo de confianza).** Sean $A = A(x_1,\dots,x_n)$ y $B = B(x_1,\dots,x_n)$ dos estadísticos con $A \le B$. El intervalo aleatorio $[A,B]$ es un intervalo de confianza de nivel $1-\alpha$ para $\theta$ si
$$P\big(A \le \theta \le B\big) = 1-\alpha$$
**para todo** valor posible de $\theta$. A esa probabilidad se la llama [cobertura]{.g} del intervalo.
:::

Dos detalles de la definición conviene no pasarlos por alto. El primero: la igualdad se exige para todo $\theta$, no para uno. Un procedimiento que acierte el 95 % de las veces cuando $\theta = 0$ y el 60 % cuando $\theta = 7$ no es un intervalo de confianza del 95 %. El segundo: nada obliga a que la cobertura real coincida con la que el método anuncia. A la anunciada se la llama **nominal**, y las secciones 4 y 5 muestran dos casos de uso corriente donde la real queda por debajo.

::: {.cita-libro}
> An interval that contains the most likely values in a sampling distribution.
>
> [Downey, *Think Stats* 3e, §8.8 *Glossary*, definición de *confidence interval*]{.ref}
:::

Downey lo construye desde el otro extremo: en §8.6 toma la distribución muestral del estimador y le recorta los percentiles 5 y 95. Es el mismo objeto —la Proposición 9.3 hace exactamente ese recorte con fórmula en vez de con percentiles empíricos— y es el camino que sigue el bootstrap de la lección 11, que no necesita fórmula ninguna. Downey además señala en esa misma sección que la interpretación del intervalo es materia de discusión entre escuelas; la sección 3 se ocupa de qué es lo que sí se puede afirmar sin entrar en esa discusión.

## 2. El intervalo para la media, con $\sigma$ conocida

::: {.definicion}
**Definición 9.2 (cuantil normal).** $z_{\alpha/2} = \Phi^{-1}\!\big(1-\alpha/2\big)$, es decir, el punto que deja $\alpha/2$ de probabilidad en la cola derecha de una normal estándar.
:::

Para los niveles de uso corriente: $z_{0.05} = 1.6449$ al 90 %, $z_{0.025} = 1.9600$ al 95 %, $z_{0.005} = 2.5758$ al 99 %.

::: {.proposicion}
**Proposición 9.3.** Sea $x_1,\dots,x_n$ una muestra de una $\mathcal{N}(\mu,\sigma^2)$ con $\sigma$ **conocida**. Entonces
$$\Big[\;\bar{x} - z_{\alpha/2}\frac{\sigma}{\sqrt{n}}, \;\; \bar{x} + z_{\alpha/2}\frac{\sigma}{\sqrt{n}}\;\Big]$$
tiene cobertura exactamente $1-\alpha$, para todo $\mu$.
:::

::: {.demostracion}
*Demostración.* Por la Proposición 6.1, $E[\bar{x}] = \mu$ y $\text{Var}(\bar{x}) = \sigma^2/n$; y por ser combinación lineal de normales independientes, $\bar{x}$ es exactamente normal (no solo aproximadamente: aquí no hace falta el teorema central del límite). Luego la variable estandarizada
$$Z = \frac{\bar{x}-\mu}{\sigma/\sqrt{n}}$$
es $\mathcal{N}(0,1)$. Por la Definición 9.2, $P(-z_{\alpha/2} \le Z \le z_{\alpha/2}) = 1-\alpha$. Se desarrolla esa desigualdad despejando $\mu$, multiplicando por $\sigma/\sqrt{n} > 0$ —que no cambia el sentido— y restando $\bar{x}$:
$$-z_{\alpha/2} \le \frac{\bar{x}-\mu}{\sigma/\sqrt{n}} \le z_{\alpha/2}
\;\Longleftrightarrow\;
\bar{x} - z_{\alpha/2}\frac{\sigma}{\sqrt{n}} \le \mu \le \bar{x} + z_{\alpha/2}\frac{\sigma}{\sqrt{n}}.$$
Las dos desigualdades son equivalentes término a término, así que los dos sucesos son el mismo suceso y tienen la misma probabilidad, $1-\alpha$. El valor de $\mu$ no interviene en ningún paso, luego la igualdad vale para todo $\mu$. [∎]{.fin-demo}
:::

Merece la pena mirar qué se ha hecho en esa cadena de equivalencias: **se ha reordenado una desigualdad, nada más**. Al principio $\mu$ estaba en el centro y los extremos eran fijos; al final los extremos son aleatorios y $\mu$ está fijo en medio. Es el mismo suceso escrito de dos maneras. De ahí sale toda la sutileza de la sección siguiente.

El visual construye cien intervalos sobre cien muestras distintas de la misma población.

```{=html}
<div class="viz-ic">
  <div class="controles">
    <label>n por muestra
      <select id="ic-n"><option>5</option><option selected>20</option><option>80</option></select>
    </label>
    <label>Confianza
      <select id="ic-c">
        <option value="0.80">80 %</option>
        <option value="0.90">90 %</option>
        <option value="0.95" selected>95 %</option>
        <option value="0.99">99 %</option>
      </select>
    </label>
    <button id="ic-otra" type="button">Otras 100 muestras</button>
  </div>
  <svg id="ic-svg" viewBox="0 0 560 330" role="img" aria-label="Cien intervalos de confianza calculados sobre cien muestras distintas; los que no contienen la media verdadera aparecen marcados"></svg>
  <p class="lectura" id="ic-lectura"></p>
</div>
<style>
.viz-ic {
  border: 1px solid var(--r-linea, #ddd);
  border-radius: 10px; padding: 1rem 1.1rem; margin: 1.7rem 0;
}
.viz-ic .controles {
  display: flex; flex-wrap: wrap; gap: .45rem 1.1rem;
  align-items: center; margin-bottom: .85rem; font-size: .85rem;
}
.viz-ic label { display: flex; align-items: center; gap: .4rem; }
.viz-ic select {
  font-size: .85rem; padding: .2rem .4rem; border-radius: 5px;
  border: 1px solid var(--r-linea, #ccc); background: transparent; color: inherit;
}
.viz-ic button {
  font-size: .8rem; padding: .22rem .6rem; border-radius: 5px; cursor: pointer;
  border: 1px solid var(--r-linea, #ccc); background: transparent; color: inherit;
}
.viz-ic button:hover { border-color: var(--r-acento, #0F6E68); color: var(--r-acento, #0F6E68); }
.viz-ic svg { width: 100%; height: auto; display: block; }
.viz-ic .lectura { font-size: .85rem; margin-top: .75rem; min-height: 4.4em; line-height: 1.6; }
.viz-ic .lectura .fx { font-family: var(--bs-font-monospace, monospace); font-size: .95em; }
</style>
<script>
(function () {
  var C_OK = "#0F6E68", C_MAL = "#B91C1C", C_MU = "#1f2430";
  var Z = {"0.80": 1.2815515655, "0.90": 1.6448536270, "0.95": 1.9599639845, "0.99": 2.5758293035};
  var svg = document.getElementById("ic-svg"), lec = document.getElementById("ic-lectura");
  var sn = document.getElementById("ic-n"), scf = document.getElementById("ic-c");
  var btn = document.getElementById("ic-otra");
  var MU = 100, SIG = 15, K = 100, semilla = 20260912;
  var X0 = 40, X1 = 540, Y0 = 300, Y1 = 44;

  function el(t, at, txt) {
    var e = document.createElementNS("http://www.w3.org/2000/svg", t);
    for (var k in at) e.setAttribute(k, at[k]);
    if (txt !== undefined) e.textContent = txt;
    return e;
  }

  function dibujar() {
    var n = +sn.value, conf = scf.value, z = Z[conf], s = semilla, i, j;
    function u() {
      s |= 0; s = s + 0x6D2B79F5 | 0;
      var t = Math.imul(s ^ s >>> 15, 1 | s);
      t = t + Math.imul(t ^ t >>> 7, 61 | t) ^ t;
      return ((t ^ t >>> 14) >>> 0) / 4294967296;
    }
    function gauss() { var a = u() || 1e-12, b = u(); return Math.sqrt(-2*Math.log(a))*Math.cos(2*Math.PI*b); }

    var ee = SIG / Math.sqrt(n), semi = z * ee;
    var medias = [], cubren = 0;
    for (i = 0; i < K; i++) {
      var ac = 0;
      for (j = 0; j < n; j++) ac += MU + SIG * gauss();
      var m = ac / n;
      medias.push(m);
      if (Math.abs(m - MU) <= semi) cubren++;
    }

    // cobertura sobre muchas mas muestras, para separar la tanda del procedimiento
    var G = 20000, largas = 0;
    for (i = 0; i < G; i++) {
      var ac2 = 0;
      for (j = 0; j < n; j++) ac2 += MU + SIG * gauss();
      if (Math.abs(ac2 / n - MU) <= semi) largas++;
    }

    while (svg.firstChild) svg.removeChild(svg.firstChild);

    var rad = Math.max(semi * 1.55, 3.2 * ee);
    function sx(v) { return X0 + (v - (MU - rad)) / (2 * rad) * (X1 - X0); }

    svg.appendChild(el("text", {x: X0, y: 22, "font-size": 12, "font-weight": 600,
      fill: "var(--r-tinta, #222)"},
      "100 muestras de n = " + n + ", un intervalo por muestra"));

    // mu verdadera
    svg.appendChild(el("line", {x1: sx(MU), y1: 30, x2: sx(MU), y2: Y0 + 6,
      stroke: C_MU, "stroke-width": 1.6, "stroke-dasharray": "5 4"}));
    svg.appendChild(el("text", {x: sx(MU) + 6, y: 30, "font-size": 11, fill: C_MU}, "μ = 100"));

    var paso = (Y0 - Y1) / K;
    for (i = 0; i < K; i++) {
      var y = Y1 + i * paso, m2 = medias[i];
      var ok = Math.abs(m2 - MU) <= semi;
      svg.appendChild(el("line", {x1: sx(m2 - semi).toFixed(1), y1: y.toFixed(1),
        x2: sx(m2 + semi).toFixed(1), y2: y.toFixed(1),
        stroke: ok ? C_OK : C_MAL, "stroke-width": ok ? 1.5 : 2.2,
        opacity: ok ? 0.5 : 1}));
      svg.appendChild(el("circle", {cx: sx(m2).toFixed(1), cy: y.toFixed(1), r: 1.5,
        fill: ok ? C_OK : C_MAL, opacity: ok ? 0.55 : 1}));
    }

    // eje
    svg.appendChild(el("line", {x1: X0, y1: Y0 + 6, x2: X1, y2: Y0 + 6,
      stroke: "var(--r-linea, #ccc)"}));
    var paso_e = rad > 12 ? 10 : (rad > 6 ? 5 : 2);
    for (var v = Math.ceil((MU - rad) / paso_e) * paso_e; v <= MU + rad; v += paso_e) {
      svg.appendChild(el("text", {x: sx(v), y: Y0 + 22, "font-size": 9.5,
        "text-anchor": "middle", fill: "var(--r-tinta-suave, #666)"}, String(v)));
    }

    var pct = Math.round(parseFloat(conf) * 100);
    lec.innerHTML = "<span class=\"fx\">en esta tanda: " + cubren + " de 100 intervalos contienen μ" +
      "   ·   en " + G + " muestras: " + (100 * largas / G).toFixed(1) + " %" +
      "   ·   nominal: " + pct + " %</span><br>" +
      "Lo aleatorio es el intervalo, no μ. μ vale 100 en las cien filas; lo que cambia de fila " +
      "en fila es dónde cae el segmento. El " + pct + " % describe con qué frecuencia el " +
      "<b>procedimiento</b> acierta, no la confianza que merece una fila concreta.<br>" +
      "El número de la tanda baila alrededor del nominal; el de las " + G + " muestras no.";
  }

  sn.addEventListener("change", dibujar);
  scf.addEventListener("change", dibujar);
  btn.addEventListener("click", function () { semilla = (semilla * 16807 + 12345) | 0; dibujar(); });
  dibujar();
})();
</script>
```

## 3. Lo que el intervalo no dice

::: {.proposicion}
**Proposición 9.4.** Una vez observada la muestra, sean $a$ y $b$ los extremos ya calculados. Entonces
$$P(a \le \mu \le b) \in \{0, 1\}.$$
:::

::: {.demostracion}
*Demostración.* Tras observar los datos, $a$ y $b$ son dos números concretos y $\mu$ es una constante desconocida. El enunciado «$a \le \mu \le b$» no contiene ninguna variable aleatoria: o es verdadero, y su probabilidad es 1, o es falso, y su probabilidad es 0. No hay un tercer caso, y cuál de los dos ocurre no se puede saber. [∎]{.fin-demo}
:::

La proposición es casi trivial y por eso es fácil de pasar por alto. Lo que descarta es la lectura corriente: dado el intervalo $[96.2, 103.8]$, **no** es correcto decir «$\mu$ está ahí con probabilidad 0,95». Esa frase le atribuye una distribución a $\mu$, y en este marco $\mu$ no la tiene.

Lo que sí se puede afirmar es sobre el **procedimiento**: si se repitiera el experimento muchas veces y cada vez se construyera el intervalo con esta fórmula, el 95 % de esos intervalos contendría $\mu$. Eso es lo que mide el segundo número del pie del visual, y es lo que se mantiene estable al pulsar *Otras 100 muestras*, mientras el primero baila.

::: {.callout-note}
La pregunta «¿con qué probabilidad está $\mu$ aquí dentro?» no es ilegítima: es la pregunta que casi todo el mundo quiere hacer. Lo que ocurre es que para responderla hace falta tratar $\mu$ como variable aleatoria, es decir, darle una distribución previa. Eso es exactamente lo que hace el enfoque bayesiano, y el objeto que resulta se llama **intervalo de credibilidad**. No es este. La lección 18 lo construye y las diferencias numéricas entre ambos se ven allí.
:::

## 4. $\sigma$ desconocida: el primer fallo de cobertura

En la práctica $\sigma$ casi nunca se conoce. El reflejo natural es sustituirla por $s$ y dejar el resto igual. Ese reflejo tiene un coste medible.

Para enunciar cuál hace falta una distribución más.

::: {.definicion}
**Definición 9.5 (distribución $t$ de Student).** $T_\nu$ tiene densidad
$$f(t;\nu) = \frac{\Gamma\!\big(\frac{\nu+1}{2}\big)}{\sqrt{\nu\pi}\,\Gamma\!\big(\frac{\nu}{2}\big)}\Big(1+\frac{t^2}{\nu}\Big)^{-\frac{\nu+1}{2}},$$
simétrica alrededor de cero, con colas más pesadas que la normal para todo $\nu$ finito, y que converge a la normal estándar cuando $\nu \to \infty$.
:::

::: {.proposicion}
**Teorema 9.6 (sin demostrar aquí).** Si $x_1,\dots,x_n$ es una muestra de una $\mathcal{N}(\mu,\sigma^2)$, entonces
$$\frac{\bar{x}-\mu}{s/\sqrt{n}} \sim T_{n-1},$$
donde $s^2 = Q/(n-1)$ es el estimador insesgado de la varianza.
:::

La demostración necesita dos hechos que esta lección todavía no tiene: que $Q/\sigma^2$ sigue una distribución $\chi^2_{n-1}$, y que $\bar{x}$ y $s^2$ son **independientes** —algo nada evidente, porque $s^2$ se calcula restando $\bar{x}$—. Las dos son consecuencias de descomponer un vector normal en subespacios ortogonales, y por eso se aplazan a la **lección 12**, donde la normal multivariante da la herramienta. Es el mismo argumento de proyección de la Matemática 5, aplicado a un vector aleatorio.

::: {.proposicion}
**Proposición 9.7.** Bajo las hipótesis del Teorema 9.6, el intervalo $\bar{x} \pm z_{\alpha/2}\, s/\sqrt{n}$ tiene cobertura exactamente
$$P\big(|T_{n-1}| \le z_{\alpha/2}\big),$$
y el intervalo $\bar{x} \pm t_{n-1,\,\alpha/2}\, s/\sqrt{n}$, donde $t_{n-1,\,\alpha/2}$ es el cuantil correspondiente de la $t$, tiene cobertura exactamente $1-\alpha$.
:::

::: {.demostracion}
*Demostración.* Escribiendo $T = (\bar{x}-\mu)/(s/\sqrt{n})$, la misma reordenación de la Proposición 9.3 —válida porque $s/\sqrt{n} > 0$— da
$$\mu \in \Big[\bar{x} - c\,\frac{s}{\sqrt{n}},\; \bar{x} + c\,\frac{s}{\sqrt{n}}\Big] \iff |T| \le c$$
para cualquier constante $c > 0$. Por el Teorema 9.6, $T \sim T_{n-1}$, luego la cobertura es $P(|T_{n-1}| \le c)$. Con $c = z_{\alpha/2}$ se obtiene la primera afirmación. Para la segunda, $t_{n-1,\alpha/2}$ está definido precisamente como el valor que hace $P(|T_{n-1}| \le c) = 1-\alpha$. [∎]{.fin-demo}
:::

Falta saber si $P(|T_{n-1}| \le z_{\alpha/2})$ queda por encima o por debajo de $1-\alpha$. Como la $t$ tiene colas más pesadas que la normal, acumula menos probabilidad en el centro y la cobertura se queda **corta**. La celda siguiente lo calcula sin simular: integra la densidad de la Definición 9.5 por la regla de Simpson.

```{pyodide}
import numpy as np

def log_gamma(x):
    # Aproximacion de Lanczos. Se necesita para la constante de la densidad t.
    g = [76.18009172947146, -86.50532032941677, 24.01409824083091,
         -1.231739572450155, 0.1208650973866179e-2, -0.5395239384953e-5]
    y, tmp = x, x + 5.5
    tmp -= (x + 0.5) * np.log(tmp)
    ser = 1.000000000190015
    for j in range(6):
        y = y + 1.0
        ser = ser + g[j] / y
    return -tmp + np.log(2.5066282746310005 * ser / x)

def prob_t(x, nu, N=2000):
    """P(|T_nu| <= x) integrando la densidad de la Definicion 9.5."""
    c = log_gamma((nu + 1) / 2) - log_gamma(nu / 2) - 0.5 * np.log(nu * np.pi)
    u = np.linspace(0.0, x, N + 1)
    f = np.exp(c - (nu + 1) / 2 * np.log1p(u * u / nu))
    w = np.ones(N + 1); w[1:-1:2] = 4; w[2:-1:2] = 2      # pesos de Simpson
    return 2 * (x / N) / 3 * (w * f).sum()

Z95 = 1.9599639845
print("  n    cobertura real de x̄ ± 1.96·s/√n   deficit")
for n in [3, 5, 8, 12, 20, 40, 80, 200]:
    cob = prob_t(Z95, n - 1)
    print(f"{n:4d}            {100*cob:7.2f} %              {100*(0.95-cob):5.2f} pp")
```

Con $n = 3$ el intervalo que dice cubrir el 95 % cubre el 81 %: uno de cada cinco falla en vez de uno de cada veinte. El déficit se cierra despacio y sigue habiendo medio punto porcentual con $n = 200$.

El visual repite el cálculo por simulación y añade las otras dos curvas.

```{=html}
<div class="viz-cob">
  <div class="controles">
    <label>Confianza nominal
      <select id="cb-c">
        <option value="0.90">90 %</option>
        <option value="0.95" selected>95 %</option>
        <option value="0.99">99 %</option>
      </select>
    </label>
    <label>n <input id="cb-n" type="range" min="0" max="7" step="1" value="2"><output id="cb-n-v">5</output></label>
  </div>
  <svg id="cb-svg" viewBox="0 0 560 320" role="img" aria-label="Cobertura real de tres intervalos de confianza en función del tamaño de muestra, comparada con la cobertura nominal"></svg>
  <p class="lectura" id="cb-lectura"></p>
</div>
<style>
.viz-cob {
  border: 1px solid var(--r-linea, #ddd);
  border-radius: 10px; padding: 1rem 1.1rem; margin: 1.7rem 0;
}
.viz-cob .controles {
  display: flex; flex-wrap: wrap; gap: .45rem 1.2rem;
  align-items: center; margin-bottom: .85rem; font-size: .85rem;
}
.viz-cob label { display: flex; align-items: center; gap: .4rem; }
.viz-cob select {
  font-size: .85rem; padding: .2rem .4rem; border-radius: 5px;
  border: 1px solid var(--r-linea, #ccc); background: transparent; color: inherit;
}
.viz-cob input[type=range] { width: 7rem; }
.viz-cob output {
  font-family: var(--bs-font-monospace, monospace); font-size: .8rem;
  min-width: 2.2rem; color: var(--r-tinta-suave, #555);
}
.viz-cob svg { width: 100%; height: auto; display: block; }
.viz-cob .lectura { font-size: .85rem; margin-top: .75rem; min-height: 5em; line-height: 1.6; }
.viz-cob .lectura .fx { font-family: var(--bs-font-monospace, monospace); font-size: .95em; }
</style>
<script>
(function () {
  var Z = {"0.90": 1.6448536270, "0.95": 1.9599639845, "0.99": 2.5758293035};
  var NS = [3, 5, 8, 12, 20, 40, 80, 200];
  var REPS = 12000;
  var COL = {z: "#0F6E68", s: "#B91C1C", t: "#B45309"};
  var svg = document.getElementById("cb-svg"), lec = document.getElementById("cb-lectura");
  var sc = document.getElementById("cb-c"), sn = document.getElementById("cb-n");
  var snv = document.getElementById("cb-n-v");
  var X0 = 52, X1 = 470, Y0 = 258, Y1 = 36;

  function el(t, at, txt) {
    var e = document.createElementNS("http://www.w3.org/2000/svg", t);
    for (var k in at) e.setAttribute(k, at[k]);
    if (txt !== undefined) e.textContent = txt;
    return e;
  }

  // ---- log Γ por Lanczos, para la densidad t ----
  var LAN = [76.18009172947146, -86.50532032941677, 24.01409824083091,
             -1.231739572450155, 0.1208650973866179e-2, -0.5395239384953e-5];
  function logGamma(x) {
    var y = x, tmp = x + 5.5, ser = 1.000000000190015, j;
    tmp -= (x + 0.5) * Math.log(tmp);
    for (j = 0; j < 6; j++) ser += LAN[j] / ++y;
    return -tmp + Math.log(2.5066282746310005 * ser / x);
  }
  // P(|T| <= x) para nu grados de libertad, por Simpson sobre la densidad
  function probT(x, nu) {
    var c = logGamma((nu + 1) / 2) - logGamma(nu / 2) - 0.5 * Math.log(nu * Math.PI);
    var N = 400, h = x / N, suma = 0, i;
    function f(u) { return Math.exp(c - (nu + 1) / 2 * Math.log(1 + u * u / nu)); }
    for (i = 0; i <= N; i++) {
      var w = (i === 0 || i === N) ? 1 : (i % 2 ? 4 : 2);
      suma += w * f(i * h);
    }
    return 2 * (h / 3) * suma;
  }
  // cuantil de t por biseccion
  function cuantilT(p, nu) {
    var lo = 0, hi = 200, i, m;
    for (i = 0; i < 80; i++) {
      m = (lo + hi) / 2;
      if (probT(m, nu) < p) lo = m; else hi = m;
    }
    return (lo + hi) / 2;
  }

  function medir(conf) {
    var z = Z[conf], nivel = parseFloat(conf), out = {z: [], s: [], t: [], tc: []};
    var semilla = 777777;
    function u() {
      semilla |= 0; semilla = semilla + 0x6D2B79F5 | 0;
      var t = Math.imul(semilla ^ semilla >>> 15, 1 | semilla);
      t = t + Math.imul(t ^ t >>> 7, 61 | t) ^ t;
      return ((t ^ t >>> 14) >>> 0) / 4294967296;
    }
    function gauss() { var a = u() || 1e-12, b = u(); return Math.sqrt(-2*Math.log(a))*Math.cos(2*Math.PI*b); }

    for (var k = 0; k < NS.length; k++) {
      var n = NS[k], tc = cuantilT(nivel, n - 1);
      out.tc.push(tc);
      var cz = 0, cs = 0, ct = 0, r, i;
      for (r = 0; r < REPS; r++) {
        var suma = 0, x = new Float64Array(n);
        for (i = 0; i < n; i++) { x[i] = gauss(); suma += x[i]; }   // mu = 0, sigma = 1
        var m = suma / n, q = 0;
        for (i = 0; i < n; i++) q += (x[i] - m) * (x[i] - m);
        var s_ = Math.sqrt(q / (n - 1)), ee_s = s_ / Math.sqrt(n), ee_z = 1 / Math.sqrt(n);
        if (Math.abs(m) <= z * ee_z) cz++;
        if (Math.abs(m) <= z * ee_s) cs++;
        if (Math.abs(m) <= tc * ee_s) ct++;
      }
      out.z.push(cz / REPS); out.s.push(cs / REPS); out.t.push(ct / REPS);
    }
    return out;
  }

  function dibujar() {
    var conf = sc.value, nivel = parseFloat(conf), idx = +sn.value;
    snv.textContent = String(NS[idx]);
    var d = medir(conf);

    var lo = Math.min(0.995, Math.min.apply(null, d.s)) - 0.02;
    lo = Math.min(lo, nivel - 0.10);
    var hi = Math.min(1.0, nivel + 0.035);
    function px(i) { return X0 + i / (NS.length - 1) * (X1 - X0); }
    function py(v) { return Y0 - (Math.min(Math.max(v, lo), hi) - lo) / (hi - lo) * (Y0 - Y1); }

    while (svg.firstChild) svg.removeChild(svg.firstChild);

    // rejilla
    for (var g = Math.ceil(lo * 100 / 2) * 2; g <= hi * 100; g += 2) {
      var y = py(g / 100);
      svg.appendChild(el("line", {x1: X0, y1: y, x2: X1, y2: y,
        stroke: "var(--r-linea,#ddd)", opacity: 0.5}));
      svg.appendChild(el("text", {x: X0 - 7, y: y + 3.5, "font-size": 9.5,
        "text-anchor": "end", fill: "var(--r-tinta-suave,#666)"}, g + " %"));
    }
    // nivel nominal
    svg.appendChild(el("line", {x1: X0, y1: py(nivel), x2: X1, y2: py(nivel),
      stroke: "var(--r-tinta,#333)", "stroke-width": 1.4, "stroke-dasharray": "5 4"}));
    svg.appendChild(el("text", {x: X1 + 6, y: py(nivel) + 3.5, "font-size": 10,
      fill: "var(--r-tinta,#333)"}, "nominal"));

    // eje n
    svg.appendChild(el("line", {x1: X0, y1: Y0, x2: X1, y2: Y0, stroke: "var(--r-linea,#ccc)"}));
    for (var k = 0; k < NS.length; k++) {
      svg.appendChild(el("text", {x: px(k), y: Y0 + 16, "font-size": 9.5,
        "text-anchor": "middle", fill: "var(--r-tinta-suave,#666)"}, String(NS[k])));
    }
    svg.appendChild(el("text", {x: (X0 + X1) / 2, y: Y0 + 32, "font-size": 10.5,
      "text-anchor": "middle", fill: "var(--r-tinta-suave,#666)"}, "tamaño de muestra n"));
    svg.appendChild(el("text", {x: X0 - 40, y: 24, "font-size": 10.5,
      fill: "var(--r-tinta-suave,#666)"}, "cobertura real"));

    svg.appendChild(el("line", {x1: px(idx), y1: Y1 - 6, x2: px(idx), y2: Y0,
      stroke: "var(--r-tinta,#333)", "stroke-dasharray": "3 3", opacity: 0.4}));

    var etiquetas = {z: "z con σ conocida", s: "z con s", t: "t con s"};
    var claves = ["z", "s", "t"], j;
    for (j = 0; j < 3; j++) {
      var c = claves[j], dd = "";
      for (var i = 0; i < NS.length; i++)
        dd += (i ? " L " : "M ") + px(i).toFixed(1) + " " + py(d[c][i]).toFixed(1);
      svg.appendChild(el("path", {d: dd, fill: "none", stroke: COL[c], "stroke-width": 2}));
      svg.appendChild(el("circle", {cx: px(idx).toFixed(1), cy: py(d[c][idx]).toFixed(1),
        r: 3.6, fill: COL[c]}));
      svg.appendChild(el("rect", {x: 486, y: 44 + j * 20 - 7, width: 9, height: 3, fill: COL[c]}));
      svg.appendChild(el("text", {x: 499, y: 44 + j * 20 - 2, "font-size": 10, fill: COL[c]},
        etiquetas[c]));
    }

    var pct = (nivel * 100).toFixed(0);
    lec.innerHTML = "Con <span class=\"fx\">n = " + NS[idx] + "</span> y nominal " + pct + " %:<br>" +
      "<span class=\"fx\">z con σ conocida " + (100 * d.z[idx]).toFixed(1) + " %" +
      "   ·   z con s " + (100 * d.s[idx]).toFixed(1) + " %" +
      "   ·   t con s " + (100 * d.t[idx]).toFixed(1) + " %" +
      "   ·   t(" + (NS[idx] - 1) + ") = " + d.tc[idx].toFixed(3) + " frente a z = " +
      Z[conf].toFixed(3) + "</span><br>" +
      "Sustituir σ por s sin cambiar nada más deja el intervalo corto: cubre menos de lo que " +
      "promete, y el fallo es grande con n pequeña. El cuantil t, más ancho, lo corrige; " +
      "al crecer n los dos cuantiles convergen y las tres curvas se juntan.";
  }

  sc.addEventListener("change", dibujar);
  sn.addEventListener("input", dibujar);
  dibujar();
})();
</script>
```

La lectura importante es cuál de las tres curvas está en su sitio y por qué. La de $\sigma$ conocida está en el nominal por la Proposición 9.3, que es exacta. La de $s$ con cuantil $z$ está por debajo por la Proposición 9.7. La de $s$ con cuantil $t$ vuelve al nominal, también por la Proposición 9.7: el cuantil $t$ está definido justamente para eso. **El arreglo no es usar más datos, es usar el cuantil correcto.**

::: {.callout-note}
Con $n$ grande la diferencia se vuelve irrelevante —$t_{199}$ vale 1,972 frente a $z = 1,960$— y por eso mucha gente usa $z$ sin consecuencias. El problema aparece exactamente donde más caro es equivocarse: muestras pequeñas.
:::

## 5. Una proporción: el segundo fallo

El caso de una proporción es el intervalo más usado que existe —encuestas, tasas de conversión, tasas de fallo— y el que peor se comporta.

::: {.definicion}
**Definición 9.8 (intervalo de Wald).** Con $k$ éxitos en $n$ intentos y $\hat{p} = k/n$,
$$\hat{p} \pm z_{\alpha/2}\sqrt{\frac{\hat{p}(1-\hat{p})}{n}}.$$
:::

Sale de aplicar la Proposición 9.3 a una Bernoulli, cuya varianza es $p(1-p)$ (Proposición 4.7), sustituyendo el $p$ desconocido de la varianza por $\hat{p}$. Ese último paso es el que rompe.

::: {.proposicion}
**Proposición 9.9.** Para todo $n$ fijo y todo $z > 0$, la cobertura del intervalo de Wald tiende a cero cuando $p \to 0^{+}$.
:::

::: {.demostracion}
*Demostración.* Si $k = 0$ entonces $\hat{p} = 0$, la raíz vale $\sqrt{0 \cdot 1/n} = 0$ y el intervalo se reduce al punto $\{0\}$, que no contiene ningún $p > 0$. Por tanto, para $p > 0$,
$$\text{cobertura}(p) \le P(k \ge 1) = 1 - (1-p)^{n}.$$
Cuando $p \to 0^{+}$, $(1-p)^{n} \to 1$ y la cota tiende a cero. [∎]{.fin-demo}
:::

No es una curiosidad de los extremos: es la situación de cualquier suceso poco frecuente. Un intervalo que ante cero éxitos responde «$p$ está entre 0 y 0» con un 95 % de confianza no está siendo conservador, está siendo falso.

El visual calcula la cobertura **exacta**, sin simular: para cada $p$ suma las probabilidades binomiales de los $k$ cuyo intervalo contiene ese $p$.

```{=html}
<div class="viz-wald">
  <div class="controles">
    <label>n
      <select id="wa-n"><option>10</option><option selected>40</option><option>100</option><option>400</option></select>
    </label>
    <label>Confianza
      <select id="wa-c">
        <option value="0.90">90 %</option>
        <option value="0.95" selected>95 %</option>
        <option value="0.99">99 %</option>
      </select>
    </label>
    <label>p <input id="wa-p" type="range" min="0.01" max="0.99" step="0.01" value="0.10"><output id="wa-p-v">0.10</output></label>
  </div>
  <svg id="wa-svg" viewBox="0 0 560 320" role="img" aria-label="Cobertura exacta del intervalo de Wald y del de Wilson para una proporción, en función del valor verdadero de p"></svg>
  <p class="lectura" id="wa-lectura"></p>
</div>
<style>
.viz-wald {
  border: 1px solid var(--r-linea, #ddd);
  border-radius: 10px; padding: 1rem 1.1rem; margin: 1.7rem 0;
}
.viz-wald .controles {
  display: flex; flex-wrap: wrap; gap: .45rem 1.2rem;
  align-items: center; margin-bottom: .85rem; font-size: .85rem;
}
.viz-wald label { display: flex; align-items: center; gap: .4rem; }
.viz-wald select {
  font-size: .85rem; padding: .2rem .4rem; border-radius: 5px;
  border: 1px solid var(--r-linea, #ccc); background: transparent; color: inherit;
}
.viz-wald input[type=range] { width: 7.5rem; }
.viz-wald output {
  font-family: var(--bs-font-monospace, monospace); font-size: .8rem;
  min-width: 2.4rem; color: var(--r-tinta-suave, #555);
}
.viz-wald svg { width: 100%; height: auto; display: block; }
.viz-wald .lectura { font-size: .85rem; margin-top: .75rem; min-height: 5em; line-height: 1.6; }
.viz-wald .lectura .fx { font-family: var(--bs-font-monospace, monospace); font-size: .95em; }
</style>
<script>
(function () {
  var Z = {"0.90": 1.6448536270, "0.95": 1.9599639845, "0.99": 2.5758293035};
  var C_W = "#B91C1C", C_S = "#0F6E68";
  var svg = document.getElementById("wa-svg"), lec = document.getElementById("wa-lectura");
  var sn = document.getElementById("wa-n"), sc = document.getElementById("wa-c");
  var sp = document.getElementById("wa-p"), spv = document.getElementById("wa-p-v");
  var X0 = 50, X1 = 462, Y0 = 258, Y1 = 34, NP = 393;

  function el(t, at, txt) {
    var e = document.createElementNS("http://www.w3.org/2000/svg", t);
    for (var k in at) e.setAttribute(k, at[k]);
    if (txt !== undefined) e.textContent = txt;
    return e;
  }

  var LAN = [76.18009172947146, -86.50532032941677, 24.01409824083091,
             -1.231739572450155, 0.1208650973866179e-2, -0.5395239384953e-5];
  function logGamma(x) {
    var y = x, tmp = x + 5.5, ser = 1.000000000190015, j;
    tmp -= (x + 0.5) * Math.log(tmp);
    for (j = 0; j < 6; j++) ser += LAN[j] / ++y;
    return -tmp + Math.log(2.5066282746310005 * ser / x);
  }
  function logComb(n, k) {
    return logGamma(n + 1) - logGamma(k + 1) - logGamma(n - k + 1);
  }

  // cobertura EXACTA: se suman las probabilidades binomiales de los k cuyo
  // intervalo contiene p. No hay simulacion.
  function cobertura(p, n, z, tipo) {
    var total = 0, k;
    for (k = 0; k <= n; k++) {
      var ph = k / n, cubre;
      if (tipo === "wald") {
        var h = z * Math.sqrt(ph * (1 - ph) / n);
        cubre = (p >= ph - h) && (p <= ph + h);
      } else {
        var den = 1 + z * z / n;
        var cen = (ph + z * z / (2 * n)) / den;
        var h2 = z / den * Math.sqrt(ph * (1 - ph) / n + z * z / (4 * n * n));
        cubre = (p >= cen - h2) && (p <= cen + h2);
      }
      if (!cubre) continue;
      var lp = logComb(n, k) + (k ? k * Math.log(p) : 0) +
               ((n - k) ? (n - k) * Math.log(1 - p) : 0);
      total += Math.exp(lp);
    }
    return total;
  }

  function dibujar() {
    var n = +sn.value, conf = sc.value, z = Z[conf], nivel = parseFloat(conf);
    var pmarca = parseFloat(sp.value);
    spv.textContent = pmarca.toFixed(2);

    var ps = [], cw = [], cs = [], i;
    for (i = 0; i <= NP; i++) {
      var p = 0.005 + i / NP * 0.99;
      ps.push(p);
      cw.push(cobertura(p, n, z, "wald"));
      cs.push(cobertura(p, n, z, "wilson"));
    }
    var minw = Math.min.apply(null, cw), mins = Math.min.apply(null, cs);

    var lo = Math.max(0, Math.min(minw, nivel - 0.12) - 0.03), hi = 1.005;
    function px(p) { return X0 + (p - 0.005) / 0.99 * (X1 - X0); }
    function py(v) { return Y0 - (Math.min(Math.max(v, lo), hi) - lo) / (hi - lo) * (Y0 - Y1); }

    while (svg.firstChild) svg.removeChild(svg.firstChild);

    for (var g = Math.ceil(lo * 10) * 10; g <= 100; g += 10) {
      var y = py(g / 100);
      svg.appendChild(el("line", {x1: X0, y1: y, x2: X1, y2: y,
        stroke: "var(--r-linea,#ddd)", opacity: 0.5}));
      svg.appendChild(el("text", {x: X0 - 7, y: y + 3.5, "font-size": 9.5,
        "text-anchor": "end", fill: "var(--r-tinta-suave,#666)"}, g + " %"));
    }
    svg.appendChild(el("line", {x1: X0, y1: py(nivel), x2: X1, y2: py(nivel),
      stroke: "var(--r-tinta,#333)", "stroke-width": 1.4, "stroke-dasharray": "5 4"}));
    svg.appendChild(el("text", {x: X1 + 6, y: py(nivel) + 3.5, "font-size": 10,
      fill: "var(--r-tinta,#333)"}, "nominal"));

    svg.appendChild(el("line", {x1: X0, y1: Y0, x2: X1, y2: Y0, stroke: "var(--r-linea,#ccc)"}));
    for (i = 0; i <= 10; i += 2) {
      svg.appendChild(el("text", {x: px(i / 10), y: Y0 + 16, "font-size": 9.5,
        "text-anchor": "middle", fill: "var(--r-tinta-suave,#666)"}, (i / 10).toFixed(1)));
    }
    svg.appendChild(el("text", {x: (X0 + X1) / 2, y: Y0 + 32, "font-size": 10.5,
      "text-anchor": "middle", fill: "var(--r-tinta-suave,#666)"}, "p verdadero"));
    svg.appendChild(el("text", {x: X0 - 42, y: 22, "font-size": 10.5,
      fill: "var(--r-tinta-suave,#666)"}, "cobertura"));

    svg.appendChild(el("line", {x1: px(pmarca), y1: Y1 - 6, x2: px(pmarca), y2: Y0,
      stroke: "var(--r-tinta,#333)", "stroke-dasharray": "3 3", opacity: 0.4}));

    var series = [{v: cw, c: C_W, n: "Wald  p̂ ± z·√(p̂(1−p̂)/n)"},
                  {v: cs, c: C_S, n: "Wilson"}];
    for (var j = 0; j < 2; j++) {
      var d = "";
      for (i = 0; i <= NP; i++)
        d += (i ? " L " : "M ") + px(ps[i]).toFixed(1) + " " + py(series[j].v[i]).toFixed(1);
      svg.appendChild(el("path", {d: d, fill: "none", stroke: series[j].c, "stroke-width": 1.6}));
      svg.appendChild(el("rect", {x: 478, y: 44 + j * 20 - 7, width: 9, height: 3, fill: series[j].c}));
      svg.appendChild(el("text", {x: 491, y: 44 + j * 20 - 2, "font-size": 9.5, fill: series[j].c},
        series[j].n));
    }
    var cwm = cobertura(pmarca, n, z, "wald"), csm = cobertura(pmarca, n, z, "wilson");
    svg.appendChild(el("circle", {cx: px(pmarca).toFixed(1), cy: py(cwm).toFixed(1), r: 3.6, fill: C_W}));
    svg.appendChild(el("circle", {cx: px(pmarca).toFixed(1), cy: py(csm).toFixed(1), r: 3.6, fill: C_S}));

    lec.innerHTML = "Con <span class=\"fx\">n = " + n + "</span>, nominal " +
      (nivel * 100).toFixed(0) + " % y <span class=\"fx\">p = " + pmarca.toFixed(2) + "</span>:<br>" +
      "<span class=\"fx\">Wald " + (100 * cwm).toFixed(1) + " %   ·   Wilson " +
      (100 * csm).toFixed(1) + " %   ·   peor caso en todo el rango: Wald " +
      (100 * minw).toFixed(1) + " %, Wilson " + (100 * mins).toFixed(1) + " %</span><br>" +
      "Estas cifras no son simuladas: se suman las probabilidades binomiales de los k cuyo " +
      "intervalo contiene p. Los dientes de sierra vienen de que k es entero, así que al mover " +
      "p la cobertura salta cuando un k entra o sale.<br>" +
      "El de Wald se hunde cerca de 0 y de 1, donde muchas encuestas y tasas de conversión " +
      "viven. En el caso extremo k = 0 el intervalo tiene ancho cero y no cubre nada.";
  }

  sn.addEventListener("change", dibujar);
  sc.addEventListener("change", dibujar);
  sp.addEventListener("input", dibujar);
  dibujar();
})();
</script>
```

Los dientes de sierra son reales y no un artefacto del dibujo. $k$ solo toma $n+1$ valores, así que al mover $p$ de forma continua la cobertura salta cada vez que un $k$ entra o sale del conjunto de los que cubren. Por eso la cobertura no converge de forma suave al nominal: oscila alrededor mientras se estrecha.

La curva verde es el **intervalo de Wilson**, que resuelve el problema:
$$\frac{\hat{p} + \frac{z^2}{2n}}{1+\frac{z^2}{n}} \;\pm\; \frac{z}{1+\frac{z^2}{n}}\sqrt{\frac{\hat{p}(1-\hat{p})}{n} + \frac{z^2}{4n^2}}.$$
No se deriva aquí porque sale de invertir una prueba de hipótesis —resolver en $p$ la desigualdad $|\hat{p}-p| \le z\sqrt{p(1-p)/n}$, con el $p$ verdadero dentro de la raíz en lugar de $\hat{p}$— y esa maquinaria es la lección 10. Lo que interesa ahora es la moraleja general: **la cobertura real es una propiedad medible de un procedimiento, y hay que medirla en vez de suponerla**.

```{pyodide}
import numpy as np

def log_gamma(x):
    g = [76.18009172947146, -86.50532032941677, 24.01409824083091,
         -1.231739572450155, 0.1208650973866179e-2, -0.5395239384953e-5]
    y, tmp = x, x + 5.5
    tmp -= (x + 0.5) * np.log(tmp)
    ser = 1.000000000190015
    for j in range(6):
        y = y + 1.0
        ser = ser + g[j] / y
    return -tmp + np.log(2.5066282746310005 * ser / x)

def cobertura_wald(p, n, z=1.9599639845):
    """Cobertura exacta: suma de las probabilidades binomiales de los k que cubren p."""
    k  = np.arange(n + 1)
    ph = k / n
    h  = z * np.sqrt(ph * (1 - ph) / n)
    cubre = (p >= ph - h) & (p <= ph + h)
    log_pmf = (log_gamma(n + 1) - log_gamma(k + 1.0) - log_gamma(n - k + 1.0)
               + k * np.log(p) + (n - k) * np.log(1 - p))
    return np.exp(log_pmf[cubre]).sum()

print("cobertura exacta del intervalo de Wald, nominal 95 %")
print("   n     p = 0.05   p = 0.10   p = 0.50")
for n in [10, 40, 100, 400]:
    fila = [100 * cobertura_wald(p, n) for p in (0.05, 0.10, 0.50)]
    print(f"{n:5d}     {fila[0]:6.2f} %   {fila[1]:6.2f} %   {fila[2]:6.2f} %")
```

Con $n = 100$ —una muestra que nadie llamaría pequeña— y $p = 0{,}05$, el intervalo del 95 % cubre menos del 88 %. La columna de $p = 0{,}5$ se porta mucho mejor: el problema no es el tamaño de la muestra, es dónde está $p$.

## Ejercicios

### Ejercicio 1 — Medir la cobertura

Estimar por simulación la cobertura real del intervalo $\bar{x} \pm 1{,}96\,s/\sqrt{n}$ con $n = 5$ y población normal, y compararla con el valor exacto $P(|T_4| \le 1{,}96) \approx 0{,}8784$. Devolver la cobertura medida.

```{pyodide}
#| exercise: ex_cob
import numpy as np

rng = np.random.default_rng(21)
n, reps, z = 5, 100_000, 1.9599639845
MU, SIGMA = 0.0, 1.0

X = rng.normal(MU, SIGMA, size=(reps, n))
medias = X.mean(axis=1)
eses   = X.std(axis=1, ddof=1)          # ddof=1 da el denominador n − 1

# El intervalo cubre MU cuando la distancia de la media a MU no pasa del semiancho.
cubre = ______

cobertura = cubre.mean()
print(f"cobertura medida : {100*cobertura:.2f} %")
print(f"exacta P(|T_4| <= 1.96) : 87.84 %")
print(f"nominal          : 95.00 %")
```

::: {.hint exercise="ex_cob"}
El semiancho de cada intervalo es `z * eses / np.sqrt(n)`, distinto en cada réplica. La condición es que `np.abs(medias - MU)` no lo supere. El resultado es un vector de booleanos, uno por réplica.
:::

::: {.solution exercise="ex_cob"}
```python
cubre = np.abs(medias - MU) <= z * eses / np.sqrt(n)
```

La cobertura medida cae cerca del 87,8 % exacto y a más de siete puntos del 95 % nominal. Nótese que `eses` cambia de réplica en réplica: los intervalos no solo están descentrados de forma distinta, también tienen anchuras distintas, y son los estrechos los que fallan.
:::

```{pyodide}
#| exercise: ex_cob
#| check: true
import numpy as np
ok = abs(cobertura - 0.8784) < 0.006
feedback = ("Correcto." if ok else
            "La cobertura no cae cerca del 87.8 % exacto; revisa cómo formas la condición.")
{"correct": bool(ok), "message": feedback}
```

### Ejercicio 2 — El peor $p$

Con $n = 40$ y nominal 95 %, recorrer $p$ en una rejilla fina y encontrar el valor donde la cobertura exacta del intervalo de Wald es menor, ignorando los extremos ($p$ entre 0,02 y 0,98). Devolver esa cobertura mínima.

```{pyodide}
#| exercise: ex_peor
import numpy as np

def log_gamma(x):
    g = [76.18009172947146, -86.50532032941677, 24.01409824083091,
         -1.231739572450155, 0.1208650973866179e-2, -0.5395239384953e-5]
    y, tmp = x, x + 5.5
    tmp -= (x + 0.5) * np.log(tmp)
    ser = 1.000000000190015
    for j in range(6):
        y = y + 1.0
        ser = ser + g[j] / y
    return -tmp + np.log(2.5066282746310005 * ser / x)

def cobertura_wald(p, n, z=1.9599639845):
    k  = np.arange(n + 1); ph = k / n
    h  = z * np.sqrt(ph * (1 - ph) / n)
    cubre = (p >= ph - h) & (p <= ph + h)
    log_pmf = (log_gamma(n + 1) - log_gamma(k + 1.0) - log_gamma(n - k + 1.0)
               + k * np.log(p) + (n - k) * np.log(1 - p))
    return np.exp(log_pmf[cubre]).sum()

n = 40
rejilla = np.linspace(0.02, 0.98, 4801)

# Evaluar la cobertura en cada p de la rejilla.
cobs = ______

peor_p   = rejilla[np.argmin(cobs)]
peor_cob = cobs.min()
print(f"peor p        : {peor_p:.4f}")
print(f"su cobertura  : {100*peor_cob:.2f} %   (nominal 95.00 %)")
```

::: {.hint exercise="ex_peor"}
`cobertura_wald` recibe un `p` escalar, así que hay que evaluarla punto a punto y juntar los resultados: `np.array([cobertura_wald(p, n) for p in rejilla])`.
:::

::: {.solution exercise="ex_peor"}
```python
cobs = np.array([cobertura_wald(p, n) for p in rejilla])
```

El mínimo cae en $p = 0{,}02$, el borde de la rejilla, con una cobertura del 55,3 % frente al 95 % nominal. Si la rejilla se acercara más a cero seguiría bajando, sin más cota inferior que el cero: eso es la Proposición 9.9. El mínimo no está en el interior, está donde se corte el rango.
:::

```{pyodide}
#| exercise: ex_peor
#| check: true
import numpy as np
ok = peor_cob < 0.80
feedback = ("Correcto." if ok else
            "La cobertura mínima debería quedar muy por debajo del 95 %; revisa cómo evalúas la rejilla.")
{"correct": bool(ok), "message": feedback}
```

## Reto

En `proyectos/notebooks/F1-retos.ipynb`, sección **Est 09**:

1. Escribir `cobertura(constructor, generador, theta, n, reps)` que mida la cobertura real de cualquier procedimiento de intervalo. Usarla para comparar los tres intervalos de la sección 4 sobre tres poblaciones: normal, exponencial y de cola pesada. La $t$ supone normalidad; medir cuánto se desvía cuando esa hipótesis no se cumple.
2. Reproducir el visual de la sección 5 con `matplotlib` para $n \in \{10, 25, 50, 100\}$, superponiendo Wald, Wilson y el intervalo de Agresti-Coull ($\hat{p}$ calculado tras sumar dos éxitos y dos fracasos). Decidir cuál se recomendaría por defecto y con qué argumento.
3. La anchura del intervalo también importa: un intervalo de $(-\infty,\infty)$ tiene cobertura perfecta y es inútil. Medir la anchura media de los tres intervalos del apartado 1 junto con su cobertura, y presentar los resultados como pares (cobertura, anchura). ¿Alguno domina a otro en las dos dimensiones a la vez?

## Del libro

*Think Stats* se publica bajo licencia [CC BY-NC-SA 4.0](https://creativecommons.org/licenses/by-nc-sa/4.0/), que permite citarlo textualmente.

::: {.cita-libro}
> A 90% confidence interval contains 90% of the values in the sampling distribution, which we can find by computing the 5th and 95th percentiles.
>
> [Downey, *Think Stats* 3e, §8.6 *Confidence Intervals*]{.ref}
:::

Es la misma construcción de la Proposición 9.3 por otra vía. Aquí se recorta la distribución muestral con la fórmula $\pm z_{\alpha/2}\sigma/\sqrt{n}$, que es exacta cuando la población es normal; Downey la recorta por percentiles de una distribución muestral simulada, que no necesita fórmula ni supone normalidad. Los dos caminos se encuentran en la lección 11.

**Preguntas para leer §8 con lápiz:**

1. §8.4 *Sampling Distributions* construye la distribución muestral por simulación. ¿Qué se está suponiendo conocido en esa simulación que en un problema real no se conocería?
2. §8.5 se titula *Standard Error*. ¿Qué relación establece entre el error estándar y la anchura del intervalo de §8.6? Contrastarla con la Proposición 9.3, donde la anchura es $2z_{\alpha/2}\,\text{EE}$.
3. §8.7 *Sources of Error* enumera fuentes de error distintas del muestreo aleatorio. Ninguna de ellas entra en la cobertura de la Definición 9.1. ¿Qué significa eso para un intervalo calculado sobre datos con sesgo de selección?

## Para el Cerebro

Nota nueva en `10-Conceptos/intervalo-de-confianza.md`, enlazada a [[estimacion]], [[teorema-central-limite]] y [[distribucion-muestral]]. La Proposición 9.4 conviene copiarla literal: es la frase que hay que tener a mano cada vez que alguien lea un intervalo en voz alta.

```
¿Qué es un intervalo de confianza?::Un intervalo aleatorio cuyos extremos son estadísticos, construido de modo que contenga al parámetro con probabilidad 1 − α, para todo valor del parámetro
¿Qué es aleatorio en un intervalo de confianza?::Los extremos. El parámetro es una constante desconocida
¿Es correcto decir que μ está en [96.2, 103.8] con probabilidad 0.95?::No. Una vez calculados los extremos, esa probabilidad es 0 o 1; el 95 % describe el procedimiento, no esa línea
¿Dónde vive entonces el 95 %?::En la frecuencia con la que el procedimiento acierta al repetirlo: 95 de cada 100 intervalos construidos así contienen μ
¿Cuál es el intervalo para la media con σ conocida?::x̄ ± z(α/2)·σ/√n
¿De dónde sale esa fórmula?::De estandarizar x̄ y reordenar la desigualdad |Z| ≤ z despejando μ. Es el mismo suceso escrito al revés
¿Qué es la cobertura nominal?::La que el método anuncia. La real es la que se mide, y no siempre coinciden
¿Qué pasa si se sustituye σ por s y se deja el cuantil z?::La cobertura se queda corta: vale exactamente P(|T_{n−1}| ≤ z), que con n = 5 al 95 % nominal es 87.8 %
¿Cómo se arregla?::Usando el cuantil de la t con n−1 grados de libertad, más ancho que el de la normal
¿Por qué n−1 grados de libertad?::Porque s se calcula restando x̄, que ya se estimó de los mismos datos. La demostración completa está en la lección 12
¿Qué es el intervalo de Wald para una proporción?::p̂ ± z·√(p̂(1−p̂)/n)
¿Cuál es su fallo?::Su cobertura tiende a cero cuando p tiende a cero. Con k = 0 el intervalo es el punto {0} y no cubre ningún p positivo
¿Por qué la cobertura tiene dientes de sierra?::Porque k es entero: al mover p de forma continua, la cobertura salta cuando un k entra o sale del conjunto de los que cubren
¿Qué hacer en la práctica con proporciones?::Usar Wilson o Agresti-Coull, no Wald, sobre todo si p está cerca de 0 o de 1
```

::: {#fuentes .fuentes}
### Fuentes

**Lo que esta página demuestra sola.** Las Proposiciones 9.3, 9.4, 9.7 y 9.9 se demuestran aquí a partir de la Proposición 6.1 y de la Definición 9.2. Las coberturas de las secciones 4 y 5 no son simuladas: la de la $t$ se obtiene integrando la densidad de la Definición 9.5 por la regla de Simpson, y la del intervalo de Wald sumando las probabilidades binomiales exactas de los $k$ que cubren cada $p$. Las dos celdas se pueden ejecutar y contrastar con el visual correspondiente.

**Lo que se enuncia sin demostrar.** El Teorema 9.6 —que $(\bar{x}-\mu)/(s/\sqrt{n})$ sigue una $t_{n-1}$— se usa sin demostración; hace falta la distribución de $Q/\sigma^2$ y la independencia entre $\bar{x}$ y $s^2$, que llegan con la normal multivariante en la lección 12. También se usa sin demostrar que la $t$ tiene colas más pesadas que la normal, aunque su consecuencia numérica se calcula en la celda de la sección 4. La fórmula del intervalo de Wilson se da sin derivar; sale de invertir la prueba de la lección 10.

**Lo que viene de los libros.**

- [*Think Stats* 3e](https://allendowney.github.io/ThinkStats/) (Downey, CC BY-NC-SA 4.0), §8.6 *Confidence Intervals* y §8.8 *Glossary* — citadas textualmente arriba.

**Lo que es mío, no del libro.** La numeración de definiciones y proposiciones. La Proposición 9.4 y su lectura, la Proposición 9.9 con la demostración por el caso $k = 0$, y el encuadre de las secciones 4 y 5 como dos fallos medibles de cobertura en vez de como recetas, son forma de presentarlo. *Think Stats* no trata el intervalo de Wald ni la distribución $t$.

[Índices verificados el 12-09-2026 contra el índice publicado del libro.]{.sello}
:::

→ Siguiente: [Pruebas de hipótesis](10-pruebas-de-hipotesis.qmd)
